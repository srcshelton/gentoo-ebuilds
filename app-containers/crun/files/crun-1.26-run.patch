--- a/src/libcrun/status.c
+++ b/src/libcrun/status.c
@@ -74,7 +74,7 @@ get_run_directory (const char *state_roo
         }
     }
   if (root == NULL)
-    root = xstrdup ("/run/crun");
+    root = xstrdup ("/var/run/crun");
 
   ret = crun_ensure_directory (root, 0700, false, err);
   if (UNLIKELY (ret < 0))
--- a/tests/test_linux_features.py
+++ b/tests/test_linux_features.py
@@ -1085,13 +1085,13 @@
         conf['mounts'] = []
     conf['mounts'].extend([
         {
-            'destination': '/run',
+            'destination': '/var/run',
             'type': 'tmpfs',
             'source': 'tmpfs',
             'options': ['nosuid', 'nodev', 'mode=755']
         },
         {
-            'destination': '/run/lock',
+            'destination': '/var/run/lock',
             'type': 'tmpfs',
             'source': 'tmpfs',
             'options': ['nosuid', 'nodev', 'noexec', 'size=5m']
--- a/tests/test_start.py
+++ b/tests/test_start.py
@@ -258,10 +258,10 @@ def test_sd_notify():
     conf['process']['args'] = ['/init', 'cat', '/proc/self/mountinfo']
     add_all_namespaces(conf)
     env = dict(os.environ)
-    env["NOTIFY_SOCKET"] = "/run/notify/the-socket"
+    env["NOTIFY_SOCKET"] = "/var/run/notify/the-socket"
     try:
         out, cid = run_and_get_output(conf, hide_stderr=True, env=env, command='run')
-        if "/run/notify/the-socket" not in str(out):
+        if "/var/run/notify/the-socket" not in str(out):
             return -1
     except:
         return -1
@@ -325,7 +325,7 @@
 
     # Check if the D-Bus system socket exists and is owned by current uid
     # If not, we can't properly use systemd/sd-notify
-    dbus_socket = '/run/dbus/system_bus_socket'
+    dbus_socket = '/var/run/dbus/system_bus_socket'
     if not os.path.exists(dbus_socket):
         return (77, "dbus socket not found")
     try:
--- a/crun.1.md
+++ b/crun.1.md
@@ -73,7 +73,7 @@ Restore a container from a checkpoint
 # STATE
 
 By default, when running as root user, crun saves its state under the
-**/run/crun** directory.  As unprivileged user, instead the
+**/var/run/crun** directory.  As unprivileged user, instead the
 *XDG_RUNTIME_DIR* environment variable is honored, and the directory
 **$XDG_RUNTIME_DIR/crun** is used.  The global option **--root**
 overrides this setting.
--- a/crun.1
+++ b/crun.1
@@ -90,7 +90,7 @@ Restore a container from a checkpoint
 
 .SH STATE
 By default, when running as root user, crun saves its state under the
-\fB/run/crun\fP directory.  As unprivileged user, instead the
+\fB/var/run/crun\fP directory.  As unprivileged user, instead the
 \fIXDG_RUNTIME_DIR\fP environment variable is honored, and the directory
 \fB$XDG_RUNTIME_DIR/crun\fP is used.  The global option \fB--root\fP
 overrides this setting.
--- a/NEWS
+++ b/NEWS
@@ -218,7 +218,7 @@
 
 * crun-1.15
 
-- fix a mount point leak under /run/crun, add a retry mechanism to
+- fix a mount point leak under /var/run/crun, add a retry mechanism to
   unmount the directory if the removal failed with EBUSY.
 - linux: cgroups: fix potential mount leak when /sys/fs/cgroup is
   already mounted, causing the posthooks to not run.
--- a/libocispec//runtime-spec/ChangeLog
+++ b/libocispec//runtime-spec/ChangeLog
@@ -885,7 +885,7 @@
 	* hooks: deduplicate the hooks docs
 	* implementations: Link to kunalkushwaha/octool
 	* implementations: Link to mrunalp/ocitools
-	* lifecycle: Don't require /run/opencontainer/<runtime>/containers
+	* lifecycle: Don't require /var/run/opencontainer/<runtime>/containers
 	* lifecycle: Mention runtime.json
 	* lifecycle: no hyphens
 	* MAINTAINERS: add tianon per the charter
