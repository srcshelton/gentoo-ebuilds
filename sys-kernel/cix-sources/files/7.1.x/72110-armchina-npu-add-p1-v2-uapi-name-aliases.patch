diff --git a/drivers/misc/armchina-npu/include/armchina_aipu.h b/drivers/misc/armchina-npu/include/armchina_aipu.h
--- a/drivers/misc/armchina-npu/include/armchina_aipu.h
+++ b/drivers/misc/armchina-npu/include/armchina_aipu.h
@@ -57,6 +57,9 @@ enum aipu_isa_version {
 	AIPU_ISA_VERSION_ZHOUYI_V3_2_1	= 8,
 };
 
+/* Source-compatible aliases for the p1_v2.0.0 UMD header names. */
+#define AIPU_ISA_VERSION_ZHOUYI_V3_2 AIPU_ISA_VERSION_ZHOUYI_V3_2_0
+
 /**
  * What is an AIPU partition?
  *
@@ -265,6 +268,8 @@ enum aipu_dma_buf_malloc_mode {
     AIPU_DMA_BUF_ATTACH_IOVA = 4,
 };
 
+#define AIPU_DMA_BUF_MALLOC_IOVA_PHY AIPU_DMA_BUF_MALLOC_BOTH
+
 /**
  * enum aipu_dma_buf_free_mode - dma buffer free mode for smmu scenario
  * @AIPU_DMA_BUF_FREE_DEFAULT:     [aipu v3_2] free I/O virtual address and physical memory by standard dma api
@@ -350,6 +355,8 @@ enum aipu_job_execution_flag {
 	AIPU_JOB_EXEC_FLAG_SEG_MMU       = 1 << 6,
 };
 
+#define AIPU_JOB_EXEC_FLAG_DBG_DISPATCH AIPU_JOB_EXEC_FLAG_BIND_DISPATCH
+
 /**
  * struct aipu_job_desc - Description of a job to be scheduled.
  * @is_defer_run:      [aipu v1/v2 only, optional] Reserve a core for this job and defer to run
