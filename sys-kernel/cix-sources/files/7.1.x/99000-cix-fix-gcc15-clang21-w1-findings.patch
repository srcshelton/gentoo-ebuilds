diff --git a/drivers/gpu/drm/cix/dptx/cix_edp_panel.c b/drivers/gpu/drm/cix/dptx/cix_edp_panel.c
--- a/drivers/gpu/drm/cix/dptx/cix_edp_panel.c
+++ b/drivers/gpu/drm/cix/dptx/cix_edp_panel.c
@@ -45,6 +45,7 @@
 #include "cix_edp_panel.h"
 
 /**
+ * struct panel_desc - fixed panel characteristics
  * @modes: Pointer to array of fixed modes appropriate for this panel.  If
  *         only one mode then this can just be the address of this the mode.
  *         NOTE: cannot be used with "timings" and also if this is specified
@@ -59,6 +60,7 @@
  * @delay: Structure containing various delay values for this panel.
  * @bus_format: See MEDIA_BUS_FMT_... defines.
  * @bus_flags: See DRM_BUS_FLAG_... defines.
+ * @connector_type: DRM connector type represented by the panel.
  */
 struct panel_desc {
 	const struct drm_display_mode *modes;
diff --git a/drivers/gpu/drm/cix/dptx/trilin_dptx.c b/drivers/gpu/drm/cix/dptx/trilin_dptx.c
--- a/drivers/gpu/drm/cix/dptx/trilin_dptx.c
+++ b/drivers/gpu/drm/cix/dptx/trilin_dptx.c
@@ -194,6 +194,7 @@ int trilin_dp_max_rate(int link_rate, u8 lane_num, u8 bpp)
  * @dp: DisplayPort IP core structure
  * @pclock: pixel clock for requested display mode
  * @current_bw: current link rate
+ * @bppex: bits per pixel, or zero to use 24 bits per pixel
  * @downshift: downshift allowed
  *
  * Find the link configuration values, rate and lane count for requested pixel
@@ -649,7 +650,7 @@ static int trilin_dp_link_configure(struct trilin_dp *dp)
 }
 
 /**
- * trilin_dp_link_train - Train the link
+ * trilin_dp_train - Train the link
  * @dp: DisplayPort IP core structure
  *
  * Return: 0 if all trains are done successfully, or corresponding error code.
@@ -949,9 +950,11 @@ static void trilin_dp_aux_cleanup(struct trilin_dp *dp)
  */
 
 /**
- * trilin_dp_encoder_mode_set_transfer_unit - Set the transfer unit values
+ * trilin_dp_mode_set_transfer_unit - Set the transfer unit values
  * @dp: DisplayPort IP core structure
+ * @source: stream source index
  * @mode: requested display mode
+ * @bpp: bits per pixel
  *
  * Set the transfer unit, and calculate all transfer unit size related values.
  * Calculation is based on DP and IP core specification.
@@ -991,7 +994,9 @@ static void trilin_dp_mode_set_transfer_unit(struct trilin_dp *dp, int source,
 /**
  * trilin_dp_mode_set_timing - Configure the main stream
  * @dp: DisplayPort IP core structure
+ * @source: stream source index
  * @mode: requested display mode
+ * @bpp: bits per pixel
  *
  * Configure the main stream based on the requested mode @mode. Calculation is
  * based on IP core specification.
@@ -1080,6 +1085,7 @@ static void trilin_dp_mode_set_timing(struct trilin_dp *dp, int source,
 
 /**
  * trilin_dp_get_audio_clk_rate - Get the current audio clock rate
+ * @dp: DisplayPort IP core structure
  *
  * Return: the current audio clock rate.
  */
@@ -1416,6 +1422,8 @@ end:
 /**
  * trilin_dp_update_misc - Write the misc registers
  * @dp: DisplayPort IP core structure
+ * @source: stream source index
+ * @config: stream configuration
  *
  * The misc register values are stored in the structure, and this
  * function applies the values into the registers.
@@ -1436,10 +1444,10 @@ static void trilin_dp_update_misc(struct trilin_dp *dp, int source,
 /**
  * trilin_dp_set_misc - Set the input format
  * @dp: DisplayPort IP core structure
- * @format: input format
- * @bpc: bits per component
+ * @source: stream source index
+ * @config: stream configuration
  *
- * Update misc register values based on input @format and @bpc.
+ * Update misc register values from @config.
  *
  * Return: 0 on success, or -EINVAL.
  */
diff --git a/drivers/gpu/drm/cix/linlon-dp/linlondp_kms.c b/drivers/gpu/drm/cix/linlon-dp/linlondp_kms.c
--- a/drivers/gpu/drm/cix/linlon-dp/linlondp_kms.c
+++ b/drivers/gpu/drm/cix/linlon-dp/linlondp_kms.c
@@ -239,6 +239,9 @@ static int linlondp_crtc_normalize_zpos(struct drm_crtc *crtc,
 
 /**
  * linlondp_vrr_match_mode - check if two modes are VRR-compatible.
+ * @mode1: first display mode
+ * @mode2: second display mode
+ *
  * I.e. if both mode have the same timings except for VFP.
  */
 static bool linlondp_vrr_match_mode(const struct drm_display_mode *mode1,
diff --git a/drivers/gpu/drm/cix/linlon-dp/linlondp_plane.c b/drivers/gpu/drm/cix/linlon-dp/linlondp_plane.c
--- a/drivers/gpu/drm/cix/linlon-dp/linlondp_plane.c
+++ b/drivers/gpu/drm/cix/linlon-dp/linlondp_plane.c
@@ -404,7 +404,7 @@ static u32 get_plane_type(struct linlondp_kms_dev *kms,
 
 /**
  * drm_plane_enable_color_mgmt - enable color management properties
- * @plane: DRM Plane
+ * @kplane: Linlon display plane
  * @plane_degamma_lut_size: the size of the degamma lut (before CSC)
  * @plane_has_ctm: whether to attach ctm_property for CSC matrix
  * @plane_gamma_lut_size: the size of the gamma lut (after CSC)
diff --git a/drivers/hwmon/cix-fan.c b/drivers/hwmon/cix-fan.c
--- a/drivers/hwmon/cix-fan.c
+++ b/drivers/hwmon/cix-fan.c
@@ -208,8 +208,8 @@ static int cix_fan_handle_ec_failure(struct cix_fan_data *data,
 static int cix_fan_get_pwm_raw(struct cix_fan_data *data, long *val)
 {
 	union acpi_object argv[] = {
-		{ .type = ACPI_TYPE_INTEGER, .integer.value = 0 },
-		{ .type = ACPI_TYPE_INTEGER, .integer.value = 0 },
+		{ .integer = { ACPI_TYPE_INTEGER, 0 } },
+		{ .integer = { ACPI_TYPE_INTEGER, 0 } },
 	};
 	struct acpi_object_list args = {
 		.count = ARRAY_SIZE(argv),
@@ -256,9 +256,9 @@ static int cix_fan_get_pwm_raw(struct cix_fan_data *data, long *val)
 static int cix_fan_set_pwm_percent(struct cix_fan_data *data, unsigned long duty)
 {
 	union acpi_object argv[] = {
-		{ .type = ACPI_TYPE_INTEGER, .integer.value = duty },
-		{ .type = ACPI_TYPE_INTEGER, .integer.value = 0 },
-		{ .type = ACPI_TYPE_INTEGER, .integer.value = 0 },
+		{ .integer = { ACPI_TYPE_INTEGER, duty } },
+		{ .integer = { ACPI_TYPE_INTEGER, 0 } },
+		{ .integer = { ACPI_TYPE_INTEGER, 0 } },
 	};
 	struct acpi_object_list args = {
 		.count = ARRAY_SIZE(argv),
diff --git a/drivers/irqchip/irq-sky1-pdc.c b/drivers/irqchip/irq-sky1-pdc.c
--- a/drivers/irqchip/irq-sky1-pdc.c
+++ b/drivers/irqchip/irq-sky1-pdc.c
@@ -136,7 +136,7 @@ static const struct irq_domain_ops pdcv1_irqchip_data_domain_ops = {
 	.free           = irq_domain_free_irqs_common,
 };
 
-static const struct of_device_id pdcv1_of_match[] = {
+static const struct of_device_id pdcv1_of_match[] __maybe_unused = {
 	{ .compatible = "cix,sky1-pdc",  .data = (const void *) 2 },
 	{ /* END */ }
 };
diff --git a/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_config.c b/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_config.c
--- a/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_config.c
+++ b/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_config.c
@@ -710,7 +710,7 @@ static void armcb_v4l2_config_queue_event(struct video_device *pvdev,
 	v4l2_event_queue(pvdev, &ev);
 }
 
-/**
+/*
  * @description: record interrupt mask and mask interrupt
  * @param {u32} *mask: pointer to interrupt mask
  * @return {*}
@@ -721,7 +721,7 @@ static inline void armcb_i5_mask_int(u32 *mask)
 	armcb_isp_write_reg(I5_INT_MASK_ADDR, 0xFFFFFFFF);
 }
 
-/**
+/*
  * @description: set register mask register to original value
  * @param {u32} mask: original interrupt mask
  * @return {*}
@@ -731,7 +731,7 @@ static inline void armcb_i5_unmask_int(u32 mask)
 	armcb_isp_write_reg(I5_INT_MASK_ADDR, mask);
 }
 
-/**
+/*
  * @description: i5 clear interupt
  * @param {u32} status: current interrupt status
  * @return {*}
@@ -744,7 +744,7 @@ static inline void armcb_i5_clear_int(u32 status)
 	armcb_isp_write_reg(I5_INT_CLEAR_ADDR, 0x0);
 }
 
-/**
+/*
  * @description: process afbc error interrupt
  * @param {isp_irq_info} *pirq_info: pointer to irq information
  * @return {*}
@@ -910,7 +910,7 @@ static struct isp_irq_info *armcb_irq_msg_ev_alloc(struct armcb_irq_msg_fh *fh)
 	return info;
 }
 
-/**
+/*
  * @description: record interrupt mask and mask interrupt
  * @param {u32} *pnormal_mask: pointer to normal mask
  * @param {u32} *perror_mask: pointer to error mask
@@ -924,7 +924,7 @@ static inline void armcb_i7_mask_int(u32 *pnormal_mask, u32 *perror_mask)
 	armcb_isp_write_reg(I7_INT_ERR_MASK_ADDR, 0xFFFFFFFF);
 }
 
-/**
+/*
  * @description: set register mask register to original value
  * @param {u32} normal_mask: original normal mask
  * @param {u32} error_mask: original error mask
@@ -957,7 +957,7 @@ static inline void armcb_i7_unmask_int(u32 normal_mask, u32 error_mask)
 	armcb_isp_write_reg(I7_INT_ERR_MASK_ADDR, error_mask);
 }
 
-/**
+/*
  * @description: clear normal interrupt
  * @param {u32} status: current normal interrupt status
  * @return {*}
@@ -969,7 +969,7 @@ static inline void armcb_i7_clear_normal_int(u32 status)
 			    status | I7_INT_VIN_EXPX_INT_MASK);
 }
 
-/**
+/*
  * @description: clear error interrupt
  * @param {u32} status: current error interrupt status
  * @return {*}
@@ -980,7 +980,7 @@ static inline void armcb_i7_clear_error_int(u32 status)
 	armcb_isp_write_reg(I7_INT_ERR_CLEAR_ADDR, status);
 }
 
-/**
+/*
  * @description: get sof frame count and update id and frame_cnt_sel
  * @param {isp_irq_info} *pirq_info: pointer to irq information
  * @param {i7_index_type_t} *pint_id: pointer to id of interrupt
@@ -1064,7 +1064,7 @@ static inline void armcb_i7_get_sof_frm_cnt(struct isp_irq_info *pirq_info,
 	}
 }
 
-/**
+/*
  * @description: get sol and 3a frame count
  * @param {isp_irq_info} *pirq_info: pointer to irq information
  * @param {i7_index_type_t} *pint_id: pointer to id of interrupt
@@ -1096,7 +1096,7 @@ armcb_i7_get_sol_3a_frm_cnt(struct isp_irq_info *pirq_info,
 	pirq_info->frm_cnt_3a = armcb_isp_read_reg(I7_INT_3A_FRMCNT_ADDR);
 }
 
-/**
+/*
  * @description: get next sol frame count in multi-cam cases
  * @param {isp_irq_info} *pirq_info: pointer to irq information
  * @param {i7_index_type_t} *pint_id: pointer to id of interrupt
@@ -1118,7 +1118,7 @@ armcb_i7_get_nxt_sol_frm_cnt(struct isp_irq_info *pirq_info,
 	pirq_info->frm_cnt_nxt_sol = armcb_isp_read_reg(I7_INT_SOL_FRMCNT_ADDR);
 }
 
-/**
+/*
  * @description: process afbc error interrupt
  * @param {isp_irq_info} *pirq_info: pointer to irq information
  * @return {*}
diff --git a/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_core.c b/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_core.c
--- a/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_core.c
+++ b/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_core.c
@@ -537,7 +537,7 @@ static int armcb_v4l2_post_streamoff_event(armcb_v4l2_dev_t *dev)
 	return 0;
 }
 
-/**
+/*
  * Release one stream slot: clear g_outport_map, stream_id_index, deinit stream,
  * clear pstreams[stream_id] and stream_mask. Caller must hold dev->queue_lock.
  */
diff --git a/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_stream.c b/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_stream.c
--- a/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_stream.c
+++ b/drivers/media/platform/cix/armcb-isp/isp/armcb_v4l2_stream.c
@@ -189,6 +189,8 @@ int armcb_v4l2_stream_init(armcb_v4l2_stream_t **ppstream, int stream_id,
 
 /**
  * armcb_v4l2_stream_clear_buffer_lists - Clear stream buffer lists without
+ * @pstream: stream whose queued buffer lists should be cleared
+ *
  * completing buffers. Call only after vb2_streamoff has already completed
  * buffers (e.g. to avoid double vb2_buffer_done). Caller must hold
  * appropriate lock (e.g. queue_lock).
diff --git a/drivers/net/ethernet/realtek/r8126/r8126.h b/drivers/net/ethernet/realtek/r8126/r8126.h
--- a/drivers/net/ethernet/realtek/r8126/r8126.h
+++ b/drivers/net/ethernet/realtek/r8126/r8126.h
@@ -2855,7 +2855,7 @@ enum mcfg {
 #define HW_PATCH_SOC_LAN (BIT_0)
 #define HW_PATCH_SAMSUNG_LAN_DONGLE (BIT_2)
 
-static const u16 other_q_intr_mask = (RxOK1 | RxDU1);
+#define OTHER_Q_INTR_MASK (RxOK1 | RxDU1)
 
 void rtl8126_mdio_write(struct rtl8126_private *tp, u16 RegAddr, u16 value);
 void rtl8126_mdio_prot_write(struct rtl8126_private *tp, u32 RegAddr, u32 value);
diff --git a/drivers/net/ethernet/realtek/r8126/r8126_n.c b/drivers/net/ethernet/realtek/r8126/r8126_n.c
--- a/drivers/net/ethernet/realtek/r8126/r8126_n.c
+++ b/drivers/net/ethernet/realtek/r8126/r8126_n.c
@@ -3884,7 +3884,7 @@ rtl8126_enable_hw_interrupt(struct rtl8126_private *tp)
                 if (R8126_MULTI_RX_Q(tp)) {
                         int i;
                         for (i=1; i<tp->num_rx_rings; i++)
-                                RTL_W16(tp, tp->imr_reg[i], other_q_intr_mask);
+                                RTL_W16(tp, tp->imr_reg[i], OTHER_Q_INTR_MASK);
                 }
                 break;
         }
@@ -13711,15 +13711,12 @@ rtl8126_init_one(struct pci_dev *pdev,
         struct net_device *dev = NULL;
         struct rtl8126_private *tp;
         void __iomem *ioaddr = NULL;
-        static int board_idx = -1;
 
         int rc;
 
         assert(pdev != NULL);
         assert(ent != NULL);
 
-        board_idx++;
-
         if (netif_msg_drv(&debug))
                 printk(KERN_INFO "%s Ethernet controller driver %s loaded\n",
                        MODULENAME, RTL8126_VERSION);
@@ -15608,7 +15605,6 @@ rtl8126_xmit_frags(struct rtl8126_private *tp,
         unsigned int cur_frag, entry;
         struct TxDesc *txd = NULL;
         const unsigned char nr_frags = info->nr_frags;
-        unsigned long PktLenCnt = 0;
         bool LsoPatchEnabled = FALSE;
 
         entry = ring->cur_tx;
@@ -15650,7 +15646,6 @@ rtl8126_xmit_frags(struct rtl8126_private *tp,
                 wmb();
                 txd->opts1 = cpu_to_le32(status);
 
-                PktLenCnt += len;
         }
 
         return cur_frag;
@@ -15767,11 +15762,6 @@ rtl8126_tso_csum(struct sk_buff *skb,
 
                 /* TCP Segmentation Offload (or TCP Large Send) */
                 if (mss) {
-                        union {
-                                struct iphdr *v4;
-                                struct ipv6hdr *v6;
-                                unsigned char *hdr;
-                        } ip;
                         union {
                                 struct tcphdr *tcp;
                                 struct udphdr *udp;
@@ -15779,7 +15769,6 @@ rtl8126_tso_csum(struct sk_buff *skb,
                         } l4;
                         u32 l4_offset, hdr_len;
 
-                        ip.hdr = skb_network_header(skb);
                         l4.hdr = skb_checksum_start(skb);
 
                         l4_offset = skb_transport_offset(skb);
diff --git a/drivers/phy/cix/phy-cix-usbdp.c b/drivers/phy/cix/phy-cix-usbdp.c
--- a/drivers/phy/cix/phy-cix-usbdp.c
+++ b/drivers/phy/cix/phy-cix-usbdp.c
@@ -1069,6 +1069,8 @@ static int udphy_dp_phy_set_rate(struct cix_udphy *udphy,
 		ret = regmap_multi_reg_write(udphy->phy_regmap, sky1_udphy_dp_partial_conf,
 						ARRAY_SIZE(sky1_udphy_dp_partial_conf));
 	}
+	if (ret)
+		return ret;
 
 	switch (dp->link_rate) {
 	case 0x6:  conf = &link_cfg[0]; break;
diff --git a/drivers/pinctrl/cix/pinctrl-sky1-base.c b/drivers/pinctrl/cix/pinctrl-sky1-base.c
--- a/drivers/pinctrl/cix/pinctrl-sky1-base.c
+++ b/drivers/pinctrl/cix/pinctrl-sky1-base.c
@@ -157,9 +157,14 @@ sky1_pinctrl_find_pin_group(struct pinctrl_dev *pctldev,
 
 	ret = pinmux_generic_get_function_groups(pctldev, selector, &groups,
 						 &num_groups);
+	if (ret)
+		return NULL;
+
 	for (i = 0; i < num_groups; i++) {
 		ret = pinctrl_get_group_pins(pctldev, groups[i], &pins,
 					     &num_pins);
+		if (ret)
+			continue;
 		if (npins_in != num_pins)
 			continue;
 		for (j = 0; j < num_pins; j++) {
@@ -427,6 +432,7 @@ static void sky1_pinconf_group_dbg_show(struct pinctrl_dev *pctldev,
 
 		name = pin_get_name(pctldev, pin_id);
 		ret = sky1_pinconf_get(pctldev, pin_id, &config);
+		if (ret)
 			return;
 		seq_printf(s, "  %s: 0x%lx\n", name, config);
 	}
diff --git a/drivers/pmdomain/core.c b/drivers/pmdomain/core.c
--- a/drivers/pmdomain/core.c
+++ b/drivers/pmdomain/core.c
@@ -3461,6 +3461,10 @@ static int genpd_get_acpi_power_domain_args(struct device *dev,
 
 /**
  * __genpd_dev_pm_attach_acpi - ACPI variant of __genpd_dev_pm_attach
+ * @dev: Device to attach to the power domain
+ * @base_dev: Device whose firmware node contains the power-domain reference
+ * @index: Power-domain reference index
+ * @power_on: Whether to power on the domain after attaching
  *
  * Uses fwnode_property_get_reference_args() on the base device's "power-domains"
  * property to resolve the PM domain provider and index, then attaches @dev to
diff --git a/drivers/tty/serial/amba-pl011.c b/drivers/tty/serial/amba-pl011.c
--- a/drivers/tty/serial/amba-pl011.c
+++ b/drivers/tty/serial/amba-pl011.c
@@ -180,7 +180,7 @@ static struct vendor_data vendor_arm = {
 	.get_fifosize		= get_fifosize_arm,
 };
 
-static const struct vendor_data vendor_sbsa = {
+static const struct vendor_data vendor_sbsa __maybe_unused = {
 	.reg_offset		= pl011_std_offsets,
 	.fr_busy		= UART01x_FR_BUSY,
 	.fr_dsr			= UART01x_FR_DSR,
@@ -2468,7 +2468,7 @@ static unsigned int sbsa_uart_get_mctrl(struct uart_port *port)
 	return 0;
 }
 
-static const struct uart_ops sbsa_uart_pops = {
+static const struct uart_ops sbsa_uart_pops __maybe_unused = {
 	.tx_empty	= pl011_tx_empty,
 	.set_mctrl	= sbsa_uart_set_mctrl,
 	.get_mctrl	= sbsa_uart_get_mctrl,
diff --git a/sound/soc/cix/card-utils.c b/sound/soc/cix/card-utils.c
--- a/sound/soc/cix/card-utils.c
+++ b/sound/soc/cix/card-utils.c
@@ -109,16 +109,9 @@ static int cix_dailink_parsing_fmt(struct device_node *np,
 static bool cix_dailink_status_check(struct device_node *np)
 {
 	const char *status;
-	int ret;
-
-	ret = of_property_read_string(np, "status", &status);
 
-	if (status) {
-		if (!strcmp(status, "okay") || !strcmp(status, "ok"))
-			return true;
-		else
-			return false;
-	}
+	if (!of_property_read_string(np, "status", &status))
+		return !strcmp(status, "okay") || !strcmp(status, "ok");
 
 	return true;
 }
