Retain the reviewed ACPI _STR label conversion and thermal-hwmon naming
improvements without the Orion-specific ECTZ filtering owned by 30127.
Mechanically generated from the exact Linux 7.1.3 predecessor tree.

diff --git a/drivers/acpi/thermal.c b/drivers/acpi/thermal.c
--- a/drivers/acpi/thermal.c
+++ b/drivers/acpi/thermal.c
@@ -23,6 +23,7 @@
 #include <linux/types.h>
 #include <linux/jiffies.h>
 #include <linux/kmod.h>
+#include <linux/nls.h>
 #include <linux/reboot.h>
 #include <linux/sched.h>
 #include <linux/device.h>
@@ -122,6 +123,7 @@ struct acpi_thermal {
 	volatile u8 zombie;
 	bool has_last_good_temp;
 	bool orion_o6_ectz;
+	char *label;
 	struct acpi_thermal_trips trips;
 	struct thermal_zone_device *thermal_zone;
 	int kelvin_offset;	/* in millidegrees */
@@ -171,6 +173,46 @@ static int acpi_thermal_read_tmp(struct acpi_thermal *tz,
 	return 0;
 }
 
+static char *acpi_thermal_get_label(struct acpi_device *device)
+{
+	struct acpi_buffer buffer = { ACPI_ALLOCATE_BUFFER, NULL };
+	union acpi_object *str_obj;
+	acpi_status status;
+	char *label = NULL;
+	int label_len;
+	int maxout;
+
+	status = acpi_evaluate_object_typed(device->handle, "_STR", NULL,
+					    &buffer, ACPI_TYPE_BUFFER);
+	if (ACPI_FAILURE(status))
+		return NULL;
+
+	str_obj = buffer.pointer;
+	if (!str_obj->buffer.pointer || !str_obj->buffer.length)
+		goto out_free;
+
+	if (str_obj->buffer.length > (INT_MAX - 1) / 2)
+		goto out_free;
+
+	maxout = str_obj->buffer.length * 2 + 1;
+	label = kzalloc(maxout, GFP_KERNEL);
+	if (!label)
+		goto out_free;
+
+	label_len = utf16s_to_utf8s((wchar_t *)str_obj->buffer.pointer,
+				    str_obj->buffer.length / sizeof(wchar_t),
+				    UTF16_LITTLE_ENDIAN, label, maxout - 1);
+	if (label_len <= 0) {
+		kfree(label);
+		label = NULL;
+	}
+
+out_free:
+	ACPI_FREE(buffer.pointer);
+
+	return label;
+}
+
 static void acpi_thermal_save_last_good_tmp(struct acpi_thermal *tz,
 					    unsigned long long tmp)
 {
@@ -764,16 +806,20 @@ static int acpi_thermal_register_thermal_zone(struct acpi_thermal *tz,
 					      int passive_delay)
 {
 	const char *tz_type = acpi_device_bid(tz->device);
+	struct thermal_zone_params tzp = {
+		.label = tz->label,
+		.hwmon_type = "acpitz",
+	};
 	int result;
 
 	if (trip_count)
 		tz->thermal_zone = thermal_zone_device_register_with_trips(
 					tz_type, trip_table, trip_count, tz,
-					&acpi_thermal_zone_ops, NULL, passive_delay,
+					&acpi_thermal_zone_ops, &tzp, passive_delay,
 					tz->polling_frequency * 100);
 	else
 		tz->thermal_zone = thermal_tripless_zone_device_register(
-					tz_type, tz, &acpi_thermal_zone_ops, NULL);
+					tz_type, tz, &acpi_thermal_zone_ops, &tzp);
 
 	if (IS_ERR(tz->thermal_zone))
 		return PTR_ERR(tz->thermal_zone);
@@ -917,6 +963,7 @@ static void acpi_thermal_free_thermal_zone(struct acpi_thermal *tz)
 	for (i = 0; i < ACPI_THERMAL_MAX_ACTIVE; i++)
 		acpi_handle_list_free(&tz->trips.active[i].trip.devices);
 
+	kfree(tz->label);
 	kfree(tz);
 }
 
@@ -943,6 +990,7 @@ static int acpi_thermal_probe(struct platform_device *pdev)
 
 	tz->device = device;
 	tz->orion_o6_ectz = acpi_thermal_is_radxa_orion_o6_ectz(tz);
+	tz->label = acpi_thermal_get_label(device);
 	strscpy(tz->name, device->pnp.bus_id);
 
 	acpi_thermal_aml_dependency_fix(tz);
diff --git a/drivers/thermal/thermal_hwmon.c b/drivers/thermal/thermal_hwmon.c
--- a/drivers/thermal/thermal_hwmon.c
+++ b/drivers/thermal/thermal_hwmon.c
@@ -33,7 +33,9 @@ struct thermal_hwmon_attr {
 struct thermal_hwmon_temp {
 	struct thermal_zone_device *tz;
 	struct thermal_hwmon_attr temp_input;	/* hwmon sys attr */
+	struct thermal_hwmon_attr temp_label;	/* hwmon sys attr */
 	struct thermal_hwmon_attr temp_crit;	/* hwmon sys attr */
+	bool temp_label_present;
 	bool temp_crit_present;
 };
 
@@ -70,6 +72,22 @@ temp_input_show(struct device *dev, struct device_attribute *attr, char *buf)
 	return sysfs_emit(buf, "%d\n", temperature);
 }
 
+static ssize_t
+temp_label_show(struct device *dev, struct device_attribute *attr, char *buf)
+{
+	struct thermal_hwmon_attr *hwmon_attr =
+		container_of(attr, struct thermal_hwmon_attr, attr);
+	struct thermal_hwmon_temp *temp =
+		container_of(hwmon_attr, struct thermal_hwmon_temp, temp_label);
+	struct thermal_zone_device *tz = temp->tz;
+	const char *label = tz->tzp ? tz->tzp->label : NULL;
+
+	if (!label)
+		return -ENODATA;
+
+	return sysfs_emit(buf, "%s\n", label);
+}
+
 static ssize_t
 temp_crit_show(struct device *dev, struct device_attribute *attr, char *buf)
 {
@@ -97,6 +115,20 @@ static bool thermal_zone_crit_temp_valid(struct thermal_zone_device *tz)
 	return tz->ops.get_crit_temp && !tz->ops.get_crit_temp(tz, &temp);
 }
 
+static bool thermal_zone_hwmon_label_valid(struct thermal_zone_device *tz)
+{
+	return tz->tzp && tz->tzp->label && tz->tzp->label[0];
+}
+
+static const char *
+thermal_hwmon_type(const struct thermal_zone_device *tz)
+{
+	if (tz->tzp && tz->tzp->hwmon_type && tz->tzp->hwmon_type[0])
+		return tz->tzp->hwmon_type;
+
+	return tz->type;
+}
+
 int thermal_add_hwmon_sysfs(struct thermal_zone_device *tz)
 {
 	struct thermal_hwmon_device *hwmon;
@@ -111,7 +143,8 @@ int thermal_add_hwmon_sysfs(struct thermal_zone_device *tz)
 	 * Append the thermal zone ID preceded by an underline character to the
 	 * type to disambiguate the sensors command output.
 	 */
-	scnprintf(hwmon->name, THERMAL_HWMON_NAME_LENGTH, "%s_%d", tz->type, tz->id);
+	scnprintf(hwmon->name, THERMAL_HWMON_NAME_LENGTH, "%s_%d",
+		  thermal_hwmon_type(tz), tz->id);
 	strreplace(hwmon->name, '-', '_');
 	hwmon->device = hwmon_device_register_for_thermal(&tz->device,
 							  hwmon->name, hwmon);
@@ -132,6 +165,19 @@ int thermal_add_hwmon_sysfs(struct thermal_zone_device *tz)
 	if (result)
 		goto unregister_name;
 
+	if (thermal_zone_hwmon_label_valid(tz)) {
+		temp->temp_label.attr.attr.name = "temp1_label";
+		temp->temp_label.attr.attr.mode = 0444;
+		temp->temp_label.attr.show = temp_label_show;
+		sysfs_attr_init(&temp->temp_label.attr.attr);
+		result = device_create_file(hwmon->device,
+					    &temp->temp_label.attr);
+		if (result)
+			goto unregister_input;
+
+		temp->temp_label_present = true;
+	}
+
 	if (thermal_zone_crit_temp_valid(tz)) {
 		temp->temp_crit.attr.attr.name = "temp1_crit";
 		temp->temp_crit.attr.attr.mode = 0444;
@@ -140,7 +186,7 @@ int thermal_add_hwmon_sysfs(struct thermal_zone_device *tz)
 		result = device_create_file(hwmon->device,
 					    &temp->temp_crit.attr);
 		if (result)
-			goto unregister_input;
+			goto unregister_label;
 
 		temp->temp_crit_present = true;
 	}
@@ -152,6 +198,9 @@ int thermal_add_hwmon_sysfs(struct thermal_zone_device *tz)
 
 	return 0;
 
+ unregister_label:
+	if (temp->temp_label_present)
+		device_remove_file(hwmon->device, &temp->temp_label.attr);
  unregister_input:
 	device_remove_file(hwmon->device, &temp->temp_input.attr);
  unregister_name:
@@ -191,6 +240,8 @@ void thermal_remove_hwmon_sysfs(struct thermal_zone_device *tz)
 	temp = &hwmon->tz_temp;
 
 	device_remove_file(hwmon->device, &temp->temp_input.attr);
+	if (temp->temp_label_present)
+		device_remove_file(hwmon->device, &temp->temp_label.attr);
 	if (temp->temp_crit_present)
 		device_remove_file(hwmon->device, &temp->temp_crit.attr);
 
diff --git a/include/linux/thermal.h b/include/linux/thermal.h
--- a/include/linux/thermal.h
+++ b/include/linux/thermal.h
@@ -152,6 +152,17 @@ struct thermal_zone_params {
 	 */
 	bool no_hwmon;
 
+	/*
+	 * Optional human-readable label for the thermal zone's hwmon input.
+	 */
+	const char *label;
+
+	/*
+	 * Optional hwmon device base name.  When unset, the thermal zone type
+	 * is used.  The thermal zone ID is appended to make the name unique.
+	 */
+	const char *hwmon_type;
+
 	/*
 	 * Sustainable power (heat) that this thermal zone can dissipate in
 	 * mW
