diff --git a/drivers/misc/armchina-npu/aipu.c b/drivers/misc/armchina-npu/aipu.c
--- a/drivers/misc/armchina-npu/aipu.c
+++ b/drivers/misc/armchina-npu/aipu.c
@@ -25,10 +25,6 @@
 #include <linux/pm_runtime.h>
 #include <linux/pm_domain.h>
 
-#ifdef CONFIG_SKY1
-#include "cix_sky1_soc.h"
-#endif
-
 static struct aipu_priv *aipu;
 static DEFINE_MUTEX(aipu_lock);
 
@@ -45,6 +41,22 @@ static struct aipu_priv *aipu_get_global(void)
 	return priv;
 }
 
+static int aipu_soc_pm_get(struct aipu_priv *priv)
+{
+	if (!priv->soc_ops || !priv->soc_ops->soc_pm_runtime_get_sync)
+		return 0;
+
+	return priv->soc_ops->soc_pm_runtime_get_sync(priv->dev, priv->soc);
+}
+
+static int aipu_soc_pm_put(struct aipu_priv *priv)
+{
+	if (!priv->soc_ops || !priv->soc_ops->soc_pm_runtime_put)
+		return 0;
+
+	return priv->soc_ops->soc_pm_runtime_put(priv->dev, priv->soc);
+}
+
 extern struct gpio_desc *__must_check devm_gpiod_get_optional(struct device *dev,
 						       const char *con_id,
 						       enum gpiod_flags flags);
@@ -57,17 +69,16 @@ static int aipu_open(struct inode *inode, struct file *filp)
 	if (!priv)
 		return -ENODEV;
 
-	filp->private_data = priv;
-
-#ifdef CONFIG_SKY1
-	sky1_npu_pm_runtime_get_sync(priv->dev);
-#endif
+	ret = aipu_soc_pm_get(priv);
+	if (ret) {
+		aipu_priv_put(priv);
+		return ret;
+	}
 
+	filp->private_data = priv;
 	ret = aipu_priv_check_status(priv);
 	if (ret) {
-#ifdef CONFIG_SKY1
-		sky1_npu_pm_runtime_put(priv->dev);
-#endif
+		aipu_soc_pm_put(priv);
 		filp->private_data = NULL;
 		aipu_priv_put(priv);
 	}
@@ -77,6 +88,7 @@ static int aipu_open(struct inode *inode, struct file *filp)
 
 static int aipu_release(struct inode *inode, struct file *filp)
 {
+	int pm_ret;
 	int ret = 0;
 	struct aipu_priv *aipu = filp->private_data;
 
@@ -89,10 +101,9 @@ static int aipu_release(struct inode *inode, struct file *filp)
 
 	aipu_detach_dma_bufs(&aipu->mm, filp);
 	aipu_mm_free_buffers(&aipu->mm, filp);
-
-#ifdef CONFIG_SKY1
-	sky1_npu_pm_runtime_put(aipu->dev);
-#endif
+	pm_ret = aipu_soc_pm_put(aipu);
+	if (!ret)
+		ret = pm_ret;
 
 	filp->private_data = NULL;
 	aipu_priv_put(aipu);
@@ -623,12 +634,26 @@ EXPORT_SYMBOL(armchina_aipu_remove);
 int armchina_aipu_suspend(struct platform_device *p_dev, pm_message_t state)
 {
 	struct aipu_partition *partition = platform_get_drvdata(p_dev);
+	struct aipu_priv *priv;
+	int rollback_ret;
 	int ret = 0;
 
+	if (!partition || !aipu_priv_get(partition->priv))
+		return -ENODEV;
+	priv = partition->priv;
 	ret = aipu_job_manager_suspend(get_job_manager(partition));
 
-	if (ret == 0 && aipu && aipu->soc_ops && aipu->soc_ops->disable_clk)
-		aipu->soc_ops->disable_clk(partition->dev, aipu->soc);
+	if (!ret && priv->soc_ops && priv->soc_ops->disable_clk) {
+		ret = priv->soc_ops->disable_clk(partition->dev, priv->soc);
+		if (ret) {
+			rollback_ret = aipu_job_manager_resume(get_job_manager(partition));
+			if (rollback_ret)
+				dev_err(partition->dev,
+					"failed to restore cluster state after suspend error: %d\n",
+					rollback_ret);
+		}
+	}
+	aipu_priv_put(priv);
 	return ret;
 }
 EXPORT_SYMBOL(armchina_aipu_suspend);
@@ -645,13 +670,30 @@ EXPORT_SYMBOL(armchina_aipu_suspend);
 int armchina_aipu_resume(struct platform_device *p_dev)
 {
 	struct aipu_partition *partition = platform_get_drvdata(p_dev);
+	struct aipu_priv *priv;
+	int rollback_ret;
 	int ret = 0;
 
-	ret = aipu_job_manager_resume(get_job_manager(partition));
+	if (!partition || !aipu_priv_get(partition->priv))
+		return -ENODEV;
+	priv = partition->priv;
+	if (priv->soc_ops && priv->soc_ops->enable_clk) {
+		ret = priv->soc_ops->enable_clk(partition->dev, priv->soc);
+		if (ret)
+			goto out;
+	}
 
-	if (ret == 0 && aipu && aipu->soc_ops && aipu->soc_ops->enable_clk)
-		aipu->soc_ops->enable_clk(partition->dev, aipu->soc);
+	ret = aipu_job_manager_resume(get_job_manager(partition));
+	if (ret && priv->soc_ops && priv->soc_ops->disable_clk) {
+		rollback_ret = priv->soc_ops->disable_clk(partition->dev, priv->soc);
+		if (rollback_ret)
+			dev_err(partition->dev,
+				"failed to disable clock after resume error: %d\n",
+				rollback_ret);
+	}
 
+out:
+	aipu_priv_put(priv);
 	return ret;
 }
 EXPORT_SYMBOL(armchina_aipu_resume);
diff --git a/drivers/misc/armchina-npu/aipu_common.c b/drivers/misc/armchina-npu/aipu_common.c
--- a/drivers/misc/armchina-npu/aipu_common.c
+++ b/drivers/misc/armchina-npu/aipu_common.c
@@ -6,35 +6,58 @@
 #include <linux/of.h>
 #include "aipu_common.h"
 #include "aipu_partition.h"
-#ifdef CONFIG_SKY1
-#include "cix_sky1_soc.h"
-#endif
 
 #define MAX_CHAR_SYSFS 4096
 #define ADDR_VALUE 2
 
 #ifdef CONFIG_SYSFS
+static struct aipu_partition *aipu_common_get_partition(struct device *dev)
+{
+	return platform_get_drvdata(to_platform_device(dev));
+}
+
+static int aipu_common_pm_get(struct aipu_partition *partition)
+{
+	struct aipu_soc_operations *ops = get_soc_ops(partition);
+
+	if (!ops || !ops->soc_pm_runtime_get_sync)
+		return 0;
+
+	return ops->soc_pm_runtime_get_sync(partition->dev, get_soc(partition));
+}
+
+static int aipu_common_pm_put(struct aipu_partition *partition)
+{
+	struct aipu_soc_operations *ops = get_soc_ops(partition);
+
+	if (!ops || !ops->soc_pm_runtime_put)
+		return 0;
+
+	return ops->soc_pm_runtime_put(partition->dev, get_soc(partition));
+}
+
 ssize_t aipu_common_ext_register_sysfs_show(struct device *dev,
 					    struct device_attribute *attr,
 					    char *buf)
 {
+	struct aipu_partition *partition = aipu_common_get_partition(dev);
+	int pm_ret;
 	int ret = 0;
 	char tmp[512];
-	struct platform_device *p_dev = container_of(dev, struct platform_device, dev);
-	struct aipu_priv *aipu = platform_get_drvdata(p_dev);
-	struct aipu_partition *partition = aipu->partitions;
 
-	if (unlikely(!partition))
-		return 0;
+	if (unlikely(!partition || !partition->ops))
+		return -ENODEV;
+
+	ret = aipu_common_pm_get(partition);
+	if (ret)
+		return ret;
 
-#ifdef CONFIG_SKY1
-       sky1_npu_pm_runtime_get_sync(aipu->dev);
-#endif
 	if (get_soc_ops(partition) &&
 	    get_soc_ops(partition)->is_clk_enabled &&
 	    !get_soc_ops(partition)->is_clk_enabled(dev, get_soc(partition))) {
-		return snprintf(buf, MAX_CHAR_SYSFS,
-				"AIPU is suspended and external registers cannot be read!\n");
+		ret = snprintf(buf, MAX_CHAR_SYSFS,
+			       "AIPU is suspended and external registers cannot be read!\n");
+		goto out_pm;
 	}
 
 	ret += snprintf(tmp, 512, "----------------------------------------------\n");
@@ -51,9 +74,10 @@ ssize_t aipu_common_ext_register_sysfs_show(struct device *dev,
 	ret += snprintf(tmp, 512, "----------------------------------------------\n");
 	strcat(buf, tmp);
 
-#ifdef CONFIG_SKY1
-       sky1_npu_pm_runtime_put(aipu->dev);
-#endif
+out_pm:
+	pm_ret = aipu_common_pm_put(partition);
+	if (!ret)
+		ret = pm_ret;
 	return ret;
 }
 
@@ -61,54 +85,66 @@ ssize_t aipu_common_ext_register_sysfs_store(struct device *dev,
 					     struct device_attribute *attr,
 					     const char *buf, size_t count)
 {
-	struct platform_device *p_dev = container_of(dev, struct platform_device, dev);
-	struct aipu_priv *aipu = platform_get_drvdata(p_dev);
-	struct aipu_partition *partition = aipu->partitions;
+	struct aipu_partition *partition = aipu_common_get_partition(dev);
+	struct aipu_io_req io_req = { 0 };
+	int pm_ret;
 	int ret = 0;
 	int value[2] = { 0 };
-	struct aipu_io_req io_req;
+
+	if (unlikely(!partition || !partition->ops || !partition->ops->io_rw))
+		return -ENODEV;
+
+	ret = aipu_common_pm_get(partition);
+	if (ret)
+		return ret;
 
 	if (get_soc_ops(partition) &&
 	    get_soc_ops(partition)->is_clk_enabled &&
-	    !get_soc_ops(partition)->is_clk_enabled(dev, get_soc(partition)))
-		return 0;
-
-#ifdef CONFIG_SKY1
-       sky1_npu_pm_runtime_get_sync(aipu->dev);
-#endif
+	    !get_soc_ops(partition)->is_clk_enabled(dev, get_soc(partition))) {
+		ret = -EIO;
+		goto out_pm;
+	}
 
 	ret = sscanf(buf, "%x %x", &value[0], &value[1]);
 	if (ret == ADDR_VALUE) {
+		io_req.partition_id = partition->id;
+		io_req.reg_type = AIPU_HOST_REG;
 		io_req.rw = AIPU_IO_WRITE;
 		io_req.offset = value[0];
 		io_req.value = value[1];
 		dev_info(dev, "[SYSFS] write 0x%x into register offset 0x%x", value[1], value[0]);
 		if (io_req.offset % 4 == 0) {
-			partition->ops->io_rw(partition, &io_req);
+			ret = partition->ops->io_rw(partition, &io_req);
+			if (ret)
+				goto out_pm;
 		} else {
 			dev_err(dev, "0x%x invalid address! 4 bytes alignment!\n", value[0]);
-			return -EINVAL;
+			ret = -EINVAL;
+			goto out_pm;
 		}
 	} else {
 		dev_info(dev, "echo \"addr_hex  value_hex\" > ext_registers\n");
-		return -EINVAL;
+		ret = -EINVAL;
+		goto out_pm;
 	}
 
+	ret = count;
 
-#ifdef CONFIG_SKY1
-       sky1_npu_pm_runtime_put(aipu->dev);
-#endif
-
-	return count;
+out_pm:
+	pm_ret = aipu_common_pm_put(partition);
+	if (ret >= 0 && pm_ret)
+		ret = pm_ret;
+	return ret;
 }
 
 ssize_t aipu_common_clock_sysfs_show(struct device *dev,
 				     struct device_attribute *attr,
 				     char *buf)
 {
-	struct platform_device *p_dev = container_of(dev, struct platform_device, dev);
-	struct aipu_priv *aipu = platform_get_drvdata(p_dev);
-	struct aipu_partition *partition = aipu->partitions;
+	struct aipu_partition *partition = aipu_common_get_partition(dev);
+
+	if (unlikely(!partition))
+		return -ENODEV;
 
 	/*
 	 * If SoC level provides no clock operations,
@@ -129,9 +165,7 @@ ssize_t aipu_common_clock_sysfs_store(struct device *dev,
 {
 	int do_suspend = 0;
 	int do_resume = 0;
-	struct platform_device *p_dev = container_of(dev, struct platform_device, dev);
-	struct aipu_priv *aipu = platform_get_drvdata(p_dev);
-	struct aipu_partition *partition = aipu->partitions;
+	struct aipu_partition *partition = aipu_common_get_partition(dev);
 
 	if (unlikely(!partition))
 		return count;
@@ -166,9 +200,10 @@ ssize_t aipu_common_clock_sysfs_store(struct device *dev,
 ssize_t aipu_common_disable_sysfs_show(struct device *dev, struct device_attribute *attr,
 				       char *buf)
 {
-	struct platform_device *p_dev = container_of(dev, struct platform_device, dev);
-	struct aipu_priv *aipu = platform_get_drvdata(p_dev);
-	struct aipu_partition *partition = aipu->partitions;
+	struct aipu_partition *partition = aipu_common_get_partition(dev);
+
+	if (unlikely(!partition))
+		return -ENODEV;
 
 	if (atomic_read(&partition->disable)) {
 		return snprintf(buf, MAX_CHAR_SYSFS,
@@ -185,9 +220,10 @@ ssize_t aipu_common_disable_sysfs_store(struct device *dev, struct device_attrib
 					const char *buf, size_t count)
 {
 	int do_disable = 0;
-	struct platform_device *p_dev = container_of(dev, struct platform_device, dev);
-	struct aipu_priv *aipu = platform_get_drvdata(p_dev);
-	struct aipu_partition *partition = aipu->partitions;
+	struct aipu_partition *partition = aipu_common_get_partition(dev);
+
+	if (unlikely(!partition))
+		return -ENODEV;
 
 	if ((strncmp(buf, "1", 1) == 0))
 		do_disable = 1;
diff --git a/drivers/misc/armchina-npu/aipu_job_manager.c b/drivers/misc/armchina-npu/aipu_job_manager.c
--- a/drivers/misc/armchina-npu/aipu_job_manager.c
+++ b/drivers/misc/armchina-npu/aipu_job_manager.c
@@ -729,6 +729,8 @@ int init_aipu_job_manager(struct aipu_job_manager *manager, struct aipu_memory_m
 	manager->asid0_base = aipu_mm_get_asid_base(mm, AIPU_BUF_ASID_0);
 	atomic_set(&manager->tick_counter, 0);
 	atomic_set(&manager->is_suspend, 0);
+	memset(&manager->suspend_config, 0, sizeof(manager->suspend_config));
+	manager->suspend_config_valid = false;
 
 	WARN_ON(IS_ERR(manager->scheduled_head));
 	WARN_ON(IS_ERR(manager->wait_queue_head));
@@ -1869,67 +1871,122 @@ int aipu_job_manager_enable_tick_counter(struct aipu_job_manager *manager)
 	return 0;
 }
 
-int aipu_job_manager_config_clusters(struct aipu_job_manager *manager,
-				     struct aipu_config_clusters *cfg)
+static int config_clusters_no_lock(struct aipu_job_manager *manager,
+				   const struct aipu_config_clusters *cfg)
 {
-	int ret = 0;
-	unsigned long flags;
 	struct aipu_job *curr = NULL;
-	struct aipu_partition *partition = NULL;
+	struct aipu_partition *partition;
 	int idx = 0;
 	u32 en_count = 0;
 	u32 core_cnt = 0;
-
-	if (!manager || !cfg)
-		return -EINVAL;
-
-	if (manager->version < AIPU_ISA_VERSION_ZHOUYI_V3)
-		return 0;
+	bool all_disabled = true;
 
 	partition = &manager->partitions[0];
-
-	spin_lock_irqsave(&manager->lock, flags);
 	list_for_each_entry(curr, &manager->scheduled_head->node, node) {
-		if (curr->state == AIPU_JOB_STATE_RUNNING ||
+		if (curr->state == AIPU_JOB_STATE_PENDING ||
+		    curr->state == AIPU_JOB_STATE_RUNNING ||
 		    curr->state == AIPU_JOB_STATE_DEFERRED) {
-			ret = -EBUSY;
 			dev_err(manager->dev, "config clusters failed: aipu is busy now");
-			goto unlock;
+			return -EBUSY;
 		}
 	}
 
+	/* Validate the whole request before changing any hardware state. */
 	for (idx = 0; idx < partition->cluster_cnt; idx++) {
 		en_count = cfg->clusters[idx].en_core_cnt;
 		core_cnt = partition->clusters[idx].core_cnt;
 
 		if (en_count > core_cnt) {
-			ret = -EINVAL;
 			dev_err(manager->dev, "invalid enable core count: %u (> tot count %u)",
 				en_count, core_cnt);
-			goto unlock;
+			return -EINVAL;
 		}
-		partition->ops->enable_core_cnt(partition, idx, en_count);
-		aipu_job_manager_destroy_command_pool_no_lock(manager, partition, true);
+		if (en_count)
+			all_disabled = false;
 	}
 
-	atomic_set(&manager->is_suspend, !en_count);
+	for (idx = 0; idx < partition->cluster_cnt; idx++)
+		partition->ops->enable_core_cnt(partition, idx,
+						cfg->clusters[idx].en_core_cnt);
 
-unlock:
+	aipu_job_manager_destroy_command_pool_no_lock(manager, partition, true);
+	atomic_set(&manager->is_suspend, all_disabled);
+	return 0;
+}
+
+int aipu_job_manager_config_clusters(struct aipu_job_manager *manager,
+				     struct aipu_config_clusters *cfg)
+{
+	struct aipu_partition *partition;
+	unsigned long flags;
+	int ret;
+
+	if (!manager || !cfg)
+		return -EINVAL;
+
+	if (manager->version < AIPU_ISA_VERSION_ZHOUYI_V3)
+		return 0;
+
+	if (!manager->partitions || manager->partition_cnt < 1)
+		return -ENODEV;
+
+	partition = &manager->partitions[0];
+	if (!partition->ops || !partition->ops->enable_core_cnt ||
+	    !partition->cluster_cnt ||
+	    partition->cluster_cnt > ARRAY_SIZE(cfg->clusters))
+		return -EINVAL;
+
+	spin_lock_irqsave(&manager->lock, flags);
+	if (manager->suspend_config_valid)
+		ret = -EBUSY;
+	else
+		ret = config_clusters_no_lock(manager, cfg);
 	spin_unlock_irqrestore(&manager->lock, flags);
 	return ret;
 }
 
 int aipu_job_manager_suspend(struct aipu_job_manager *manager)
 {
-	struct aipu_config_clusters cfg;
+	struct aipu_config_clusters cfg = { 0 };
+	struct aipu_partition *partition;
+	unsigned long flags;
+	int idx;
+	int ret;
 
 	if (!manager)
 		return -EINVAL;
 
-	memset(&cfg, 0, sizeof(cfg));
+	if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
+		if (!manager->partitions || manager->partition_cnt < 1)
+			return -ENODEV;
+
+		partition = &manager->partitions[0];
+		if (!partition->ops || !partition->ops->enable_core_cnt ||
+		    !partition->cluster_cnt ||
+		    partition->cluster_cnt > ARRAY_SIZE(cfg.clusters))
+			return -EINVAL;
+
+		spin_lock_irqsave(&manager->lock, flags);
+		if (manager->suspend_config_valid) {
+			ret = 0;
+			goto unlock;
+		}
+
+		for (idx = 0; idx < partition->cluster_cnt; idx++)
+			manager->suspend_config.clusters[idx].en_core_cnt =
+				atomic_read(&partition->clusters[idx].en_core_cnt);
 
-	if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3)
-		return aipu_job_manager_config_clusters(manager, &cfg);
+		ret = config_clusters_no_lock(manager, &cfg);
+		if (!ret) {
+			manager->suspend_config_valid = true;
+		} else {
+			memset(&manager->suspend_config, 0,
+			       sizeof(manager->suspend_config));
+		}
+unlock:
+		spin_unlock_irqrestore(&manager->lock, flags);
+		return ret;
+	}
 
 	atomic_set(&manager->is_suspend, 1);
 	return 0;
@@ -1937,15 +1994,43 @@ int aipu_job_manager_suspend(struct aipu_job_manager *manager)
 
 int aipu_job_manager_resume(struct aipu_job_manager *manager)
 {
-	struct aipu_config_clusters cfg;
+	struct aipu_config_clusters cfg = { 0 };
+	struct aipu_partition *partition;
+	unsigned long flags;
+	int idx;
+	int ret;
 
 	if (!manager)
 		return -EINVAL;
 
-	cfg.clusters[0].en_core_cnt = manager->partitions[0].clusters[0].core_cnt;
+	if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
+		if (!manager->partitions || manager->partition_cnt < 1)
+			return -ENODEV;
+
+		partition = &manager->partitions[0];
+		if (!partition->ops || !partition->ops->enable_core_cnt ||
+		    !partition->cluster_cnt ||
+		    partition->cluster_cnt > ARRAY_SIZE(cfg.clusters))
+			return -EINVAL;
+
+		spin_lock_irqsave(&manager->lock, flags);
+		if (manager->suspend_config_valid) {
+			cfg = manager->suspend_config;
+		} else {
+			for (idx = 0; idx < partition->cluster_cnt; idx++)
+				cfg.clusters[idx].en_core_cnt =
+					partition->clusters[idx].core_cnt;
+		}
 
-	if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3)
-		return aipu_job_manager_config_clusters(manager, &cfg);
+		ret = config_clusters_no_lock(manager, &cfg);
+		if (!ret) {
+			memset(&manager->suspend_config, 0,
+			       sizeof(manager->suspend_config));
+			manager->suspend_config_valid = false;
+		}
+		spin_unlock_irqrestore(&manager->lock, flags);
+		return ret;
+	}
 
 	atomic_set(&manager->is_suspend, 0);
 	return 0;
@@ -2064,4 +2149,4 @@ int aipu_job_manager_free_sflag_id(struct aipu_job_manager *manager,
 	mutex_unlock(&manager->id_lock);
 
 	return 0;
-}
\ No newline at end of file
+}
diff --git a/drivers/misc/armchina-npu/aipu_job_manager.h b/drivers/misc/armchina-npu/aipu_job_manager.h
--- a/drivers/misc/armchina-npu/aipu_job_manager.h
+++ b/drivers/misc/armchina-npu/aipu_job_manager.h
@@ -170,6 +170,8 @@ struct command_pool {
  * @exit_tcb:        buffer of the exit_TCB
  * @tick_counter:    atomic lock for tick counter
  * @is_suspend:      is suspended or not
+ * @suspend_config:  cluster configuration saved across system suspend
+ * @suspend_config_valid: whether suspend_config contains a saved configuration
  * @dbg_do_destroy:  do destroy flag (enabled after a bind-dispatch job ends)
  * @tec_intr_en:     is TEC interrupts enabled or not
  * @grid_id:         the latest grid ID allocated for userspace
@@ -199,6 +201,8 @@ struct aipu_job_manager {
 	struct aipu_tcb *exit_tcb;
 	atomic_t tick_counter;
 	atomic_t is_suspend;
+	struct aipu_config_clusters suspend_config;
+	bool suspend_config_valid;
 	bool dbg_do_destroy;
 	bool tec_intr_en;
 	u16  grid_id;
diff --git a/drivers/misc/armchina-npu/aipu_mm.c b/drivers/misc/armchina-npu/aipu_mm.c
--- a/drivers/misc/armchina-npu/aipu_mm.c
+++ b/drivers/misc/armchina-npu/aipu_mm.c
@@ -853,10 +853,13 @@ static void aipu_mm_free_filp_in_region(struct aipu_memory_manager *mm,
 static ssize_t aipu_gm_policy_sysfs_show(struct device *dev, struct device_attribute *attr,
 					 char *buf)
 {
-	struct platform_device *p_dev = container_of(dev, struct platform_device, dev);
-	struct aipu_priv *aipu = platform_get_drvdata(p_dev);
-	struct aipu_partition *partition = aipu->partitions;
-	struct aipu_memory_manager *mm = &partition->priv->mm;
+	struct aipu_partition *partition =
+		platform_get_drvdata(to_platform_device(dev));
+	struct aipu_memory_manager *mm;
+
+	if (!partition || !partition->priv)
+		return -ENODEV;
+	mm = &partition->priv->mm;
 
 	if (mm->gm_policy == AIPU_GM_POLICY_SHARED) {
 		return snprintf(buf, 128, "[%d] AIPU GM is shared by tasks of all QoS level.\n",
@@ -874,10 +877,13 @@ static ssize_t aipu_gm_policy_sysfs_show(struct device *dev, struct device_attri
 static ssize_t aipu_gm_policy_sysfs_store(struct device *dev, struct device_attribute *attr,
 					  const char *buf, size_t count)
 {
-	struct platform_device *p_dev = container_of(dev, struct platform_device, dev);
-	struct aipu_priv *aipu = platform_get_drvdata(p_dev);
-	struct aipu_partition *partition = aipu->partitions;
-	struct aipu_memory_manager *mm = &partition->priv->mm;
+	struct aipu_partition *partition =
+		platform_get_drvdata(to_platform_device(dev));
+	struct aipu_memory_manager *mm;
+
+	if (!partition || !partition->priv)
+		return -ENODEV;
+	mm = &partition->priv->mm;
 
 	mutex_lock(&mm->lock);
 	if ((strncmp(buf, "0", 1) == 0))
diff --git a/drivers/misc/armchina-npu/include/cix_sky1_soc.h b/drivers/misc/armchina-npu/include/cix_sky1_soc.h
--- a/drivers/misc/armchina-npu/include/cix_sky1_soc.h
+++ b/drivers/misc/armchina-npu/include/cix_sky1_soc.h
@@ -21,6 +21,7 @@ struct cix_aipu_domain {
 	bool driver_flags_changed;
 	bool saved_ignore_parent;
 	bool ignore_parent_changed;
+	bool runtime_ref;
 };
 
 struct cix_aipu_priv {
@@ -30,6 +31,8 @@ struct cix_aipu_priv {
 	bool is_acpi;
 	bool saved_ignore_children;
 	bool ignore_children_changed;
+	bool parent_runtime_enabled_by_us;
+	bool parent_runtime_ref;
 	struct device *opp_pmdomain;
 	struct device_link *opp_dl;
 	struct devfreq_dev_profile devfreq_profile;
@@ -40,7 +43,5 @@ struct cix_aipu_priv {
 };
 
 struct cix_aipu_priv *sky1_priv_init(struct device *dev);
-int sky1_npu_pm_runtime_get_sync(struct device *dev);
-int sky1_npu_pm_runtime_put(struct device *dev);
 
 #endif /* __CIX_SKY1_SOC_H__ */
diff --git a/drivers/misc/armchina-npu/sky1/sky1.c b/drivers/misc/armchina-npu/sky1/sky1.c
--- a/drivers/misc/armchina-npu/sky1/sky1.c
+++ b/drivers/misc/armchina-npu/sky1/sky1.c
@@ -368,40 +368,102 @@ static int sky1_npu_devfreq_init(struct device *dev,
 }
 #endif
 
-int sky1_npu_pm_runtime_get_sync(struct device *dev)
+static int sky1_npu_pm_runtime_get_sync(struct device *dev, struct aipu_soc *soc)
 {
 #ifdef CONFIG_PM
-	int ret = 0;
+	struct cix_aipu_priv *priv = sky1_priv_get(dev);
 
-	ret = pm_runtime_get_sync(dev);
-	if (ret < 0) {
-		dev_info(dev, "PM runtime get sync failed! ret = %d", ret);
-		return ret;
-	}
+	if (!priv || !soc || soc->priv != priv)
+		return -ENODEV;
 
-	return ret;
+	return pm_runtime_resume_and_get(dev);
 #else /* !CONFIG_PM  */
+	(void)dev;
+	(void)soc;
 	return 0;
 #endif /* CONFIG_PM */
 }
 
-int sky1_npu_pm_runtime_put(struct device *dev)
+static int sky1_npu_pm_runtime_put(struct device *dev, struct aipu_soc *soc)
 {
 #ifdef CONFIG_PM
-	int ret = 0;
+	struct cix_aipu_priv *priv = sky1_priv_get(dev);
 
-#if KERNEL_VERSION(7, 0, 0) > LINUX_VERSION_CODE
-	ret = pm_runtime_put(dev);
+	if (!priv || !soc || soc->priv != priv)
+		return -ENODEV;
+	return min(pm_runtime_put_sync(dev), 0);
+#else /* !CONFIG_PM  */
+	(void)dev;
+	(void)soc;
+	return 0;
+#endif /* CONFIG_PM */
+}
+
+static int sky1_npu_core_get(struct cix_aipu_domain *domain)
+{
+	int ret;
+
+	if (domain->runtime_ref)
+		return 0;
+
+	ret = pm_runtime_resume_and_get(domain->dev);
 	if (ret < 0)
-		dev_err(dev, "PM runtime put failed! ret=%d", ret);
+		return ret;
+
+	domain->runtime_ref = true;
+	return 0;
+}
+
+static void sky1_npu_core_put(struct cix_aipu_domain *domain)
+{
+	if (!domain->runtime_ref)
+		return;
+
+#if KERNEL_VERSION(7, 0, 0) > LINUX_VERSION_CODE
+	if (pm_runtime_put(domain->dev) < 0)
+		dev_warn(domain->dev, "runtime PM put reported an error\n");
 #else
-	pm_runtime_put(dev);
+	pm_runtime_put(domain->dev);
 #endif
+	domain->runtime_ref = false;
+}
+
+static void sky1_npu_put_all_cores(struct cix_aipu_priv *priv)
+{
+	u32 i;
+
+	for (i = priv->pd_count; i > 0; i--)
+		sky1_npu_core_put(&priv->core[i - 1]);
+}
+
+static int sky1_npu_get_all_cores(struct cix_aipu_priv *priv)
+{
+	bool acquired[CIX_NPU_PD_MAX_NUM] = { false };
+	u32 i;
+	int ret;
+
+	if (!priv->is_acpi)
+		return 0;
+
+	for (i = 0; i < priv->pd_count; i++) {
+		if (priv->core[i].runtime_ref)
+			continue;
+
+		ret = sky1_npu_core_get(&priv->core[i]);
+		if (ret)
+			goto err_put;
+		acquired[i] = true;
+	}
 
-	return ret;
-#else /* !CONFIG_PM  */
 	return 0;
-#endif /* CONFIG_PM */
+
+err_put:
+	while (i > 0) {
+		i--;
+		if (acquired[i])
+			sky1_npu_core_put(&priv->core[i]);
+	}
+	return ret;
 }
 
 static void sky1_npu_release_domain(struct cix_aipu_domain *domain)
@@ -413,6 +475,8 @@ static void sky1_npu_release_domain(struct cix_aipu_domain *domain)
 	if (!domain->dev)
 		return;
 
+	sky1_npu_core_put(domain);
+
 	if (domain->link) {
 		device_link_del(domain->link);
 		domain->link = NULL;
@@ -451,10 +515,12 @@ static void sky1_npu_detach_pd(struct cix_aipu_priv *priv)
 	for (i = priv->pd_count; i > 0; i--)
 		sky1_npu_release_domain(&priv->core[i - 1]);
 
+#ifdef CONFIG_PM
 	if (priv->ignore_children_changed &&
 	    priv->dev->power.ignore_children)
 		priv->dev->power.ignore_children = priv->saved_ignore_children;
 	priv->ignore_children_changed = false;
+#endif
 }
 
 static int sky1_npu_attach_pd(struct cix_aipu_priv *priv)
@@ -555,11 +621,13 @@ static int sky1_npu_attach_acpi_cores(struct cix_aipu_priv *priv)
 		}
 	}
 
+#ifdef CONFIG_PM
 	priv->saved_ignore_children = priv->dev->power.ignore_children;
 	if (!priv->saved_ignore_children) {
 		priv->dev->power.ignore_children = true;
 		priv->ignore_children_changed = true;
 	}
+#endif
 
 	for (i = 0; i < priv->pd_count; i++) {
 		domain = &priv->core[i];
@@ -628,6 +696,8 @@ static struct aipu_soc_operations sky1_ops = {
 	.disable_clk = NULL,
 	.is_clk_enabled = NULL,
 	.is_aipu_irq = NULL,
+	.soc_pm_runtime_get_sync = sky1_npu_pm_runtime_get_sync,
+	.soc_pm_runtime_put = sky1_npu_pm_runtime_put,
 };
 
 static int sky1_npu_probe(struct platform_device *p_dev)
@@ -636,7 +706,6 @@ static int sky1_npu_probe(struct platform_device *p_dev)
 	struct cix_aipu_priv *priv;
 	u32 mask = 3;
 	int ret;
-	int i = 0;
 
 	if (device_property_present(dev, "core_mask")) {
 		ret = device_property_read_u32(dev, "core_mask", &mask);
@@ -671,18 +740,22 @@ static int sky1_npu_probe(struct platform_device *p_dev)
 #endif
 
 #ifdef CONFIG_PM
+	if (pm_runtime_enabled(dev)) {
+		ret = -EBUSY;
+		goto npu_probe_failed;
+	}
 	pm_runtime_get_noresume(dev);
-	pm_runtime_set_active(dev);
+	priv->parent_runtime_ref = true;
+	ret = pm_runtime_set_active(dev);
+	if (ret)
+		goto npu_probe_failed;
 	pm_runtime_enable(dev);
+	priv->parent_runtime_enabled_by_us = true;
 #endif /* CONFIG_PM */
 
-	if (priv->is_acpi) {
-		for (i = 0; i < priv->pd_count; i++) {
-			ret = pm_runtime_resume_and_get(priv->core[i].dev);
-			if (ret < 0)
-				goto npu_probe_failed;
-		}
-	}
+	ret = sky1_npu_get_all_cores(priv);
+	if (ret)
+		goto npu_probe_failed;
 
 	ret = armchina_aipu_probe(p_dev, &sky1, &sky1_ops);
 	if (ret) {
@@ -693,15 +766,27 @@ static int sky1_npu_probe(struct platform_device *p_dev)
 	dev_dbg(dev, "%s: armchina_aipu_probe done\n", __func__);
 
 #ifdef CONFIG_PM
-	sky1_npu_pm_runtime_put(dev);
+	ret = sky1_npu_pm_runtime_put(dev, &sky1);
+	priv->parent_runtime_ref = false;
+	if (ret)
+		dev_warn(dev, "initial runtime PM put reported %d\n", ret);
 #endif /* CONFIG_PM */
 
 	return 0;
 
 npu_probe_failed:
-	pm_runtime_put(dev);
+	sky1_npu_put_all_cores(priv);
 
-	pm_runtime_disable(dev);
+#ifdef CONFIG_PM
+	if (priv->parent_runtime_ref) {
+		pm_runtime_put_noidle(dev);
+		priv->parent_runtime_ref = false;
+	}
+	if (priv->parent_runtime_enabled_by_us) {
+		pm_runtime_disable(dev);
+		priv->parent_runtime_enabled_by_us = false;
+	}
+#endif
 
 #if IS_REACHABLE(CONFIG_PM_DEVFREQ)
 	sky1_npu_devfreq_remove(dev, priv);
@@ -728,6 +813,14 @@ static void sky1_npu_remove(struct platform_device *p_dev)
 	if (!priv)
 		goto out;
 
+#ifdef CONFIG_PM
+	if (!priv->parent_runtime_ref) {
+		pm_runtime_get_noresume(&p_dev->dev);
+		priv->parent_runtime_ref = true;
+	}
+	pm_runtime_barrier(&p_dev->dev);
+#endif /* CONFIG_PM */
+
 	/* File and dma-buf cleanup may still invoke Sky1 runtime PM. */
 	armchina_aipu_remove(p_dev);
 
@@ -735,8 +828,17 @@ static void sky1_npu_remove(struct platform_device *p_dev)
 	sky1_npu_devfreq_remove(&p_dev->dev, priv);
 #endif
 
+	sky1_npu_put_all_cores(priv);
+
 #ifdef CONFIG_PM
-	pm_runtime_disable(&p_dev->dev);
+	if (priv->parent_runtime_ref) {
+		pm_runtime_put_noidle(&p_dev->dev);
+		priv->parent_runtime_ref = false;
+	}
+	if (priv->parent_runtime_enabled_by_us &&
+	    pm_runtime_enabled(&p_dev->dev))
+		pm_runtime_disable(&p_dev->dev);
+	priv->parent_runtime_enabled_by_us = false;
 #endif /* CONFIG_PM */
 
 	sky1_npu_detach_pd(priv);
@@ -753,10 +855,9 @@ static void sky1_npu_remove(struct platform_device *p_dev)
 static int sky1_npu_runtime_suspend(struct device *dev)
 {
 	struct cix_aipu_priv *priv = sky1_priv_get(dev);
-	int ret = 0;
 	struct platform_device *p_dev = to_platform_device(dev);
-	pm_message_t state;
-	state.event = 0;
+	pm_message_t state = { .event = 0 };
+	int ret;
 
 	if (!priv)
 		return -ENODEV;
@@ -767,22 +868,9 @@ static int sky1_npu_runtime_suspend(struct device *dev)
 		return ret;
 	}
 
-	if (priv->is_acpi) {
-		for (int i = 0; i < priv->pd_count; i++) {
-#if KERNEL_VERSION(7, 0, 0) > LINUX_VERSION_CODE
-			ret = pm_runtime_put(priv->core[i].dev);
-			if (ret < 0) {
-				dev_err(priv->core[i].dev,
-					"NPU core PM runtime put failed! ret=%d", ret);
-				return ret;
-			}
-#else
-			pm_runtime_put(priv->core[i].dev);
-#endif
-		}
-	}
+	sky1_npu_put_all_cores(priv);
 
-	return ret;
+	return 0;
 }
 
 static int sky1_npu_runtime_resume(struct device *dev)
@@ -794,18 +882,15 @@ static int sky1_npu_runtime_resume(struct device *dev)
 	if (!priv)
 		return -ENODEV;
 
-	if (priv->is_acpi) {
-		for (int i = 0; i < priv->pd_count; i++) {
-			ret = pm_runtime_get_sync(priv->core[i].dev);
-			if (ret < 0) {
-				dev_err(priv->core[i].dev,
-					"NPU core PM runtime get sync failed! ret=%d", ret);
-				return ret;
-			}
-		}
-	}
+	ret = sky1_npu_get_all_cores(priv);
+	if (ret)
+		return ret;
 
-	return armchina_aipu_resume(p_dev);
+	ret = armchina_aipu_resume(p_dev);
+	if (ret)
+		sky1_npu_put_all_cores(priv);
+
+	return ret;
 }
 
 static void npu_complete(struct device *dev) {
