diff --git a/drivers/cpufreq/cppc_cpufreq.c b/drivers/cpufreq/cppc_cpufreq.c
--- a/drivers/cpufreq/cppc_cpufreq.c
+++ b/drivers/cpufreq/cppc_cpufreq.c
@@ -618,6 +618,8 @@ static int cppc_cpufreq_cpu_init(struct cpufreq_policy *policy)
 	 */
 	policy->cpuinfo.min_freq = cppc_perf_to_khz(caps, caps->lowest_perf);
 	policy->cpuinfo.max_freq = policy->max;
+	if (!arch_scale_freq_ref(policy->cpu))
+		topology_update_freq_ref(policy);
 
 	policy->transition_delay_us = cppc_cpufreq_get_transition_delay_us(cpu);
 	policy->shared_type = cpu_data->shared_type;
diff --git a/drivers/cpufreq/cpufreq.c b/drivers/cpufreq/cpufreq.c
--- a/drivers/cpufreq/cpufreq.c
+++ b/drivers/cpufreq/cpufreq.c
@@ -407,14 +407,18 @@ EXPORT_SYMBOL_GPL(cpufreq_freq_transition_begin);
 void cpufreq_freq_transition_end(struct cpufreq_policy *policy,
 		struct cpufreq_freqs *freqs, int transition_failed)
 {
+	unsigned int max_freq;
+
 	if (WARN_ON(!policy->transition_ongoing))
 		return;
 
 	cpufreq_notify_post_transition(policy, freqs, transition_failed);
 
-	arch_set_freq_scale(policy->related_cpus,
-			    policy->cur,
-			    arch_scale_freq_ref(policy->cpu));
+	max_freq = arch_scale_freq_ref(policy->cpu);
+	if (!max_freq)
+		max_freq = policy->cpuinfo.max_freq;
+	if (max_freq)
+		arch_set_freq_scale(policy->related_cpus, policy->cur, max_freq);
 
 	spin_lock(&policy->transition_lock);
 	policy->transition_ongoing = false;
diff --git a/drivers/dma/arm-dma350.c b/drivers/dma/arm-dma350.c
--- a/drivers/dma/arm-dma350.c
+++ b/drivers/dma/arm-dma350.c
@@ -1356,11 +1356,13 @@ static int d350_probe(struct platform_device *pdev)
 	dmac->base = base;
 	platform_set_drvdata(pdev, dmac);
 
-	/* Initialize reserved memory region if specified in DT */
-	ret = of_reserved_mem_device_init(dev);
-	if (ret && ret != -ENODEV && ret != -EINVAL) {
-		dev_err(dev, "Failed to initialize reserved memory: %d\n", ret);
-		goto err_pm_put;
+	if (dev->of_node) {
+		/* Initialize reserved memory region if specified in DT */
+		ret = of_reserved_mem_device_init(dev);
+		if (ret && ret != -ENODEV && ret != -EINVAL) {
+			dev_err(dev, "Failed to initialize reserved memory: %d\n", ret);
+			goto err_pm_put;
+		}
 	}
 
 	reg = readl_relaxed(base + DMAINFO + DMA_BUILDCFG1);
@@ -1560,7 +1562,8 @@ static void d350_remove(struct platform_device *pdev)
 		acpi_dma_controller_free(&pdev->dev);
 	of_dma_controller_free(pdev->dev.of_node);
 	dma_async_device_unregister(&dmac->dma);
-	of_reserved_mem_device_release(&pdev->dev);
+	if (pdev->dev.of_node)
+		of_reserved_mem_device_release(&pdev->dev);
 	pm_runtime_put_sync(&pdev->dev);
 	pm_runtime_disable(&pdev->dev);
 }
diff --git a/drivers/edac/a72_edac.c b/drivers/edac/a72_edac.c
--- a/drivers/edac/a72_edac.c
+++ b/drivers/edac/a72_edac.c
@@ -13,6 +13,7 @@
 #include <linux/module.h>
 #include <linux/of.h>
 #include <linux/bitfield.h>
+#include <linux/acpi.h>
 #include <asm/smp_plat.h>
 
 #include "edac_module.h"
@@ -187,6 +188,9 @@ static int __init a72_edac_driver_init(void)
 {
 	int cpu;
 
+	if (!acpi_disabled)
+		return 0;
+
 	for_each_possible_cpu(cpu) {
 		struct device_node *np __free(device_node) = of_cpu_device_node_get(cpu);
 		if (np) {
diff --git a/drivers/firmware/arm_scmi/driver.c b/drivers/firmware/arm_scmi/driver.c
--- a/drivers/firmware/arm_scmi/driver.c
+++ b/drivers/firmware/arm_scmi/driver.c
@@ -3094,20 +3094,22 @@ static const struct scmi_desc *scmi_transport_setup(struct device *dev)
 
 	dev_info(dev, "Using %s\n", dev_driver_string(trans->supplier));
 
-	ret = of_property_read_u32(dev->of_node, "arm,max-rx-timeout-ms",
-				   &trans->desc.max_rx_timeout_ms);
-	if (ret && ret != -EINVAL)
-		dev_err(dev, "Malformed arm,max-rx-timeout-ms DT property.\n");
+	if (dev->of_node) {
+		ret = of_property_read_u32(dev->of_node, "arm,max-rx-timeout-ms",
+					   &trans->desc.max_rx_timeout_ms);
+		if (ret && ret != -EINVAL)
+			dev_err(dev, "Malformed arm,max-rx-timeout-ms DT property.\n");
 
-	ret = of_property_read_u32(dev->of_node, "arm,max-msg-size",
-				   &trans->desc.max_msg_size);
-	if (ret && ret != -EINVAL)
-		dev_err(dev, "Malformed arm,max-msg-size DT property.\n");
+		ret = of_property_read_u32(dev->of_node, "arm,max-msg-size",
+					   &trans->desc.max_msg_size);
+		if (ret && ret != -EINVAL)
+			dev_err(dev, "Malformed arm,max-msg-size DT property.\n");
 
-	ret = of_property_read_u32(dev->of_node, "arm,max-msg",
-				   &trans->desc.max_msg);
-	if (ret && ret != -EINVAL)
-		dev_err(dev, "Malformed arm,max-msg DT property.\n");
+		ret = of_property_read_u32(dev->of_node, "arm,max-msg",
+					   &trans->desc.max_msg);
+		if (ret && ret != -EINVAL)
+			dev_err(dev, "Malformed arm,max-msg DT property.\n");
+	}
 
 	dev_info(dev,
 		 "SCMI max-rx-timeout: %dms / max-msg-size: %dbytes / max-msg: %d\n",
@@ -3115,7 +3117,8 @@ static const struct scmi_desc *scmi_transport_setup(struct device *dev)
 		 trans->desc.max_msg);
 
 	/* System wide atomic threshold for atomic ops .. if any */
-	if (!of_property_read_u32(dev->of_node, "atomic-threshold-us",
+	if (dev->of_node &&
+	    !of_property_read_u32(dev->of_node, "atomic-threshold-us",
 				  &trans->desc.atomic_threshold))
 		dev_info(dev,
 			 "SCMI System wide atomic threshold set to %u us\n",
diff --git a/drivers/pci/vpd.c b/drivers/pci/vpd.c
--- a/drivers/pci/vpd.c
+++ b/drivers/pci/vpd.c
@@ -578,6 +578,13 @@ static void quirk_blacklist_vpd(struct pci_dev *dev)
 	dev->vpd.len = PCI_VPD_SZ_INVALID;
 	pci_warn(dev, FW_BUG "disabling VPD access (can't determine size of non-standard VPD format)\n");
 }
+
+static void quirk_disable_vpd(struct pci_dev *dev)
+{
+	dev->vpd.cap = 0;
+	dev->vpd.len = PCI_VPD_SZ_INVALID;
+}
+
 DECLARE_PCI_FIXUP_HEADER(PCI_VENDOR_ID_LSI_LOGIC, 0x0060, quirk_blacklist_vpd);
 DECLARE_PCI_FIXUP_HEADER(PCI_VENDOR_ID_LSI_LOGIC, 0x007c, quirk_blacklist_vpd);
 DECLARE_PCI_FIXUP_HEADER(PCI_VENDOR_ID_LSI_LOGIC, 0x0413, quirk_blacklist_vpd);
@@ -590,7 +597,7 @@ DECLARE_PCI_FIXUP_HEADER(PCI_VENDOR_ID_LSI_LOGIC, 0x002f, quirk_blacklist_vpd);
 DECLARE_PCI_FIXUP_HEADER(PCI_VENDOR_ID_LSI_LOGIC, 0x005d, quirk_blacklist_vpd);
 DECLARE_PCI_FIXUP_HEADER(PCI_VENDOR_ID_LSI_LOGIC, 0x005f, quirk_blacklist_vpd);
 DECLARE_PCI_FIXUP_HEADER(PCI_VENDOR_ID_ATTANSIC, PCI_ANY_ID, quirk_blacklist_vpd);
-DECLARE_PCI_FIXUP_HEADER(PCI_VENDOR_ID_REALTEK, 0x8126, quirk_blacklist_vpd);
+DECLARE_PCI_FIXUP_HEADER(PCI_VENDOR_ID_REALTEK, 0x8126, quirk_disable_vpd);
 /*
  * The Amazon Annapurna Labs 0x0031 device id is reused for other non Root Port
  * device types, so the quirk is registered for the PCI_CLASS_BRIDGE_PCI class.
