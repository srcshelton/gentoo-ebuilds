diff --git a/drivers/misc/armchina-npu/include/cix_sky1_soc.h b/drivers/misc/armchina-npu/include/cix_sky1_soc.h
--- a/drivers/misc/armchina-npu/include/cix_sky1_soc.h
+++ b/drivers/misc/armchina-npu/include/cix_sky1_soc.h
@@ -7,6 +7,7 @@
 #include <linux/clk.h>
 #include <linux/devfreq.h>
 #include <linux/devfreq-event.h>
+#include <linux/pm_opp.h>
 #include "aipu_priv.h"
 
 #define CIX_NPU_PD_MAX_NUM				(3)
@@ -17,6 +18,7 @@ struct cix_aipu_priv {
 	struct device_link *opp_dl;
 	struct devfreq_dev_profile devfreq_profile;
 	struct devfreq *devfreq;
+	unsigned long current_freq;
 };
 
 int sky1_npu_pm_runtime_get_sync(struct device *dev);
diff --git a/drivers/misc/armchina-npu/sky1/sky1.c b/drivers/misc/armchina-npu/sky1/sky1.c
--- a/drivers/misc/armchina-npu/sky1/sky1.c
+++ b/drivers/misc/armchina-npu/sky1/sky1.c
@@ -68,11 +68,15 @@ static void remove_debugfs_dir(const char *name)
 #ifdef CONFIG_ENABLE_DEVFREQ
 static int sky1_npu_devfreq_target(struct device *dev, unsigned long *freq, u32 flags)
 {
+    struct cix_aipu_priv *priv = dev_get_drvdata(dev);
     struct dev_pm_opp *opp;
     unsigned long pre_freq;
     unsigned long target_freq = *freq;
     int ret;
 
+    if (!priv)
+        return -ENODEV;
+
     opp = devfreq_recommended_opp(dev, freq, flags);
     if (IS_ERR(opp)) {
         dev_err(dev, "Failed to get recommended opp instance\n");
@@ -80,18 +84,25 @@ static int sky1_npu_devfreq_target(struct device *dev, unsigned long *freq, u32
         return ret;
     }
     dev_pm_opp_put(opp);
-    pre_freq = scmi_device_get_freq(cix_aipu_priv->opp_pmdomain);
-    ret = scmi_device_set_freq(cix_aipu_priv->opp_pmdomain, *freq);
+    pre_freq = priv->current_freq;
+    ret = dev_pm_opp_set_rate(dev, *freq);
+    if (!ret)
+        priv->current_freq = *freq;
 
     dev_dbg(dev, "%s: target=%ld, previous=%ld, current=%ld.",
-                    __func__, target_freq, pre_freq, *freq);
+                    __func__, target_freq, pre_freq, priv->current_freq);
 
     return ret;
 }
 
 static int sky1_npu_devfreq_get_cur_freq(struct device *dev, unsigned long *freq)
 {
-    *freq = scmi_device_get_freq(cix_aipu_priv->opp_pmdomain);
+    struct cix_aipu_priv *priv = dev_get_drvdata(dev);
+
+    if (!priv)
+        return -ENODEV;
+
+    *freq = priv->current_freq;
     dev_dbg(dev, "%s: %ld", __func__, *freq);
 
     return 0;
@@ -100,9 +111,14 @@ static int sky1_npu_devfreq_get_cur_freq(struct device *dev, unsigned long *freq
 static int sky1_npu_devfreq_get_dev_status(struct device *dev,
                             struct devfreq_dev_status *stat)
 {
+    struct cix_aipu_priv *priv = dev_get_drvdata(dev);
+
+    if (!priv)
+        return -ENODEV;
+
     dev_dbg(dev, "%s\n", __func__);
 
-    stat->current_frequency = scmi_device_get_freq(cix_aipu_priv->opp_pmdomain);
+    stat->current_frequency = priv->current_freq;
 
     return 0;
 }
@@ -141,13 +157,6 @@ static int sky1_npu_devfreq_init(struct device *dev, struct cix_aipu_priv *cix_a
         goto detach_opp;
     }
 
-    /* Add opps to opp power domain. */
-    ret = scmi_device_opp_table_parse(cix_aipu_priv->opp_pmdomain);
-    if (ret) {
-        dev_err(dev, "Failed to add opps to the device");
-        ret = -ENODEV;
-        goto unlink_opp;
-    }
     opp_count = dev_pm_opp_get_opp_count(cix_aipu_priv->opp_pmdomain);
     if (opp_count <= 0) {
         dev_err(dev, "Failed to get opps count.");
@@ -175,6 +184,8 @@ static int sky1_npu_devfreq_init(struct device *dev, struct cix_aipu_priv *cix_a
     }
 
     profile->max_state = i;
+    if (i > 0)
+        cix_aipu_priv->current_freq = profile->freq_table[0];
     profile->polling_ms = 50;
     profile->target = sky1_npu_devfreq_target;
     profile->get_dev_status = sky1_npu_devfreq_get_dev_status;
