Expose ACPI thermal-zone _STR descriptions as hwmon input labels.
Linux 7.2 groups thermal zones with a common hwmon type. ACPI zones
therefore share the standard acpitz device while retaining distinct
tempN_label attributes.

diff --git a/drivers/acpi/thermal.c b/drivers/acpi/thermal.c
--- a/drivers/acpi/thermal.c
+++ b/drivers/acpi/thermal.c
@@ -24,6 +24,7 @@
 #include <linux/delay.h>
 #include <linux/jiffies.h>
 #include <linux/kmod.h>
+#include <linux/nls.h>
 #include <linux/reboot.h>
 #include <linux/device.h>
 #include <linux/platform_device.h>
@@ -130,6 +131,7 @@ struct acpi_thermal {
 	bool has_last_good_temp;
 	bool invalid_temp_reported;
 	enum acpi_thermal_orion_o6_tmp_type orion_o6_tmp_type;
+	char *label;
 	struct acpi_thermal_trips trips;
 	struct thermal_zone_device *thermal_zone;
 	int kelvin_offset;	/* in millidegrees */
@@ -138,6 +140,45 @@ struct acpi_thermal {
 	refcount_t thermal_check_count;
 };
 
+static char *acpi_thermal_get_label(struct acpi_device *device)
+{
+	struct acpi_buffer buffer = { ACPI_ALLOCATE_BUFFER, NULL };
+	union acpi_object *obj;
+	acpi_status status;
+	char *label = NULL;
+	int length;
+
+	status = acpi_evaluate_object_typed(device->handle, "_STR", NULL,
+					    &buffer, ACPI_TYPE_BUFFER);
+	if (ACPI_FAILURE(status))
+		return NULL;
+
+	obj = buffer.pointer;
+	if (!obj || !obj->buffer.pointer || !obj->buffer.length ||
+	    obj->buffer.length % sizeof(wchar_t) ||
+	    obj->buffer.length > (INT_MAX - 1) / 2)
+		goto out;
+
+	label = kmalloc(obj->buffer.length * 2 + 1, GFP_KERNEL);
+	if (!label)
+		goto out;
+
+	length = utf16s_to_utf8s((const wchar_t *)obj->buffer.pointer,
+				 obj->buffer.length / sizeof(wchar_t),
+				 UTF16_LITTLE_ENDIAN, label,
+				 obj->buffer.length * 2);
+	if (length <= 0) {
+		kfree(label);
+		label = NULL;
+	} else {
+		label[length] = '\0';
+	}
+
+out:
+	ACPI_FREE(buffer.pointer);
+	return label;
+}
+
 /* --------------------------------------------------------------------------
                              Thermal Zone Management
    -------------------------------------------------------------------------- */
@@ -771,16 +812,20 @@ static int acpi_thermal_register_thermal_zone(struct acpi_thermal *tz,
 					      int passive_delay)
 {
 	const char *tz_type = acpi_device_bid(tz->device);
+	struct thermal_zone_params tzp = {
+		.hwmon_label = tz->label,
+		.hwmon_type = "acpitz",
+	};
 	int result;
 
 	if (trip_count)
 		tz->thermal_zone = thermal_zone_device_register_with_trips(
 					tz_type, trip_table, trip_count, tz,
-					&acpi_thermal_zone_ops, NULL, passive_delay,
+					&acpi_thermal_zone_ops, &tzp, passive_delay,
 					tz->polling_frequency * 100);
 	else
 		tz->thermal_zone = thermal_tripless_zone_device_register(
-					tz_type, tz, &acpi_thermal_zone_ops, NULL);
+					tz_type, tz, &acpi_thermal_zone_ops, &tzp);
 
 	if (IS_ERR(tz->thermal_zone))
 		return PTR_ERR(tz->thermal_zone);
@@ -929,6 +974,7 @@ static void acpi_thermal_zone_free(void *data)
 	for (i = 0; i < ACPI_THERMAL_MAX_ACTIVE; i++)
 		acpi_handle_list_free(&tz->trips.active[i].trip.devices);
 
+	kfree(tz->label);
 	kfree(tz);
 }
 
@@ -961,6 +1007,7 @@ static int acpi_thermal_probe(struct platform_device *pdev)
 	tz->device = device;
 	strscpy(tz->name, device->pnp.bus_id);
 	tz->orion_o6_tmp_type = acpi_thermal_orion_o6_get_tmp_type(tz);
+	tz->label = acpi_thermal_get_label(device);
 
 	acpi_thermal_aml_dependency_fix(tz);
 
diff --git a/drivers/thermal/thermal_hwmon.c b/drivers/thermal/thermal_hwmon.c
--- a/drivers/thermal/thermal_hwmon.c
+++ b/drivers/thermal/thermal_hwmon.c
@@ -39,7 +39,9 @@ struct thermal_hwmon_temp {
 	struct list_head hwmon_node;
 	struct thermal_zone_device *tz;
 	struct thermal_hwmon_attr temp_input;	/* hwmon sys attr */
+	struct thermal_hwmon_attr temp_label;	/* hwmon sys attr */
 	struct thermal_hwmon_attr temp_crit;	/* hwmon sys attr */
+	bool temp_label_present;
 	bool temp_crit_present;
 };
 
@@ -67,6 +69,17 @@ temp_input_show(struct device *dev, struct device_attribute *attr, char *buf)
 	return sysfs_emit(buf, "%d\n", temperature);
 }
 
+static ssize_t
+temp_label_show(struct device *dev, struct device_attribute *attr, char *buf)
+{
+	struct thermal_hwmon_attr *hwmon_attr =
+		container_of(attr, struct thermal_hwmon_attr, attr);
+	struct thermal_hwmon_temp *temp =
+		container_of(hwmon_attr, struct thermal_hwmon_temp, temp_label);
+
+	return sysfs_emit(buf, "%s\n", temp->tz->tzp->hwmon_label);
+}
+
 static ssize_t
 temp_crit_show(struct device *dev, struct device_attribute *attr, char *buf)
 {
@@ -88,6 +101,13 @@ temp_crit_show(struct device *dev, struct device_attribute *attr, char *buf)
 	return sysfs_emit(buf, "%d\n", temperature);
 }
 
+static const char *thermal_hwmon_type(const struct thermal_zone_device *tz)
+{
+	if (tz->tzp && tz->tzp->hwmon_type && *tz->tzp->hwmon_type)
+		return tz->tzp->hwmon_type;
+
+	return tz->type;
+}
 
 static struct thermal_hwmon_device *
 thermal_hwmon_lookup_by_type(const struct thermal_zone_device *tz)
@@ -95,10 +115,11 @@ thermal_hwmon_lookup_by_type(const struct thermal_zone_device *tz)
 	struct thermal_hwmon_device *hwmon;
 	char type[THERMAL_NAME_LENGTH];
 
+	strscpy(type, thermal_hwmon_type(tz));
+	strreplace(type, '-', '_');
+
 	mutex_lock(&thermal_hwmon_list_lock);
 	list_for_each_entry(hwmon, &thermal_hwmon_list, node) {
-		strscpy(type, tz->type);
-		strreplace(type, '-', '_');
 		if (!strcmp(hwmon->type, type)) {
 			mutex_unlock(&thermal_hwmon_list_lock);
 			return hwmon;
@@ -137,12 +158,12 @@ int thermal_add_hwmon_sysfs(struct thermal_zone_device *tz)
 {
 	struct thermal_hwmon_device *hwmon;
 	struct thermal_hwmon_temp *temp;
-	int new_hwmon_device = 1;
+	bool new_hwmon_device = true;
 	int result;
 
 	hwmon = thermal_hwmon_lookup_by_type(tz);
 	if (hwmon) {
-		new_hwmon_device = 0;
+		new_hwmon_device = false;
 		goto register_sys_interface;
 	}
 
@@ -151,7 +172,7 @@ int thermal_add_hwmon_sysfs(struct thermal_zone_device *tz)
 		return -ENOMEM;
 
 	INIT_LIST_HEAD(&hwmon->tz_list);
-	strscpy(hwmon->type, tz->type, THERMAL_NAME_LENGTH);
+	strscpy(hwmon->type, thermal_hwmon_type(tz));
 	strreplace(hwmon->type, '-', '_');
 	hwmon->device = hwmon_device_register_for_thermal(&tz->device,
 							  hwmon->type, hwmon);
@@ -180,6 +201,21 @@ int thermal_add_hwmon_sysfs(struct thermal_zone_device *tz)
 	if (result)
 		goto free_temp_mem;
 
+	if (tz->tzp && tz->tzp->hwmon_label && *tz->tzp->hwmon_label) {
+		snprintf(temp->temp_label.name, sizeof(temp->temp_label.name),
+			 "temp%d_label", hwmon->count);
+		temp->temp_label.attr.attr.name = temp->temp_label.name;
+		temp->temp_label.attr.attr.mode = 0444;
+		temp->temp_label.attr.show = temp_label_show;
+		sysfs_attr_init(&temp->temp_label.attr.attr);
+		result = device_create_file(hwmon->device,
+					    &temp->temp_label.attr);
+		if (result)
+			goto unregister_input;
+
+		temp->temp_label_present = true;
+	}
+
 	if (thermal_zone_crit_temp_valid(tz)) {
 		snprintf(temp->temp_crit.name,
 				sizeof(temp->temp_crit.name),
@@ -191,7 +227,7 @@ int thermal_add_hwmon_sysfs(struct thermal_zone_device *tz)
 		result = device_create_file(hwmon->device,
 					    &temp->temp_crit.attr);
 		if (result)
-			goto unregister_input;
+			goto unregister_label;
 
 		temp->temp_crit_present = true;
 	}
@@ -204,6 +240,9 @@ int thermal_add_hwmon_sysfs(struct thermal_zone_device *tz)
 
 	return 0;
 
+ unregister_label:
+	if (temp->temp_label_present)
+		device_remove_file(hwmon->device, &temp->temp_label.attr);
  unregister_input:
 	device_remove_file(hwmon->device, &temp->temp_input.attr);
  free_temp_mem:
@@ -239,6 +278,8 @@ void thermal_remove_hwmon_sysfs(struct thermal_zone_device *tz)
 	}
 
 	device_remove_file(hwmon->device, &temp->temp_input.attr);
+	if (temp->temp_label_present)
+		device_remove_file(hwmon->device, &temp->temp_label.attr);
 	if (temp->temp_crit_present)
 		device_remove_file(hwmon->device, &temp->temp_crit.attr);
 
diff --git a/include/linux/thermal.h b/include/linux/thermal.h
--- a/include/linux/thermal.h
+++ b/include/linux/thermal.h
@@ -155,6 +155,12 @@ struct thermal_zone_params {
 	 */
 	bool no_hwmon;
 
+	/* Optional human-readable label for the hwmon temperature input. */
+	const char *hwmon_label;
+
+	/* Optional hwmon device type; defaults to the thermal zone type. */
+	const char *hwmon_type;
+
 	/*
 	 * Sustainable power (heat) that this thermal zone can dissipate in
 	 * mW
