diff --git a/drivers/media/platform/cix/if/mvx_firmware_v2.c b/drivers/media/platform/cix/if/mvx_firmware_v2.c
--- a/drivers/media/platform/cix/if/mvx_firmware_v2.c
+++ b/drivers/media/platform/cix/if/mvx_firmware_v2.c
@@ -2041,22 +2041,6 @@ static int put_message_v2(struct mvx_fw *fw,
                            sizeof(param.data.rate_control));
             break;
         }
-        case MVX_FW_SET_RATE_CONTROL_RRC_DQP_RANGE: {
-            struct mve_buffer_param param = {0};
-
-            param.type = MVE_BUFFER_PARAM_TYPE_RATE_CONTROL_RRC_DQP_RANGE;
-            param.data.arg = msg->set_option.rrc_dqp_range;
-            ret = put_fw_buf_param(fw, &param, sizeof(param.data.arg));
-            break;
-        }
-        case MVX_FW_SET_RATE_CONTROL_RRC_DQP_STEP: {
-            struct mve_buffer_param param = {0};
-
-            param.type = MVE_BUFFER_PARAM_TYPE_RATE_CONTROL_RRC_DQP_STEP;
-            param.data.arg = msg->set_option.rrc_dqp_step;
-            ret = put_fw_buf_param(fw, &param, sizeof(param.data.arg));
-            break;
-        }
         case MVX_FW_SET_RATE_CONTROL_JPEG: {
             struct mve_request_set_option opt = {0};
 
diff --git a/drivers/media/platform/cix/if/mvx_session.c b/drivers/media/platform/cix/if/mvx_session.c
--- a/drivers/media/platform/cix/if/mvx_session.c
+++ b/drivers/media/platform/cix/if/mvx_session.c
@@ -767,6 +767,8 @@ static bool is_stream_on(struct mvx_session *session)
         return session->port[MVX_DIR_INPUT].stream_on;
 }
 
+static int switch_out_rsp(struct mvx_session *session);
+
 /**
  * wait_pending() - Wait for procedure to finish.
  *
@@ -804,6 +806,7 @@ static int wait_pending(struct mvx_session *session)
                 msecs_to_jiffies(wait_pending_timeout));
 
             if (ret == 0) {
+                switch_out_rsp(session);
                 send_event_error(session, -ETIME);
                 ret = -ETIME;
                 goto lock_mutex;
@@ -1699,6 +1702,7 @@ static int fw_encoder_setup(struct mvx_session *session)
                 return ret;
             }
         }
+
     } else {
         /*
         //handle JPEG rc
@@ -1922,7 +1926,8 @@ static int fw_encoder_setup(struct mvx_session *session)
             session->color_desc.transfer_characteristics != 2 ||
             session->color_desc.sar_height != 0 || session->color_desc.sar_width != 0 ||
             session->color_desc.aspect_ratio_idc != 0 ||
-            session->color_desc.flags != 0) {
+            session->color_desc.flags != 0 ||
+            (!session->disable_timescale && session->fps_d && session->fps_n)) {
             struct mvx_fw_set_option option;
 
             option.code = MVX_FW_SET_COLOUR_DESC;
@@ -3587,6 +3592,8 @@ int mvx_session_construct(struct mvx_session *session,
     session->dentry = dentry;
     session->port[MVX_DIR_INPUT].buffer_min = 1;
     session->port[MVX_DIR_OUTPUT].buffer_min = 1;
+    session->port[MVX_DIR_OUTPUT].buffer_min_afbc = 1;
+    session->port[MVX_DIR_OUTPUT].buffer_min_planar = 1;
     session->port[MVX_DIR_OUTPUT].buffer_max = VIDEO_MAX_FRAME;
     session->port[MVX_DIR_INPUT].buffer_allocated = 0;//1;
     session->port[MVX_DIR_OUTPUT].buffer_allocated = 0;//1;
@@ -3613,6 +3620,8 @@ int mvx_session_construct(struct mvx_session *session,
     session->port[MVX_DIR_INPUT].roi_config_num = 0;
     session->port[MVX_DIR_OUTPUT].qp_num = 0;
     session->port[MVX_DIR_INPUT].qp_num = 0;
+    mutex_init(&session->port[MVX_DIR_OUTPUT].port_changing_mutex);
+    mutex_init(&session->port[MVX_DIR_INPUT].port_changing_mutex);
     session->crop_left = 0;
     session->crop_right = 0;
     session->crop_top = 0;
@@ -3644,7 +3653,6 @@ int mvx_session_construct(struct mvx_session *session,
     session->inter_ipenalty_angular = MVX_INVALID_VAL;
     session->inter_ipenalty_planar = MVX_INVALID_VAL;
     session->inter_ipenalty_dc = MVX_INVALID_VAL;
-
     if (session->enable_buffer_dump == true) {
         char input_file[64];
         scnprintf(input_file, sizeof(input_file) - 1,
@@ -3860,6 +3868,16 @@ int mvx_session_set_format(struct mvx_session *session,
         release_fw_bin(session);
     }
 
+    if (dir == MVX_DIR_OUTPUT && !session->is_encoder &&
+        mvx_is_afbc(port->format) != mvx_is_afbc(format)) {
+        mutex_lock(&port->port_changing_mutex);
+        if (mvx_is_afbc(format))
+            port->buffer_min = port->buffer_min_afbc;
+        else
+            port->buffer_min = port->buffer_min_planar;
+        mutex_unlock(&port->port_changing_mutex);
+    }
+
     /* Update port settings. */
     port->format = format;
     port->old_format = format;
@@ -3983,7 +4001,7 @@ int mvx_session_streamon(struct mvx_session *session,
     enum mvx_direction bdir;
     struct mvx_hw_ver hw_ver;
     enum mvx_direction i;
-    int ret;
+    int ret = 0;
 
     MVX_SESSION_INFO(session, "Stream on. dir=%u.", dir);
 
@@ -4015,11 +4033,26 @@ int mvx_session_streamon(struct mvx_session *session,
      * need to send flush message to VPU here to finish port changing.
      * Otherwise, VPU firmware will get hang.
      */
-    if (dir == MVX_DIR_OUTPUT && session->port[dir].in_port_changing) {
-        return_done_buffers(session, dir);
-        flush_and_qbufs(session, dir);
-        session->port[dir].in_port_changing = false;
-        session->port[dir].hold_off_buffer_done = false;
+    if (dir == MVX_DIR_OUTPUT) {
+        struct mvx_session_port *port = &session->port[dir];
+
+        mutex_lock(&port->port_changing_mutex);
+        if (port->in_port_changing) {
+            if (port->get_format_called) {
+                return_done_buffers(session, dir);
+                ret = flush_and_qbufs(session, dir);
+                port->in_port_changing = false;
+                port->hold_off_buffer_done = false;
+            } else {
+                ret = queue_pending_buffers(session, dir);
+            }
+        }
+        mutex_unlock(&port->port_changing_mutex);
+
+        if (ret != 0 && ret != -EAGAIN) {
+            MVX_SESSION_ERR(session, "Failed to queue pending buffers.");
+            goto disable_port;
+        }
     }
 
     /* If a firmware binary is already loaded, then we are done. */
@@ -4157,6 +4190,7 @@ static void mvx_handle_alloc_param (struct mvx_session *session, struct mvx_fw_m
     struct mvx_session_port *input = &session->port[MVX_DIR_INPUT];
     struct mvx_session_port *output = &session->port[MVX_DIR_OUTPUT];
 
+    mutex_lock(&output->port_changing_mutex);
     output->in_port_changing = msg->alloc_param.width != session->orig_width;
     output->in_port_changing |= msg->alloc_param.height != session->orig_height;
     if (mvx_is_afbc(output->format))
@@ -4167,7 +4201,9 @@ static void mvx_handle_alloc_param (struct mvx_session *session, struct mvx_fw_m
     if (output->in_port_changing) {
         output->received_seq_param = false;
         output->pending_source_change_event = true;
+        output->get_format_called = false;
     }
+    mutex_unlock(&output->port_changing_mutex);
 
     session->orig_width = msg->alloc_param.width;
     session->orig_height = msg->alloc_param.height;
@@ -4196,7 +4232,7 @@ static void mvx_handle_alloc_param (struct mvx_session *session, struct mvx_fw_m
         output->afbc_width = msg->alloc_param.afbc_width;
     }
 
-    if(session->dual_afbc_downscaled && output->interlaced== 0) {
+    if (output->interlaced == 0) {
         output->afbc_alloc_bytes_downscaled = msg->alloc_param.afbc_alloc_bytes_downscaled;
         output->afbc_width_in_superblocks_downscaled = msg->alloc_param.afbc_width_in_superblocks_downscaled;
     } else {
@@ -4570,9 +4606,20 @@ static void mvx_handle_seq_param(struct mvx_session *session, struct mvx_fw_msg
                 msg->seq_param.afbc.buffers_min,
                 p->interlaced);
 
+    mutex_lock(&p->port_changing_mutex);
+    p->buffer_min_afbc = msg->seq_param.afbc.buffers_min;
+    p->buffer_min_planar =
+        session->port[MVX_DIR_INPUT].format == MVX_FORMAT_AV1 ?
+        MVX_DECODE_AV1_PLANNAR_BUFFER_NUM : MVX_DECODE_PLANNAR_BUFFER_NUM;
+    if ((uint64_t)p->new_width * p->new_height <= 8192ULL * 8192)
+        p->buffer_min_planar = max(msg->seq_param.planar.buffers_min,
+                      p->buffer_min_planar);
+    else
+        p->buffer_min_planar = msg->seq_param.planar.buffers_min;
+
     if (mvx_is_afbc(p->format) != false) {
-        p->in_port_changing |= msg->seq_param.afbc.buffers_min != p->buffer_min;
-        p->buffer_min = msg->seq_param.afbc.buffers_min;
+        p->in_port_changing |= p->buffer_min_afbc != p->buffer_min;
+        p->buffer_min = p->buffer_min_afbc;
         //for MVX_FW_CODE_ALLOC_PARAM is send before MVX_FW_CODE_SEQ_PARAM msg
         if(p->last_interlaced_from_sps != p->interlaced) {
             //auto detected interlace streams from sps,need update try_format
@@ -4600,17 +4647,17 @@ static void mvx_handle_seq_param(struct mvx_session *session, struct mvx_fw_msg
             p->last_interlaced_from_sps =p->interlaced;
         }
     } else {
-        p->in_port_changing |= msg->seq_param.planar.buffers_min != p->buffer_min;
-        p->buffer_min = session->port[MVX_DIR_INPUT].format == MVX_FORMAT_AV1 ?
-                                MVX_DECODE_AV1_PLANNAR_BUFFER_NUM : MVX_DECODE_PLANNAR_BUFFER_NUM;
-        p->buffer_min = p->new_width * p->new_height <= 8192 * 8192 ?
-                            max(msg->seq_param.planar.buffers_min, p->buffer_min) :
-                            msg->seq_param.planar.buffers_min;
+        p->in_port_changing |= p->buffer_min_planar != p->buffer_min;
+        p->buffer_min = p->buffer_min_planar;
     }
 
     p->in_port_changing |= msg->seq_param.bitdepth_luma != session->bitdepth_luma;
     p->in_port_changing |= msg->seq_param.bitdepth_chroma != session->bitdepth_chroma;
     p->in_port_changing |= msg->seq_param.chroma_format != session->coded_chroma_format;
+    if (p->in_port_changing)
+        p->get_format_called = false;
+    mutex_unlock(&p->port_changing_mutex);
+
     mvx_update_format(session, msg->seq_param.bitdepth_chroma,
               msg->seq_param.chroma_format);
     session->bitdepth_luma = msg->seq_param.bitdepth_luma;
@@ -6998,3 +7045,15 @@ int mvx_session_set_enc_inter_ipenalty_dc(struct mvx_session *session, int val)
 
     return 0;
 }
+
+int mvx_session_set_enc_disable_timescale(struct mvx_session *session, int val)
+{
+    if (session->error != 0)
+        return session->error;
+
+    if (is_fw_loaded(session))
+        return -EBUSY;
+
+    session->disable_timescale = val;
+    return 0;
+}
diff --git a/drivers/media/platform/cix/if/mvx_session.h b/drivers/media/platform/cix/if/mvx_session.h
--- a/drivers/media/platform/cix/if/mvx_session.h
+++ b/drivers/media/platform/cix/if/mvx_session.h
@@ -143,6 +143,8 @@ struct mvx_session_port {
     unsigned int afbc_width_in_superblocks_downscaled;
     bool stream_on;
     unsigned int buffer_min;
+    unsigned int buffer_min_planar;
+    unsigned int buffer_min_afbc;
     unsigned int buffer_max;
     unsigned int buffer_count;
     unsigned int buffer_allocated;
@@ -170,9 +172,11 @@ struct mvx_session_port {
     unsigned int afbc_crop_left;
     unsigned int afbc_crop_top;
     bool in_port_changing;
+    struct mutex port_changing_mutex;
     bool hold_off_buffer_done;
     bool received_seq_param;
     bool pending_source_change_event;
+    bool get_format_called;
     unsigned int dump_count;
     enum mvx_format old_format;
     unsigned int old_pixelformat;
@@ -351,6 +355,8 @@ struct mvx_session {
     uint32_t fps_n;
     uint32_t fps_d;
     uint32_t last_fps;
+    uint32_t pause_counter;
+    uint32_t fps_before_pause;
     struct timespec64 last_timespec;
     unsigned int target_bitrate;
     unsigned int maximum_bitrate;
@@ -477,6 +483,7 @@ struct mvx_session {
     uint32_t inter_ipenalty_planar;
     uint32_t inter_ipenalty_dc;
     struct mvx_lambda_scale lambda_scale;
+    uint32_t disable_timescale;
 };
 
 /****************************************************************************
@@ -1488,6 +1495,7 @@ int mvx_session_set_enc_intra_ipenalty_dc(struct mvx_session *session, int val);
 int mvx_session_set_enc_inter_ipenalty_angular(struct mvx_session *session, int val);
 int mvx_session_set_enc_inter_ipenalty_planar(struct mvx_session *session, int val);
 int mvx_session_set_enc_inter_ipenalty_dc(struct mvx_session *session, int val);
+int mvx_session_set_enc_disable_timescale(struct mvx_session *session, int val);
 /**
  * mvx_session_update_buffer_count() - Update session port buffer max and min.
  * @session:    Pointer to session.
diff --git a/drivers/media/platform/cix/if/v4l2/mvx_v4l2_ctrls.c b/drivers/media/platform/cix/if/v4l2/mvx_v4l2_ctrls.c
--- a/drivers/media/platform/cix/if/v4l2/mvx_v4l2_ctrls.c
+++ b/drivers/media/platform/cix/if/v4l2/mvx_v4l2_ctrls.c
@@ -957,6 +957,10 @@ static int set_ctrl(struct v4l2_ctrl *ctrl)
         i32_val = *ctrl->p_new.p_s32;
         ret = mvx_session_set_enc_inter_ipenalty_dc(session, i32_val);
         break;
+    case V4L2_CID_MVE_VIDEO_ENC_DISABLE_TIMESCALE:
+        i32_val = *ctrl->p_new.p_s32;
+        ret = mvx_session_set_enc_disable_timescale(session, i32_val);
+        break;
     }
 unlock_mutex:
     mutex_unlock(&vsession->mutex);
@@ -1885,32 +1889,6 @@ int mvx_v4l2_ctrls_init_enc(struct v4l2_ctrl_handler *hnd)
 
     ctrl->flags |= V4L2_CTRL_FLAG_READ_ONLY | V4L2_CTRL_FLAG_VOLATILE;
 
-    ctrl = v4l2_ctrl_new_std(
-        hnd, &ctrl_ops, V4L2_CID_JPEG_RESTART_INTERVAL,
-        -1, 0xffff, 1, -1);
-    if (ctrl == NULL)
-        goto handler_free;
-
-    ctrl = v4l2_ctrl_new_std(
-        hnd, &ctrl_ops, V4L2_CID_JPEG_COMPRESSION_QUALITY,
-        0, 100, 1, 0);
-    if (ctrl == NULL)
-        goto handler_free;
-
-    ctrl = mvx_v4l2_ctrl_new_custom_int(
-        hnd, V4L2_CID_MVE_VIDEO_JPEG_QUALITY_LUMA,
-        "JPEG compress luma quality",
-        0, 100, 0, 1);
-    if (ctrl == NULL)
-        goto handler_free;
-
-    ctrl = mvx_v4l2_ctrl_new_custom_int(
-        hnd, V4L2_CID_MVE_VIDEO_JPEG_QUALITY_CHROMA,
-        "JPEG compress chroma quality",
-        0, 100, 0, 1);
-    if (ctrl == NULL)
-        goto handler_free;
-
     ctrl = mvx_v4l2_ctrl_new_custom_int(
         hnd, V4L2_CID_MVE_VIDEO_INIT_QP_I,
         "init qp of I frame",
@@ -2126,10 +2104,6 @@ int mvx_v4l2_ctrls_init_enc(struct v4l2_ctrl_handler *hnd)
     if (ctrl == NULL)
         goto handler_free;
 
-    ret = v4l2_ctrl_handler_setup(hnd);
-    if (ret != 0)
-        goto handler_free;
-
     ctrl = mvx_v4l2_ctrl_new_custom_int(
         hnd, V4L2_CID_MVE_VIDEO_ENC_INTRA_IPENALTY_ANGULAR,
         "intra ipenalty angular",
@@ -2172,6 +2146,17 @@ int mvx_v4l2_ctrls_init_enc(struct v4l2_ctrl_handler *hnd)
     if (ctrl == NULL)
         goto handler_free;
 
+    ctrl = mvx_v4l2_ctrl_new_custom_int(
+        hnd, V4L2_CID_MVE_VIDEO_ENC_DISABLE_TIMESCALE,
+        "disable timescale in vui",
+        0, 1, 0, 1);
+    if (ctrl == NULL)
+        goto handler_free;
+
+    ret = v4l2_ctrl_handler_setup(hnd);
+    if (ret != 0)
+        goto handler_free;
+
     return 0;
 
 handler_free:
diff --git a/drivers/media/platform/cix/if/v4l2/mvx_v4l2_vidioc.c b/drivers/media/platform/cix/if/v4l2/mvx_v4l2_vidioc.c
--- a/drivers/media/platform/cix/if/v4l2/mvx_v4l2_vidioc.c
+++ b/drivers/media/platform/cix/if/v4l2/mvx_v4l2_vidioc.c
@@ -813,6 +813,12 @@ static int mvx_v4l2_vidioc_g_fmt_vid(struct file *file,
     to_v4l2_format(f, f->type, &vport->pix_mp, port->stride, port->size,
                port->interlaced);
 
+    mutex_lock(&port->port_changing_mutex);
+    port->get_format_called = true;
+    port->old_format = port->format;
+    port->old_pixelformat = port->pixelformat;
+    mutex_unlock(&port->port_changing_mutex);
+
     mutex_unlock(&vsession->mutex);
 
     print_format(&vsession->session, f, "Get format");
diff --git a/drivers/media/platform/cix/linux/mvx-v4l2-controls.h b/drivers/media/platform/cix/linux/mvx-v4l2-controls.h
--- a/drivers/media/platform/cix/linux/mvx-v4l2-controls.h
+++ b/drivers/media/platform/cix/linux/mvx-v4l2-controls.h
@@ -11,7 +11,7 @@
  * permitted to do so under the terms of a subsisting license agreement
  * from Arm Technology (China) Co., Ltd.
  *
- * SPDX-License-Identifier: GPL-2.0-only
+ * SPDX-License-Identifier: (GPL-2.0-only WITH Linux-syscall-note)
  *
  * This program is free software; you can redistribute it and/or
  * modify it under the terms of the GNU General Public License
@@ -480,6 +480,7 @@ enum v4l2_cid_mve_video {
     V4L2_CID_MVE_VIDEO_ENC_INTER_IPENALTY_ANGULAR,
     V4L2_CID_MVE_VIDEO_ENC_INTER_IPENALTY_PLANAR,
     V4L2_CID_MVE_VIDEO_ENC_INTER_IPENALTY_DC,
+    V4L2_CID_MVE_VIDEO_ENC_DISABLE_TIMESCALE,
 };
 
 #define V4L2_SESSION_PRIORITY_PREEMPTION 0
