diff --git a/drivers/misc/armchina-npu-r2p0/aipu.c b/drivers/misc/armchina-npu-r2p0/aipu.c
--- a/drivers/misc/armchina-npu-r2p0/aipu.c
+++ b/drivers/misc/armchina-npu-r2p0/aipu.c
@@ -23,9 +23,7 @@
 #include <linux/pm_runtime.h>
 #include <linux/pm_domain.h>
 
-#ifdef CONFIG_SKY1
 #include "cix_sky1_soc.h"
-#endif
 
 static struct aipu_priv *aipu;
 static DEFINE_MUTEX(aipu_lock);
@@ -135,7 +133,6 @@ static long aipu_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)
 	struct aipu_config_clusters config_clusters;
 	struct aipu_dma_buf_request dmabuf_req;
 	struct aipu_dma_buf dmabuf_info;
-	struct aipu_group_id_desc group_id_desc;
 	int fd = 0;
 
 	u64 job_id;
@@ -318,22 +315,8 @@ static long aipu_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)
 					   sizeof(ret)) ? -EFAULT : 0;
 		break;
 	case AIPU_IOCTL_ALLOC_GROUP_ID:
-		if (!copy_from_user(&group_id_desc, (struct aipu_group_id_desc __user *)arg,
-				    sizeof(group_id_desc))) {
-			ret = aipu_job_manager_alloc_group_id(manager, &group_id_desc);
-			if (!ret && copy_to_user((char __user *)arg, &group_id_desc,
-						 sizeof(group_id_desc)))
-				ret = -EFAULT;
-		} else {
-			ret = -EFAULT;
-		}
-		break;
 	case AIPU_IOCTL_FREE_GROUP_ID:
-		if (!copy_from_user(&group_id_desc, (struct aipu_group_id_desc __user *)arg,
-				    sizeof(group_id_desc)))
-			ret = aipu_job_manager_free_group_id(manager, &group_id_desc);
-		else
-			ret = -EFAULT;
+		ret = -EOPNOTSUPP;
 		break;
 	case AIPU_IOCTL_BUF_CACHE_INVALID:
 		if (!copy_from_user(&desc, (struct buf_desc __user *)arg, sizeof(desc)))
diff --git a/drivers/misc/armchina-npu-r2p0/aipu_common.c b/drivers/misc/armchina-npu-r2p0/aipu_common.c
--- a/drivers/misc/armchina-npu-r2p0/aipu_common.c
+++ b/drivers/misc/armchina-npu-r2p0/aipu_common.c
@@ -6,9 +6,7 @@
 #include <linux/of.h>
 #include "aipu_common.h"
 #include "aipu_partition.h"
-#ifdef CONFIG_SKY1
 #include "cix_sky1_soc.h"
-#endif
 
 #define MAX_CHAR_SYSFS 4096
 #define KBUF_MAX_SIZE 1024
diff --git a/drivers/misc/armchina-npu-r2p0/aipu_irq.h b/drivers/misc/armchina-npu-r2p0/aipu_irq.h
--- a/drivers/misc/armchina-npu-r2p0/aipu_irq.h
+++ b/drivers/misc/armchina-npu-r2p0/aipu_irq.h
@@ -12,12 +12,6 @@ typedef void (*aipu_irq_bhandler_t) (void *arg);
 typedef void (*aipu_irq_trigger_t) (void *arg);
 typedef void (*aipu_irq_ack_t) (void *arg);
 
-enum irq_event_type {
-	AIPU_IRQ_EVENT_NONE,
-	AIPU_IRQ_EVENT_PRINT,
-	AIPU_IRQ_EVENT_RESET,
-};
-
 /**
  * struct aipu_irq_object - interrupt object for every single AIPU instance
  * @irqnum:    interrupt number used to request IRQ
diff --git a/drivers/misc/armchina-npu-r2p0/aipu_job_manager.c b/drivers/misc/armchina-npu-r2p0/aipu_job_manager.c
--- a/drivers/misc/armchina-npu-r2p0/aipu_job_manager.c
+++ b/drivers/misc/armchina-npu-r2p0/aipu_job_manager.c
@@ -12,7 +12,6 @@
 #include "aipu_common.h"
 #include "zhouyi.h"
 #include "v3.h"
-#include "v3_1.h"
 
 static struct aipu_thread_wait_queue *do_create_thread_wait_queue(int uthread_id, struct file *filp)
 {
@@ -243,33 +242,37 @@ static void delete_job_queue(struct aipu_job_manager *manager, struct aipu_job *
 	}
 }
 
-inline bool is_job_version_match(struct aipu_partition *core, struct aipu_job_desc *user_job)
+static bool is_job_version_match(struct aipu_partition *partition,
+				 struct aipu_job_desc *user_job)
 {
-	if (core->arch != user_job->aipu_arch)
+	if (partition->arch != user_job->aipu_arch)
 		return false;
 
 	if (user_job->version_compatible)
 		return true;
 
-	if (core->version != user_job->aipu_version)
+	if (partition->version != user_job->aipu_version)
 		return false;
 
 	/* The configuration number is defined only for Zhouyi v1/v2 jobs. */
-	return core->version >= AIPU_ISA_VERSION_ZHOUYI_V3 ||
-		core->config == user_job->aipu_config;
+	return partition->version >= AIPU_ISA_VERSION_ZHOUYI_V3 ||
+		partition->config == user_job->aipu_config;
 }
 
-inline bool is_job_ok_for_core(struct aipu_partition *core, struct aipu_job_desc *user_job)
+static bool is_job_ok_for_core(struct aipu_partition *partition,
+			       struct aipu_job_desc *user_job)
 {
-	if (!is_job_version_match(core, user_job)) {
-		dev_err(core->dev, "invalid specified arch %d or version %d or configuration %d\n",
-			core->arch, core->version, core->config);
+	if (!is_job_version_match(partition, user_job)) {
+		dev_err(partition->dev,
+			"invalid specified arch %d or version %d or configuration %d\n",
+			partition->arch, partition->version, partition->config);
 		return false;
 	}
 
-	if (user_job->dtcm_size_kb > (core->dtcm_size >> 10)) {
-		dev_err(core->dev, "the requested dtcm size (%dKB) > total dtcm size (%dKB)\n",
-			user_job->dtcm_size_kb, core->dtcm_size >> 10);
+	if (user_job->dtcm_size_kb > partition->dtcm_size >> 10) {
+		dev_err(partition->dev,
+			"the requested dtcm size (%dKB) > total dtcm size (%dKB)\n",
+			user_job->dtcm_size_kb, partition->dtcm_size >> 10);
 		return false;
 	}
 
@@ -284,126 +287,41 @@ static bool is_user_job_valid(struct aipu_job_manager *manager, struct aipu_job_
 		AIPU_JOB_EXEC_FLAG_DBG_DISPATCH | AIPU_JOB_EXEC_FLAG_SEG_MMU;
 	u32 partition_id = user_job->partition_id;
 	u32 core_id = user_job->core_id;
-	int core_cnt = 0;
-	struct aipu_partition *partition = NULL;
-	struct aipu_partition *core = NULL;
-
-	if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3_1) {
-		if (manager->version != AIPU_ISA_VERSION_ZHOUYI_V3_1 ||
-		    partition_id > 1) {
-			dev_err(manager->dev, "invalid version number (%d) or partition ID (%d)",
-				user_job->aipu_version, partition_id);
-			return false;
-		}
+	struct aipu_partition *partition;
 
-		return true;
+	if (user_job->aipu_version != AIPU_ISA_VERSION_ZHOUYI_V3 ||
+	    manager->version != AIPU_ISA_VERSION_ZHOUYI_V3 ||
+	    partition_id >= (u32)manager->partition_cnt) {
+		dev_err(manager->dev, "invalid version number (%d) or partition ID (%u)",
+			user_job->aipu_version, partition_id);
+		return false;
 	}
-
-	/* for v3: partition->clusters->cores arch */
-	if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		if (manager->version != AIPU_ISA_VERSION_ZHOUYI_V3 ||
-		    partition_id >= (u32)manager->partition_cnt) {
-			dev_err(manager->dev, "invalid version number (%d) or partition ID (%u)",
-				user_job->aipu_version, partition_id);
-			return false;
-		}
-		if (user_job->version_compatible || user_job->exec_flag & ~v3_exec_mask ||
-		    (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_QOS_SLOW &&
-		     user_job->exec_flag & AIPU_JOB_EXEC_FLAG_QOS_FAST) ||
-		    (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_SINGLE_GROUP &&
-		     user_job->exec_flag & AIPU_JOB_EXEC_FLAG_MULTI_GROUP)) {
-			dev_err(manager->dev,
-				"invalid v3 execution flags or compatibility mode\n");
-			return false;
-		}
-
-		partition = &manager->partitions[partition_id];
-		if (!is_job_ok_for_core(partition, user_job))
-			return false;
-		if (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_DBG_DISPATCH) {
-			if (partition->cluster_cnt != 1)
-				return false;
-			core_cnt = atomic_read(&partition->clusters[0].en_core_cnt);
-			if (core_cnt <= 0 || core_id >= (u32)core_cnt) {
-				dev_err(partition->dev,
-					"invalid core ID (%u) >= enabled core count (%d)",
-					core_id, core_cnt);
-				return false;
-			}
-		}
-
-		return true;
+	if (user_job->version_compatible || user_job->exec_flag & ~v3_exec_mask ||
+	    (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_QOS_SLOW &&
+	     user_job->exec_flag & AIPU_JOB_EXEC_FLAG_QOS_FAST) ||
+	    (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_SINGLE_GROUP &&
+	     user_job->exec_flag & AIPU_JOB_EXEC_FLAG_MULTI_GROUP)) {
+		dev_err(manager->dev, "invalid v3 execution flags or compatibility mode\n");
+		return false;
 	}
+	partition = &manager->partitions[partition_id];
+	if (!is_job_ok_for_core(partition, user_job))
+		return false;
+	if (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_DBG_DISPATCH) {
+		int core_cnt;
 
-	/* for non-v3 archs, a partition just means an AIPU core */
-	core_cnt = manager->partition_cnt;
-
-	/* debugger deferred execution: reserve the specified core ID */
-	if (user_job->is_defer_run) {
-		if (core_id >= core_cnt) {
-			dev_err(manager->dev, "invalid core ID (%d) >= core count (%d)",
+		if (partition->cluster_cnt != 1)
+			return false;
+		core_cnt = atomic_read(&partition->clusters[0].en_core_cnt);
+		if (core_cnt <= 0 || core_id >= (u32)core_cnt) {
+			dev_err(partition->dev,
+				"invalid core ID (%u) >= enabled core count (%d)",
 				core_id, core_cnt);
 			return false;
 		}
-
-		core = &manager->partitions[core_id];
-		return is_job_ok_for_core(core, user_job);
 	}
 
-	/* for non-v3 normal execution: find an available core ID */
-	for (core_id = 0; core_id < core_cnt; core_id++) {
-		core = &manager->partitions[core_id];
-		if (is_job_ok_for_core(core, user_job))
-			return true;
-	}
-
-	return false;
-}
-
-static int get_available_core_no_lock(struct aipu_job_manager *manager, struct aipu_job *job)
-{
-	int id = 0;
-	struct aipu_partition *core = NULL;
-
-	if (unlikely(!manager))
-		return -1;
-
-	for (id = 0; id < manager->partition_cnt; id++) {
-		core = &manager->partitions[id];
-		if (!atomic_read(&core->disable) && manager->idle_bmap[id] &&
-		    is_job_ok_for_core(core, &job->desc))
-			return id;
-	}
-
-	return -1;
-}
-
-static void reserve_core_for_job_no_lock(struct aipu_job_manager *manager, struct aipu_job *job,
-					 int do_trigger)
-{
-	struct aipu_partition *sched_core = NULL;
-	int ret = 0;
-
-	WARN_ON(job->core_id < 0);
-	WARN_ON(job->core_id >= manager->partition_cnt);
-
-	sched_core = &manager->partitions[job->core_id];
-	manager->idle_bmap[job->core_id] = 0;
-	if (job->desc.enable_prof) {
-		get_soc_ops(sched_core)->start_bw_profiling(sched_core->dev, get_soc(sched_core));
-		job->sched_time = ktime_get();
-	}
-
-	ret = sched_core->ops->reserve(sched_core, &job->desc, do_trigger, 0);
-	if (do_trigger && !ret)
-		job->state = AIPU_JOB_STATE_RUNNING;
-
-	if (do_trigger)
-		dev_dbg(sched_core->dev, "[Job %lld of Thread %d] trigger job running done\n",
-			job->desc.job_id, job->uthread_id);
-	else
-		dev_dbg(sched_core->dev, "[Job %lld of Thread %d] reserve for deferred job done\n",
-			job->desc.job_id, job->uthread_id);
+	return true;
 }
 
 static void check_enable_tec_interrupts(struct aipu_job_manager *manager, struct aipu_job *job)
@@ -436,44 +354,6 @@ static void check_enable_tec_interrupts(struct aipu_job_manager *manager, struct
 	manager->tec_intr_en = true;
 }
 
-static int schedule_v3_1_job_no_lock(struct aipu_job_manager *manager, struct aipu_job *job)
-{
-	int ret = 0;
-	struct aipu_partition *cluster = &manager->partitions[0];
-	int pool_type = ZHOUYI_COMMAND_POOL_PCP;
-	int partition_id = job->desc.partition_id;
-	int trigger_type = ZHOUYI_TRIGGER_TYPE_CREATE;
-	struct command_pool *pool = &manager->pools[partition_id];
-
-	if (job->desc.partition_id == AIPU_PARTITION_ID_0) {
-		pool_type = ZHOUYI_COMMAND_POOL_PCP;
-	} else {
-		dev_err(manager->dev, "invalid partition_id: %d\n", job->desc.partition_id);
-		return -EINVAL;
-	}
-
-	if (!pool->created)
-		trigger_type = ZHOUYI_TRIGGER_TYPE_CREATE;
-	else if (pool->aborted)
-		trigger_type = ZHOUYI_TRIGGER_TYPE_UPDATE_DISPATCH;
-	else
-		trigger_type = ZHOUYI_TRIGGER_TYPE_DISPATCH;
-
-	ret = cluster->ops->reserve(cluster, &job->desc, trigger_type, pool_type);
-	if (!ret) {
-		if (trigger_type == ZHOUYI_TRIGGER_TYPE_CREATE)
-			pool->created = true;
-
-		if (job->desc.exec_flag & AIPU_JOB_EXEC_FLAG_DBG_DISPATCH)
-			pool->debug = true;
-
-		if (pool->aborted)
-			pool->aborted = false;
-	}
-
-	return ret;
-}
-
 static int schedule_v3_job_no_lock(struct aipu_job_manager *manager, struct aipu_job *job)
 {
 	int ret = 0;
@@ -634,8 +514,7 @@ static int schedule_new_job(struct aipu_job_manager *manager, struct aipu_job_de
 	}
 	mutex_unlock(&manager->wq_lock);
 
-	if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3 &&
-	    (!user_job->head_tcb_pa ||
+	if (!user_job->head_tcb_pa ||
 	    user_job->head_tcb_pa > user_job->first_task_tcb_pa ||
 	    user_job->first_task_tcb_pa > user_job->last_task_tcb_pa ||
 	    user_job->last_task_tcb_pa > user_job->tail_tcb_pa ||
@@ -643,7 +522,7 @@ static int schedule_new_job(struct aipu_job_manager *manager, struct aipu_job_de
 	    !IS_ALIGNED(user_job->head_tcb_pa, sizeof(struct aipu_tcb)) ||
 	    !IS_ALIGNED(user_job->first_task_tcb_pa, sizeof(struct aipu_tcb)) ||
 	    !IS_ALIGNED(user_job->last_task_tcb_pa, sizeof(struct aipu_tcb)) ||
-	    !IS_ALIGNED(user_job->tail_tcb_pa, sizeof(struct aipu_tcb)))) {
+	    !IS_ALIGNED(user_job->tail_tcb_pa, sizeof(struct aipu_tcb))) {
 		ret = -EINVAL;
 		goto fail_job;
 	}
@@ -651,26 +530,22 @@ static int schedule_new_job(struct aipu_job_manager *manager, struct aipu_job_de
 				  user_job->tail_tcb_pa, &kern_job->tcb_mapping);
 	if (ret)
 		goto fail_job;
-	if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		/* Validate the documented V3 envelope after pinning its owner's allocation. */
-		head_tcb = aipu_mm_get_tcb(manager->mm, user_job->head_tcb_pa);
-		first_tcb = aipu_mm_get_tcb(manager->mm, user_job->first_task_tcb_pa);
-		last_tcb = aipu_mm_get_tcb(manager->mm, user_job->last_task_tcb_pa);
-		if (!head_tcb || !first_tcb || !last_tcb ||
-		    !IS_INIT_TCB(READ_ONCE(head_tcb->flag)) ||
-		    !IS_TASK_TCB(READ_ONCE(first_tcb->flag)) ||
-		    !IS_TASK_TCB(READ_ONCE(last_tcb->flag))) {
-			ret = -EINVAL;
-			goto fail_job;
-		}
+	/* Validate the documented V3 envelope after pinning its owner's allocation. */
+	head_tcb = aipu_mm_get_tcb(manager->mm, user_job->head_tcb_pa);
+	first_tcb = aipu_mm_get_tcb(manager->mm, user_job->first_task_tcb_pa);
+	last_tcb = aipu_mm_get_tcb(manager->mm, user_job->last_task_tcb_pa);
+	if (!head_tcb || !first_tcb || !last_tcb ||
+	    !IS_INIT_TCB(READ_ONCE(head_tcb->flag)) ||
+	    !IS_TASK_TCB(READ_ONCE(first_tcb->flag)) ||
+	    !IS_TASK_TCB(READ_ONCE(last_tcb->flag))) {
+		ret = -EINVAL;
+		goto fail_job;
 	}
 
-	if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		ret = aipu_mm_hold_tcb_buf_alloc(manager->mm, kern_job);
-		if (ret != 0) {
-			dev_err(manager->dev, "malloc placeholder tcb failed.\n");
-			goto fail_job;
-		}
+	ret = aipu_mm_hold_tcb_buf_alloc(manager->mm, kern_job);
+	if (ret) {
+		dev_err(manager->dev, "malloc placeholder tcb failed.\n");
+		goto fail_job;
 	}
 
 	spin_lock_irqsave(&manager->lock, flags);
@@ -679,60 +554,17 @@ static int schedule_new_job(struct aipu_job_manager *manager, struct aipu_job_de
 		list_add_tail(&kern_job->node, &manager->scheduled_head->node);
 		listed = true;
 
-		if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3_1) {
-			ret = schedule_v3_1_job_no_lock(manager, kern_job);
-			if (!ret) {
-				kern_job->state = AIPU_JOB_STATE_RUNNING;
-			} else if (ret == ZHOUYI_V3_1_COMMAND_POOL_FULL) {
-				kern_job->state = AIPU_JOB_STATE_PENDING;
-				ret = 0;
-			} else {
-				kern_job->state = AIPU_JOB_STATE_EXCEP;
-			}
-		} else if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-			ret = schedule_v3_job_no_lock(manager, kern_job);
-			if (!ret)
-				kern_job->state = AIPU_JOB_STATE_RUNNING;
-			else
-				kern_job->state = AIPU_JOB_STATE_DEFERRED;
-		} else {
-			/*
-			 * For a job using SRAM managed by AIPU Gbuilder, it should be
-			 * executed exclusively in serial with other Gbuilder-managed-SRAM ones,
-			 * and parallel scheduling is not allowed.
-			 *
-			 * Pending it if there has been a Gbuilder-managed-SRAM job running;
-			 * otherwise mark the flag and reserve a core for running.
-			 */
-			if (kern_job->desc.exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX) {
-				if (manager->exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX)
-					goto unlock;
-				else
-					manager->exec_flag |= AIPU_JOB_EXEC_FLAG_SRAM_MUTEX;
-			}
-			kern_job->core_id = get_available_core_no_lock(manager, kern_job);
-			if (kern_job->core_id >= 0)
-				reserve_core_for_job_no_lock(manager, kern_job, do_trigger);
-		}
+		ret = schedule_v3_job_no_lock(manager, kern_job);
+		if (!ret)
+			kern_job->state = AIPU_JOB_STATE_RUNNING;
+		else
+			kern_job->state = AIPU_JOB_STATE_DEFERRED;
 	} else {
-		if (user_job->aipu_version < AIPU_ISA_VERSION_ZHOUYI_V3 &&
-		    (user_job->core_id >= manager->partition_cnt ||
-		     !manager->idle_bmap[user_job->core_id])) {
-			dev_err(manager->dev, "schedule new job (0x%llx) failed: invalid core ID %u",
-				kern_job->desc.job_id, user_job->core_id);
-			ret = -EINVAL;
-			goto unlock;
-		}
-
 		kern_job->state = AIPU_JOB_STATE_DEFERRED;
 		kern_job->core_id = user_job->core_id;
 		list_add_tail(&kern_job->node, &manager->scheduled_head->node);
 		listed = true;
-
-		if (user_job->aipu_version < AIPU_ISA_VERSION_ZHOUYI_V3)
-			reserve_core_for_job_no_lock(manager, kern_job, do_trigger);
 	}
-unlock:
 	if (ret && listed)
 		list_del_init(&kern_job->node);
 	spin_unlock_irqrestore(&manager->lock, flags);
@@ -750,26 +582,14 @@ static int trigger_deferred_job_run(struct aipu_job_manager *manager,
 {
 	unsigned long flags;
 	struct aipu_job *curr = NULL;
-	struct aipu_partition *sched_core = NULL;
 	int ret = -ENOENT;
 
 	spin_lock_irqsave(&manager->lock, flags);
 	list_for_each_entry(curr, &manager->scheduled_head->node, node) {
 		if (curr->filp == filp && curr->desc.job_id == user_job->job_id &&
 		    curr->state == AIPU_JOB_STATE_DEFERRED) {
-			if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3_1) {
-				ret = schedule_v3_1_job_no_lock(manager, curr);
-			} else if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-				/**
-				 * for debugger: it should ensure that the NPUs are free to accept
-				 * new jobs before the deferred job is scheduled.
-				 */
-				ret = schedule_v3_job_no_lock(manager, curr);
-			} else {
-				sched_core = &manager->partitions[curr->core_id];
-				sched_core->ops->trigger(sched_core);
-				ret = 0;
-			}
+			/* Debugger must ensure the NPU can accept a deferred job. */
+			ret = schedule_v3_job_no_lock(manager, curr);
 			if (!ret)
 				curr->state = AIPU_JOB_STATE_RUNNING;
 			break;
@@ -810,7 +630,6 @@ int init_aipu_job_manager(struct aipu_job_manager *manager, struct aipu_memory_m
 	manager->partition_cnt = 0;
 	manager->partitions = NULL;
 	manager->pools = NULL;
-	manager->idle_bmap = NULL;
 	manager->mm = mm;
 	manager->priv = priv;
 	manager->job_cache =
@@ -879,8 +698,6 @@ void deinit_aipu_job_manager(struct aipu_job_manager *manager)
 	if (!manager || !manager->is_init)
 		return;
 
-	kfree(manager->idle_bmap);
-	manager->idle_bmap = NULL;
 	delete_job_queue(manager, &manager->scheduled_head);
 	delete_wait_queue(&manager->wait_queue_head);
 	mutex_destroy(&manager->wq_lock);
@@ -910,18 +727,11 @@ int aipu_job_manager_set_partitions_info(struct aipu_job_manager *manager, int p
 
 	manager->partition_cnt = partition_cnt;
 	manager->partitions = partitions;
-	if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3 ||
-	    manager->version == AIPU_ISA_VERSION_ZHOUYI_V3_1) {
-		manager->pools = devm_kzalloc(partitions[0].dev,
-					      partition_cnt * sizeof(*manager->pools),
-					      GFP_KERNEL);
-		if (!manager->pools)
-			return -ENOMEM;
-	}
-	manager->idle_bmap = kmalloc_array(partition_cnt, sizeof(bool), GFP_KERNEL);
-	if (!manager->idle_bmap)
+	manager->pools = devm_kzalloc(partitions[0].dev,
+				      partition_cnt * sizeof(*manager->pools),
+				      GFP_KERNEL);
+	if (!manager->pools)
 		return -ENOMEM;
-	memset(manager->idle_bmap, 1, partition_cnt);
 
 	return 0;
 }
@@ -1117,8 +927,8 @@ static bool is_curr_irq_job(struct aipu_job *job, struct job_irq_info *info, u64
 		   info->tail_tcbp <= (u32)(job->desc.last_task_tcb_pa - asid_base);
 }
 
-static bool is_v3x_job_done_or_excep(struct aipu_job *job, struct job_irq_info *info,
-				      u64 asid_base, int flag)
+static bool is_v3_job_done_or_excep(struct aipu_job *job, struct job_irq_info *info,
+				    u64 asid_base, int flag)
 {
 	return info->tail_tcbp == (u32)(job->desc.last_task_tcb_pa - asid_base) &&
 	       (IS_DONE_IRQ(flag) || IS_EXCEPTION_IRQ(flag));
@@ -1132,44 +942,20 @@ static bool do_abortion(int flag, struct job_irq_info *info)
 	return IS_SERIOUS_ERR(flag) || IS_EXCEPTION_SIGNAL(info->sig_flag);
 }
 
-static bool do_abortion_V3_1(int flag, struct job_irq_info *info)
-{
-	if (!info)
-		return false;
-
-	if (IS_TEC_IRQ_V3_1(flag) && (IS_FAULT_IRQ_V3_1(flag)))
-		return true;
-
-	return IS_EXCEPTION_SIGNAL_V3_1(info->sig_flag);
-}
-
-static bool is_job_end(struct aipu_job *job, struct aipu_partition *partition,
-		       struct job_irq_info *info, u64 asid_base, int flag)
+static bool is_job_end(struct aipu_job *job, struct job_irq_info *info,
+		       u64 asid_base, int flag)
 {
 	if (job->state != AIPU_JOB_STATE_RUNNING)
 		return false;
 
-	if (job->desc.aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		return is_v3x_job_done_or_excep(job, info, asid_base, flag) ||
-			do_abortion(flag, info);
-	} else if (job->desc.aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3_1) {
-		return is_v3x_job_done_or_excep(job, info, asid_base, flag) ||
-			IS_ERROR_IRQ_V3_1(flag) || IS_TIMEOUT_IRQ_V3_1(flag) ||
-			IS_FAULT_IRQ_V3_1(flag) || do_abortion_V3_1(flag, info);
-	}
-
-	return job->core_id == partition->id;
+	return is_v3_job_done_or_excep(job, info, asid_base, flag) ||
+		do_abortion(flag, info);
 }
 
 static bool is_job_abnormal(struct aipu_job *job, int flag,
 			    struct job_irq_info *info)
 {
-	if (job->desc.aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3)
-		return IS_ABNORMAL(flag) || IS_EXCEPTION_SIGNAL(info->sig_flag);
-	else if (job->desc.aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3_1)
-		return IS_ABNORMAL_V3_1(flag) || IS_EXCEPTION_SIGNAL_V3_1(info->sig_flag);
-
-	return flag != 0;
+	return IS_ABNORMAL(flag) || IS_EXCEPTION_SIGNAL(info->sig_flag);
 }
 
 /**
@@ -1183,72 +969,40 @@ void aipu_job_manager_irq_upper_half(struct aipu_partition *partition, int flag,
 {
 	struct aipu_job *curr = NULL;
 	struct aipu_job_manager *manager = NULL;
-	int triggered = 0;
+	bool abort_cmdpool;
 
 	if (unlikely(!partition))
 		return;
 
 	manager = get_job_manager(partition);
 
-	if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3_1) {
-		if (IS_SIGNAL_IRQ_V3_1(flag)) {
-			if (IS_PRINTF_SIGNAL_V3_1(info->sig_flag))
-				aipu_job_manager_real_time_printk(manager, partition, info);
+	if (IS_SIGNAL_IRQ(flag)) {
+		if (IS_PRINTF_SIGNAL(info->sig_flag))
+			aipu_job_manager_real_time_printk(manager, partition, info);
 
 #if AIPU_CONFIG_ENABLE_INTR_PROFILING
-			if (IS_PROFILER_SIGNAL_V3_1(info->sig_flag)) {
-				pr_info("profiler signal intr...\n");
-				aipu_job_manager_real_time_get_pdata(manager, info);
-			}
-#endif
-			if (IS_COREDUMP_SIGNAL_V3_1(info->sig_flag)) {
-				spin_lock(&manager->lock);
-				list_for_each_entry(curr, &manager->scheduled_head->node, node) {
-					if (is_curr_irq_job(curr, info, manager->asid0_base)) {
-						curr->state = AIPU_JOB_STATE_CORED;
-						break;
-					}
-				}
-				spin_unlock(&manager->lock);
-			}
-			return;
+		if (IS_PROFILER_SIGNAL(info->sig_flag)) {
+			pr_info("profiler signal intr...\n");
+			aipu_job_manager_real_time_get_pdata(manager, info);
 		}
-	} else if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		if (IS_SIGNAL_IRQ(flag)) {
-			if (IS_PRINTF_SIGNAL(info->sig_flag))
-				aipu_job_manager_real_time_printk(manager, partition, info);
-
-#if AIPU_CONFIG_ENABLE_INTR_PROFILING
-			if (IS_PROFILER_SIGNAL(info->sig_flag)) {
-				pr_info("profiler signal intr...\n");
-				aipu_job_manager_real_time_get_pdata(manager, info);
-			}
 #endif
-			if (IS_COREDUMP_SIGNAL(info->sig_flag)) {
-				spin_lock(&manager->lock);
-				list_for_each_entry(curr, &manager->scheduled_head->node, node) {
-					if (is_curr_irq_job(curr, info, manager->asid0_base)) {
-						curr->state = AIPU_JOB_STATE_CORED;
-						break;
-					}
+		if (IS_COREDUMP_SIGNAL(info->sig_flag)) {
+			spin_lock(&manager->lock);
+			list_for_each_entry(curr, &manager->scheduled_head->node, node) {
+				if (is_curr_irq_job(curr, info, manager->asid0_base)) {
+					curr->state = AIPU_JOB_STATE_CORED;
+					break;
 				}
-				spin_unlock(&manager->lock);
 			}
-			return;
+			spin_unlock(&manager->lock);
 		}
+		return;
 	}
 
 	spin_lock(&manager->lock);
 
 	/* soft reset association irq not in coredump scope */
-	bool abort_cmdpool = false;
-	if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3_1) {
-		if (do_abortion_V3_1(flag, info) && manager->pools[partition->id].created)
-			abort_cmdpool = true;
-	} else if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		if (do_abortion(flag, info))
-			abort_cmdpool = true;
-	}
+	abort_cmdpool = do_abortion(flag, info);
 
 	if (abort_cmdpool) {
 		list_for_each_entry(curr, &manager->scheduled_head->node, node) {
@@ -1258,13 +1012,12 @@ void aipu_job_manager_irq_upper_half(struct aipu_partition *partition, int flag,
 				return;
 			}
 		}
-			partition->ops->abort_command_pool(partition, 0);
-			if (manager->pools)
-				manager->pools[partition->id].aborted = true;
-		}
+		partition->ops->abort_command_pool(partition, 0);
+		manager->pools[partition->id].aborted = true;
+	}
 
 	list_for_each_entry(curr, &manager->scheduled_head->node, node) {
-		if (is_job_end(curr, partition, info, manager->asid0_base, flag)) {
+		if (is_job_end(curr, info, manager->asid0_base, flag)) {
 			if (unlikely(is_job_abnormal(curr, flag, info)))
 				curr->state = AIPU_JOB_STATE_EXCEP;
 			else
@@ -1284,37 +1037,13 @@ void aipu_job_manager_irq_upper_half(struct aipu_partition *partition, int flag,
 			else
 				curr->pdata.tick_counter = 0;
 
-			if (curr->desc.exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX)
-				manager->exec_flag &= ~AIPU_JOB_EXEC_FLAG_SRAM_MUTEX;
-
-			if (manager->pools && manager->pools->debug)
+			if (manager->pools->debug)
 				manager->dbg_do_destroy = true;
 
 			break;
 		}
 	}
 
-	if (!atomic_read(&partition->disable)) {
-		list_for_each_entry(curr, &manager->scheduled_head->node, node) {
-			if (curr->state == AIPU_JOB_STATE_PENDING &&
-			    is_job_ok_for_core(partition, &curr->desc)) {
-				if (curr->desc.exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX) {
-					if (manager->exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX)
-						continue;
-					else
-						manager->exec_flag |= AIPU_JOB_EXEC_FLAG_SRAM_MUTEX;
-				}
-				curr->core_id = partition->id;
-				reserve_core_for_job_no_lock(manager, curr, 1);
-				triggered = 1;
-				break;
-			}
-		}
-	}
-
-	if (!triggered)
-		manager->idle_bmap[partition->id] = 1;
-
 	spin_unlock(&manager->lock);
 }
 
@@ -1330,22 +1059,13 @@ static void aipu_job_manager_destroy_command_pool_no_lock(struct aipu_job_manage
 
 	pool = manager->pools;
 	if (pool && pool->created) {
-		if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-			ret = partition->ops->destroy_command_pool(partition, 0);
-			if (ret == 0) {
+		ret = partition->ops->destroy_command_pool(partition, 0);
+		if (!ret) {
 			memset(pool->qlist, 0, sizeof(*pool->qlist) * AIPU_JOB_QOS_MAX);
 			pool->created = false;
 			pool->debug = false;
 			manager->tec_intr_en = false;
 			aipu_mm_set_final_htbuf_index(manager->mm, -1);
-			}
-		} else if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3_1) {
-			ret = partition->ops->destroy_command_pool(partition, 0);
-			if (ret == 0) {
-			manager->tec_intr_en = false;
-			pool->created = false;
-			pool->debug = false;
-			}
 		}
 	}
 }
@@ -1360,8 +1080,7 @@ void aipu_job_manager_irq_bottom_half(struct aipu_partition *core)
 	struct aipu_job *next = NULL;
 	struct aipu_job_manager *manager = NULL;
 	unsigned long flags;
-	bool do_destroy = core->version == AIPU_ISA_VERSION_ZHOUYI_V3 ||
-			  core->version == AIPU_ISA_VERSION_ZHOUYI_V3_1;
+	bool do_destroy = true;
 
 	if (unlikely(!core))
 		return;
@@ -1370,36 +1089,14 @@ void aipu_job_manager_irq_bottom_half(struct aipu_partition *core)
 
 	spin_lock_irqsave(&manager->lock, flags);
 
-	//global reset in bottom half and set all job exception
-	if (core->version == AIPU_ISA_VERSION_ZHOUYI_V3_1) {
-		if (core->event_type == AIPU_IRQ_EVENT_RESET) {
-			if (core->ops->soft_reset(core, core->reg)) {
-				dev_err(core->dev, "global reset fails in bottom half.\n");
-			} else {
-				list_for_each_entry_safe(curr, next,
-							 &manager->scheduled_head->node, node) {
-					curr->state = AIPU_JOB_STATE_EXCEP;
-				}
-				core->event_type = AIPU_IRQ_EVENT_NONE;
-				manager->pools->created = false;
-				manager->pools->debug = false;
-				manager->tec_intr_en = false;
-			}
-		}
-	}
-
 	list_for_each_entry_safe(curr, next, &manager->scheduled_head->node, node) {
-		if (curr->state >= AIPU_JOB_STATE_EXCEP && !curr->wake_up &&
-		    (curr->desc.aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3 ||
-		     curr->core_id == core->id)) {
+		if (curr->state >= AIPU_JOB_STATE_EXCEP && !curr->wake_up) {
 			if (curr->desc.enable_prof)
 				curr->pdata.execution_time_ns =
 				(long)ktime_to_ns(ktime_sub(curr->done_time, curr->sched_time));
 
-			if (curr->desc.aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-				aipu_mm_unlink_tcb(manager->mm, curr->curr_hold_tcb, false);
-				curr->curr_hold_tcb = 0;
-			}
+			aipu_mm_unlink_tcb(manager->mm, curr->curr_hold_tcb, false);
+			curr->curr_hold_tcb = 0;
 		}
 
 		/* destroy the v3 command pool if all jobs are done */
@@ -1421,9 +1118,7 @@ void aipu_job_manager_irq_bottom_half(struct aipu_partition *core)
 		aipu_job_manager_destroy_command_pool_no_lock(manager, core, true);
 
 	list_for_each_entry_safe(curr, next, &manager->scheduled_head->node, node) {
-		if (curr->state >= AIPU_JOB_STATE_EXCEP && !curr->wake_up &&
-		    (curr->desc.aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3 ||
-		     curr->core_id == core->id)) {
+		if (curr->state >= AIPU_JOB_STATE_EXCEP && !curr->wake_up) {
 			wake_up_interruptible(curr->thread_queue);
 			curr->wake_up = 1;
 #ifdef DEBUG
@@ -1950,12 +1645,7 @@ int aipu_job_manager_suspend(struct aipu_job_manager *manager)
 
 	memset(&cfg, 0, sizeof(cfg));
 
-	if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3 ||
-	    manager->version == AIPU_ISA_VERSION_ZHOUYI_V3_1)
-		return aipu_job_manager_config_clusters(manager, &cfg);
-
-	atomic_set(&manager->is_suspend, 1);
-	return 0;
+	return aipu_job_manager_config_clusters(manager, &cfg);
 }
 
 int aipu_job_manager_resume(struct aipu_job_manager *manager)
@@ -1967,12 +1657,7 @@ int aipu_job_manager_resume(struct aipu_job_manager *manager)
 
 	cfg.clusters[0].en_core_cnt = manager->partitions[0].clusters[0].core_cnt;
 
-	if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3 ||
-	    manager->version == AIPU_ISA_VERSION_ZHOUYI_V3_1)
-		return aipu_job_manager_config_clusters(manager, &cfg);
-
-	atomic_set(&manager->is_suspend, 0);
-	return 0;
+	return aipu_job_manager_config_clusters(manager, &cfg);
 }
 
 int aipu_job_manager_alloc_grid_id(struct aipu_job_manager *manager)
@@ -1988,47 +1673,3 @@ int aipu_job_manager_alloc_grid_id(struct aipu_job_manager *manager)
 
 	return id;
 }
-
-int aipu_job_manager_alloc_group_id(struct aipu_job_manager *manager,
-				    struct aipu_group_id_desc *desc)
-{
-	int ret = 0;
-	u16 id = 0;
-
-	if (!manager || !desc || !manager->group_id_bmap)
-		return -EINVAL;
-
-	if (!desc->group_size || desc->group_size >= manager->group_id_num)
-		return -EINVAL;
-
-	mutex_lock(&manager->id_lock);
-	id = bitmap_find_next_zero_area(manager->group_id_bmap, manager->group_id_num,
-					0, desc->group_size, 0);
-	if (id < manager->group_id_num) {
-		bitmap_set(manager->group_id_bmap, id, desc->group_size);
-		desc->first_id = id;
-	} else {
-		desc->first_id = 0;
-		ret = -EINVAL;
-	}
-	mutex_unlock(&manager->id_lock);
-
-	return ret;
-}
-
-int aipu_job_manager_free_group_id(struct aipu_job_manager *manager,
-				   struct aipu_group_id_desc *desc)
-{
-	if (!manager || !desc || !manager->group_id_bmap)
-		return -EINVAL;
-
-	if (desc->first_id >= manager->group_id_num ||
-	    (desc->first_id + desc->group_size) >= manager->group_id_num)
-		return -EINVAL;
-
-	mutex_lock(&manager->id_lock);
-	bitmap_clear(manager->group_id_bmap, desc->first_id, desc->group_size);
-	mutex_unlock(&manager->id_lock);
-
-	return 0;
-}
diff --git a/drivers/misc/armchina-npu-r2p0/aipu_job_manager.h b/drivers/misc/armchina-npu-r2p0/aipu_job_manager.h
--- a/drivers/misc/armchina-npu-r2p0/aipu_job_manager.h
+++ b/drivers/misc/armchina-npu-r2p0/aipu_job_manager.h
@@ -153,7 +153,6 @@ struct command_pool {
  * @dev:             pointer to struct device
  * @partitions:      aipu partition/core struct pointer array
  * @pools:           v3 command pools
- * @idle_bmap:       idle flag bitmap for every partition/core
  * @scheduled_head:  scheduled job list head
  * @lock:            spinlock
  * @wait_queue_head: wait queue list head
@@ -172,9 +171,7 @@ struct command_pool {
  * @dbg_do_destroy:  do destroy flag (enabled after a debug-dispatch job ends)
  * @tec_intr_en:     is TEC interrupts enabled or not
  * @grid_id:         the latest grid ID allocated for userspace
- * @group_id_bmap:   gruop ID bitmap
- * @group_id_num:    number of group IDs available
- * @id_lock:         lock to protect the grid Id & group IDs
+ * @id_lock:         lock protecting grid ID allocation
  */
 struct aipu_job_manager {
 	int version;
@@ -182,7 +179,6 @@ struct aipu_job_manager {
 	struct device *dev;
 	struct aipu_partition *partitions;
 	struct command_pool *pools;
-	bool *idle_bmap;
 	struct aipu_job *scheduled_head;
 	spinlock_t lock; /* Protect cores and jobs status */
 	struct aipu_thread_wait_queue *wait_queue_head;
@@ -202,9 +198,7 @@ struct aipu_job_manager {
 	bool dbg_do_destroy;
 	bool tec_intr_en;
 	u16  grid_id;
-	unsigned long *group_id_bmap;
-	u16  group_id_num;
-	struct mutex id_lock; /* Protect grid & group IDs */
+	struct mutex id_lock;
 };
 
 int init_aipu_job_manager(struct aipu_job_manager *manager, struct aipu_memory_manager *mm,
@@ -236,9 +230,5 @@ int aipu_job_manager_config_clusters(struct aipu_job_manager *manager,
 int aipu_job_manager_suspend(struct aipu_job_manager *manager);
 int aipu_job_manager_resume(struct aipu_job_manager *manager);
 int aipu_job_manager_alloc_grid_id(struct aipu_job_manager *manager);
-int aipu_job_manager_alloc_group_id(struct aipu_job_manager *manager,
-				    struct aipu_group_id_desc *desc);
-int aipu_job_manager_free_group_id(struct aipu_job_manager *manager,
-				   struct aipu_group_id_desc *desc);
 
 #endif /* __AIPU_JOB_MANAGER_H__ */
diff --git a/drivers/misc/armchina-npu-r2p0/aipu_mm.c b/drivers/misc/armchina-npu-r2p0/aipu_mm.c
--- a/drivers/misc/armchina-npu-r2p0/aipu_mm.c
+++ b/drivers/misc/armchina-npu-r2p0/aipu_mm.c
@@ -24,7 +24,6 @@
 #include "aipu_mm.h"
 #include "aipu_common.h"
 #include "aipu_dma_buf.h"
-#include "v2.h"
 
 #define EXIT_ENCODE_BUF_SIZE (1024)
 /*
@@ -1092,13 +1091,9 @@ static int aipu_mm_add_iova_region(struct aipu_memory_manager *mm)
 		reg = obj->reg;
 
 		list_add(&obj->list, &mm->mem.head->list);
-		if (mm->version > AIPU_ISA_VERSION_ZHOUYI_V1) {
-			ret = add_region_list(mm, asid_idx, reg);
-			if (iova_region == 1)
-				ret = ret ?: add_region_list(mm, AIPU_BUF_ASID_1, reg);
-		} else {
-			ret = add_region_list(mm, AIPU_BUF_ASID_0, reg);
-		}
+		ret = add_region_list(mm, asid_idx, reg);
+		if (iova_region == 1)
+			ret = ret ?: add_region_list(mm, AIPU_BUF_ASID_1, reg);
 		if (ret) {
 			remove_region_list_refs(mm, reg);
 			aipu_mm_destroy_region_object(mm, obj);
@@ -1161,13 +1156,6 @@ static int aipu_mm_add_reserved_regions(struct aipu_memory_manager *mm)
 			continue;
 		}
 
-		if (type == AIPU_MEM_REGION_TYPE_DTCM &&
-		    mm->version == AIPU_ISA_VERSION_ZHOUYI_V1) {
-			dev_err(mm->dev, "dts: bypass because v1 does not support dtcm\n");
-			of_node_put(np);
-			continue;
-		}
-
 		ret = of_address_to_resource(np, 0, &res);
 		if (ret) {
 			of_node_put(np);
@@ -1176,12 +1164,6 @@ static int aipu_mm_add_reserved_regions(struct aipu_memory_manager *mm)
 		}
 
 		size = res.end - res.start + 1;
-		if (type == AIPU_MEM_REGION_TYPE_DTCM && size > ZHOUYI_V2_DTCM_MAX_BYTES) {
-			dev_info(mm->dev,
-				 "the DTCM will be clipped to the maximum configurable value\n");
-			size = ZHOUYI_V2_DTCM_MAX_BYTES;
-		}
-
 		if (of_property_read_u64(np, "host-aipu-offset", &offset))
 			offset = 0;
 
@@ -1196,38 +1178,33 @@ static int aipu_mm_add_reserved_regions(struct aipu_memory_manager *mm)
 		list_add(&obj->list, &mm->mem.head->list);
 
 		/* get asid of this region */
-		if (mm->version > AIPU_ISA_VERSION_ZHOUYI_V1) {
-			for (asid_idx = 0; asid_idx < ZHOUYI_ASID_COUNT; asid_idx++) {
-				u32 asid = 0;
-
-				if (!of_property_read_u32_index(np, "asid", asid_idx, &asid)) {
-					if (asid >= ZHOUYI_ASID_COUNT) {
-						dev_err(mm->dev, "dts: invalid asid: %u\n", asid);
-						continue;
-					}
-
-					list_ret = add_region_list(mm, asid, reg);
-					if (list_ret)
-						break;
-					if (asid >= mm->valid_asid_cnt)
-						mm->valid_asid_cnt = asid + 1;
-
-					asid_set = true;
-				} else {
-					break;
+		for (asid_idx = 0; asid_idx < ZHOUYI_ASID_COUNT; asid_idx++) {
+			u32 asid = 0;
+
+			if (!of_property_read_u32_index(np, "asid", asid_idx, &asid)) {
+				if (asid >= ZHOUYI_ASID_COUNT) {
+					dev_err(mm->dev, "dts: invalid asid: %u\n", asid);
+					continue;
 				}
-			}
 
-			if (!list_ret && !asid_set) {
-				dev_err(mm->dev, "dts: reg asid is not set, use default asid\n");
-				mm->valid_asid_cnt = 2;
-				list_ret = add_region_list(mm, AIPU_BUF_ASID_0, reg);
-				if (!list_ret)
-					list_ret = add_region_list(mm, AIPU_BUF_ASID_1, reg);
+				list_ret = add_region_list(mm, asid, reg);
+				if (list_ret)
+					break;
+				if (asid >= mm->valid_asid_cnt)
+					mm->valid_asid_cnt = asid + 1;
+
+				asid_set = true;
+			} else {
+				break;
 			}
-		} else {
-			/* use ASID 0 for V1 */
+		}
+
+		if (!list_ret && !asid_set) {
+			dev_err(mm->dev, "dts: reg asid is not set, use default asid\n");
+			mm->valid_asid_cnt = 2;
 			list_ret = add_region_list(mm, AIPU_BUF_ASID_0, reg);
+			if (!list_ret)
+				list_ret = add_region_list(mm, AIPU_BUF_ASID_1, reg);
 		}
 		if (list_ret) {
 			remove_region_list_refs(mm, reg);
@@ -1355,23 +1332,15 @@ int aipu_init_mm(struct aipu_memory_manager *mm, struct platform_device *p_dev,
 	if (ret)
 		goto finish;
 
-	/* we accept at maximum 2 GM regions: for QoS fast & slow */
-	if (mm->version == AIPU_ISA_VERSION_ZHOUYI_V3)
-		mm->gm_max_cnt = 2;
-
-	/* currently, we only support one DTCM region in v2_2 */
-	if (mm->version == AIPU_ISA_VERSION_ZHOUYI_V2_2)
-		mm->dtcm_max_cnt = 1;
-
-	if (version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		mm->hold_tcb_head = kmem_cache_zalloc(mm->hold_tbuf_cache, GFP_KERNEL);
-		if (!mm->hold_tcb_head) {
-			ret = -ENOMEM;
-			goto finish;
-		}
-		INIT_LIST_HEAD(&mm->hold_tcb_head->node);
-		mm->hold_tcb_head->nums = 0;
+	/* Sky1 V3 accepts at maximum 2 GM regions: QoS fast and slow. */
+	mm->gm_max_cnt = 2;
+	mm->hold_tcb_head = kmem_cache_zalloc(mm->hold_tbuf_cache, GFP_KERNEL);
+	if (!mm->hold_tcb_head) {
+		ret = -ENOMEM;
+		goto finish;
 	}
+	INIT_LIST_HEAD(&mm->hold_tcb_head->node);
+	mm->hold_tcb_head->nums = 0;
 	/**
 	 * Device tree binding for Zhouyi V3:
 	 *
@@ -1382,24 +1351,19 @@ int aipu_init_mm(struct aipu_memory_manager *mm, struct platform_device *p_dev,
 	 * 1: GM is shared by all tasks in 1 cluster (by default if this attribute is not provided)
 	 * 2: GM is divided half-by-half: for QoS slow & fast tasks, respectively
 	 */
-	if (version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		ret = device_property_read_u32(mm->dev, "gm-policy", &mm->gm_policy);
-		if (ret || mm->gm_policy > AIPU_GM_POLICY_HALF_DIVIDED)
-			mm->gm_policy = AIPU_GM_POLICY_SHARED;
+	ret = device_property_read_u32(mm->dev, "gm-policy", &mm->gm_policy);
+	if (ret || mm->gm_policy > AIPU_GM_POLICY_HALF_DIVIDED)
+		mm->gm_policy = AIPU_GM_POLICY_SHARED;
 
-		if (IS_ERR(aipu_common_create_attr(mm->dev, &mm->gm_policy_attr, "gm_policy", 0444,
-						   aipu_gm_policy_sysfs_show, NULL))) {
-			mm->gm_policy_attr = NULL;
-			dev_err(mm->dev, "create gm_policy attr failed");
-		}
-
-		dev_info(mm->dev, "GM policy is %s",
-			 mm->gm_policy == AIPU_GM_POLICY_SHARED ? "shared" : "half-by-half");
-	} else {
-		mm->gm_policy = AIPU_GM_POLICY_NONE;
+	if (IS_ERR(aipu_common_create_attr(mm->dev, &mm->gm_policy_attr, "gm_policy", 0444,
+					   aipu_gm_policy_sysfs_show, NULL))) {
 		mm->gm_policy_attr = NULL;
+		dev_err(mm->dev, "create gm_policy attr failed");
 	}
 
+	dev_info(mm->dev, "GM policy is %s",
+		 mm->gm_policy == AIPU_GM_POLICY_SHARED ? "shared" : "half-by-half");
+
 	group = iommu_group_get(mm->dev);
 	if (group) {
 		mm->iommu_domain = iommu_get_domain_for_dev(mm->dev);
@@ -1432,7 +1396,7 @@ int aipu_init_mm(struct aipu_memory_manager *mm, struct platform_device *p_dev,
 	mm->res_cnt = ret;
 	ret = 0;
 
-	if (version > AIPU_ISA_VERSION_ZHOUYI_V1 && mm->res_cnt)
+	if (mm->res_cnt)
 		aipu_mm_set_asid_base(mm);
 
 	dev_info(mm->dev, "driver mem management is %s\n", mm->res_cnt ? "enabled" : "disabled");
@@ -1555,33 +1519,28 @@ static bool aipu_mm_check_address_validity(struct aipu_memory_manager *mm,
 	u64 asid0_base = 0;
 	u64 size_4G = 0x100000000ULL;
 
-	if (mm->version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		remainder = desc->pa % size_4G;
-		if ((remainder + desc->bytes) >= size_4G)
-			return false;
-	}
+	remainder = desc->pa % size_4G;
+	if ((remainder + desc->bytes) >= size_4G)
+		return false;
 
 	asid_size = aipu_mm_get_asid_size(mm, desc->asid);
 	if (asid_size <= SZ_2G)
 		return true;
 
 	asid_base = aipu_mm_get_asid_base(mm, desc->asid);
-	if (mm->version == AIPU_ISA_VERSION_ZHOUYI_V3_1 ||
-	    mm->version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		lower_bound = asid_base + size_4G - SZ_1G;
-		upper_bound = asid_base + size_4G;
-
-		if (desc->asid > AIPU_BUF_ASID_0) {
-			asid0_base = aipu_mm_get_asid_base(mm, AIPU_BUF_ASID_0);
-			if (asid0_base != asid_base)
-				lower_bound += SZ_512M;
-		}
+	lower_bound = asid_base + size_4G - SZ_1G;
+	upper_bound = asid_base + size_4G;
 
-		desc_end = desc->pa + desc->bytes;
-		if (desc->pa < upper_bound && desc_end >= lower_bound)
-			return false;
+	if (desc->asid > AIPU_BUF_ASID_0) {
+		asid0_base = aipu_mm_get_asid_base(mm, AIPU_BUF_ASID_0);
+		if (asid0_base != asid_base)
+			lower_bound += SZ_512M;
 	}
 
+	desc_end = desc->pa + desc->bytes;
+	if (desc->pa < upper_bound && desc_end >= lower_bound)
+		return false;
+
 	return true;
 }
 
@@ -1614,9 +1573,6 @@ int aipu_mm_alloc(struct aipu_memory_manager *mm, struct aipu_buf_request *buf_r
 		return -EINVAL;
 	}
 
-	if (mm->version == AIPU_ISA_VERSION_ZHOUYI_V1)
-		buf_req->asid = AIPU_BUF_ASID_0;
-
 	if (buf_req->region < AIPU_MEM_REGION_TYPE_MAX) {
 		type = buf_req->region;
 	} else {
@@ -1625,11 +1581,7 @@ int aipu_mm_alloc(struct aipu_memory_manager *mm, struct aipu_buf_request *buf_r
 		return -EINVAL;
 	}
 
-	/* fall back to SRAM if DTCM/GM is not applicable for certain archs */
-	if (mm->version < AIPU_ISA_VERSION_ZHOUYI_V2_2 && type == AIPU_MEM_REGION_TYPE_DTCM)
-		type = AIPU_MEM_REGION_TYPE_SRAM;
-
-	if ((mm->version < AIPU_ISA_VERSION_ZHOUYI_V3 || mm->gm_policy == AIPU_GM_POLICY_NONE) &&
+	if (mm->gm_policy == AIPU_GM_POLICY_NONE &&
 	    type == AIPU_MEM_REGION_TYPE_GM)
 		type = AIPU_MEM_REGION_TYPE_SRAM;
 
@@ -1684,9 +1636,7 @@ int aipu_mm_alloc(struct aipu_memory_manager *mm, struct aipu_buf_request *buf_r
 		spin_unlock_irqrestore(&mm->slock, flags);
 	}
 
-	if ((mm->version == AIPU_ISA_VERSION_ZHOUYI_V3 ||
-	     mm->version == AIPU_ISA_VERSION_ZHOUYI_V3_1) &&
-	    !mm->has_iommu) {
+	if (!mm->has_iommu) {
 		if (!aipu_mm_check_address_validity(mm, &buf_req->desc)) {
 			aipu_mm_free(mm, &buf_req->desc, filp, true);
 			allocated = false;
diff --git a/drivers/misc/armchina-npu-r2p0/aipu_partition.h b/drivers/misc/armchina-npu-r2p0/aipu_partition.h
--- a/drivers/misc/armchina-npu-r2p0/aipu_partition.h
+++ b/drivers/misc/armchina-npu-r2p0/aipu_partition.h
@@ -105,8 +105,6 @@ struct cluster_info {
  * @dtcm_size:       DTCM size in bytes
  * @cluster_cnt:     cluster count
  * @clusters:        cluster information array
- * @partition_mode:  partition mode in a cluster (for zhouyi v3_1 only)
- * @event_type:      interrupt's event type (for zhouyi v3_1 only)
  */
 struct aipu_partition {
 	u32 id;
@@ -131,8 +129,6 @@ struct aipu_partition {
 	u32 dtcm_size;
 	u32 cluster_cnt;
 	struct cluster_info clusters[8];
-	int partition_mode;
-	int event_type;
 };
 
 /**
diff --git a/drivers/misc/armchina-npu-r2p0/aipu_priv.c b/drivers/misc/armchina-npu-r2p0/aipu_priv.c
--- a/drivers/misc/armchina-npu-r2p0/aipu_priv.c
+++ b/drivers/misc/armchina-npu-r2p0/aipu_priv.c
@@ -5,10 +5,7 @@
 #include <linux/of_address.h>
 #include "aipu_priv.h"
 #include "config.h"
-#include "v1.h"
-#include "v2.h"
 #include "v3.h"
-#include "v3_1.h"
 
 static int init_misc_dev(struct aipu_priv *aipu)
 {
@@ -71,7 +68,6 @@ int init_aipu_priv(struct aipu_priv *aipu, struct platform_device *p_dev,
 {
 	int ret = 0;
 	int version = 0;
-	int config = 0;
 	int revision = 0;
 
 	if (!aipu || !p_dev || !fops)
@@ -104,27 +100,13 @@ int init_aipu_priv(struct aipu_priv *aipu, struct platform_device *p_dev,
 	aipu->reg.size = 0;
 	aipu->ops = NULL;
 
-	zhouyi_detect_aipu_version(p_dev, &version, &config, &revision);
-	dev_dbg(aipu->dev, "AIPU core0 ISA version %d, configuration %d\n", version, config);
+	zhouyi_detect_aipu_version(p_dev, &version, &revision);
+	dev_dbg(aipu->dev, "AIPU core0 ISA version %d\n", version);
 	aipu->version = version;
 	aipu->revision = revision;
 
-#ifdef CONFIG_ARMCHINA_NPU_ARCH_V3_1
-	if (version == AIPU_ISA_VERSION_ZHOUYI_V3_1) {
-		aipu->ops = get_v3_1_priv_ops();
-		aipu->core_reset_delay_us = AIPU_CONFIG_CORE_RESET_DELAY_US;
-	}
-#endif
-
-#ifdef CONFIG_ARMCHINA_NPU_ARCH_V3
 	if (version == AIPU_ISA_VERSION_ZHOUYI_V3)
 		aipu->ops = get_v3_priv_ops();
-#endif
-
-#if (defined CONFIG_ARMCHINA_NPU_ARCH_V1) || (defined CONFIG_ARMCHINA_NPU_ARCH_V2)
-	if (version > 0 && version <= AIPU_ISA_VERSION_ZHOUYI_V2_2)
-		aipu->ops = get_v1v2_priv_ops();
-#endif
 
 	if (!aipu->ops) {
 		ret = -EINVAL;
@@ -236,8 +218,7 @@ int aipu_priv_get_version(struct aipu_priv *aipu)
 
 /**
  * @aipu_priv_get_partition_cnt() - get AIPU partition count
- *        For V1/V2, a *partition* represents an AIPU core
- *        For V3, a *partition* represents a group of AIPU clusters in the same power domain
+ *        For V3, a partition represents AIPU clusters in one power domain
  * @aipu: pointer to the aipu private struct initialized in init_aipu_priv()
  *
  * Return AIPU partition count
diff --git a/drivers/misc/armchina-npu-r2p0/aipu_priv.h b/drivers/misc/armchina-npu-r2p0/aipu_priv.h
--- a/drivers/misc/armchina-npu-r2p0/aipu_priv.h
+++ b/drivers/misc/armchina-npu-r2p0/aipu_priv.h
@@ -42,7 +42,7 @@ struct aipu_priv_operations {
  * @partitions:    partition/core array
  * @reg:           AIPU register base (v3 only)
  * @irq_obj:       interrupt object (v3 only)
- * @dev:           device struct pointer (of core 0 for v1/v2)
+ * @dev:           device struct pointer
  * @soc:           SoC private data
  * @soc_ops:       SoC operation pointer
  * @aipu_fops:     file operation struct
diff --git a/drivers/misc/armchina-npu-r2p0/zhouyi/v1.h b/drivers/misc/armchina-npu-r2p0/zhouyi/v1.h
deleted file mode 100644
--- a/drivers/misc/armchina-npu-r2p0/zhouyi/v1.h
+++ /dev/null
@@ -1,34 +0,0 @@
-/* SPDX-License-Identifier: GPL-2.0 */
-/* Copyright (c) 2023-2024 Arm Technology (China) Co. Ltd. */
-
-#ifndef __V1_H__
-#define __V1_H__
-
-#include "zhouyi.h"
-
-/*
- * Zhouyi V1 AIPU Interrupts
- */
-#define ZHOUYI_V1_IRQ              (ZHOUYI_IRQ)
-#define ZHOUYI_V1_IRQ_ENABLE_FLAG  (ZHOUYI_V1_IRQ)
-#define ZHOUYI_V1_IRQ_DISABLE_FLAG (ZHOUYI_IRQ_NONE)
-
-#define ZHOUYI_V1_MAX_SCHED_JOB_NUM  1
-
-/*
- * Zhouyi V1 AIPU Specific Host Control Register Map
- */
-#define ZHOUYI_INTR_CAUSE_REG_OFFSET          0x20
-#define ZHOUYI_INTR_STAT_REG_OFFSET           0x24
-#define ZHOUYI_INTR_BACKUP_STAT_REG_OFFSET    0x28
-#define ZHOUYI_INTR_BACKUP_PC_REG_OFFSET      0x2C
-#define ZHOUYI_DBG_ERR_CAUSE_REG_OFFSET       0x30
-#define ZHOUYI_DBG_DATA_REG_0_OFFSET          0x34
-#define ZHOUYI_DBG_DATA_REG_1_OFFSET          0x38
-#define ZHOUYI_L2_CACHE_FEATURE_REG_OFFSET    0x6C
-#define ZHOUYI_V1_MAX_REG_OFFSET              0x6C
-
-struct aipu_priv_operations *get_v1v2_priv_ops(void);
-struct aipu_operations *get_zhouyi_v1_ops(void);
-
-#endif /* __V1_H__ */
diff --git a/drivers/misc/armchina-npu-r2p0/zhouyi/v2.h b/drivers/misc/armchina-npu-r2p0/zhouyi/v2.h
deleted file mode 100644
--- a/drivers/misc/armchina-npu-r2p0/zhouyi/v2.h
+++ /dev/null
@@ -1,54 +0,0 @@
-/* SPDX-License-Identifier: GPL-2.0 */
-/* Copyright (c) 2023-2024 Arm Technology (China) Co. Ltd. */
-
-#ifndef __V2_H__
-#define __V2_H__
-
-#include "zhouyi.h"
-
-/*
- * Zhouyi V2 AIPU Specific Interrupts/Configs
- */
-#define ZHOUYI_IRQ_FAULT                    0x8
-
-#define ZHOUYI_V2_IRQ                       (ZHOUYI_IRQ | ZHOUYI_IRQ_FAULT)
-#define ZHOUYI_V2_IRQ_ENABLE_FLAG           (ZHOUYI_V2_IRQ)
-#define ZHOUYI_V2_IRQ_DISABLE_FLAG          (ZHOUYI_IRQ_NONE)
-
-#define ZHOUYI_V2_MAX_SCHED_JOB_NUM         1
-
-#define ZHOUYI_V2_ASE_READ_ENABLE           BIT(31)
-#define ZHOUYI_V2_ASE_WRITE_ENABLE          BIT(30)
-#define ZHOUYI_V2_ASE_RW_ENABLE             (ZHOUYI_V2_ASE_READ_ENABLE | ZHOUYI_V2_ASE_WRITE_ENABLE)
-#define ZHOUYI_V2_DTCM_ENABLE               BIT(16)
-#define ZHOUYI_V2_DTCM_MAX_BYTES            (32 * SZ_1M) /* 32MB at maximum */
-
-/*
- * Zhouyi V2 AIPU Specific Host Control Register Map
- */
-#define AIPU_DATA_ADDR_2_REG_OFFSET         0x1C
-#define AIPU_DATA_ADDR_3_REG_OFFSET         0x20
-#define AIPU_SECURE_CONFIG_REG_OFFSET       0x30
-#define AIPU_POWER_CTRL_REG_OFFSET          0x38
-#define AIPU_ADDR_EXT0_CTRL_REG_OFFSET      0xC0
-#define AIPU_ADDR_EXT0_HIGH_BASE_REG_OFFSET 0xC4
-#define AIPU_ADDR_EXT0_LOW_BASE_REG_OFFSET  0xC8
-#define AIPU_ADDR_EXT1_CTRL_REG_OFFSET      0xCC
-#define AIPU_ADDR_EXT1_HIGH_BASE_REG_OFFSET 0xD0
-#define AIPU_ADDR_EXT1_LOW_BASE_REG_OFFSET  0xD4
-#define AIPU_ADDR_EXT2_CTRL_REG_OFFSET      0xD8
-#define AIPU_ADDR_EXT2_HIGH_BASE_REG_OFFSET 0xDC
-#define AIPU_ADDR_EXT2_LOW_BASE_REG_OFFSET  0xE0
-#define AIPU_ADDR_EXT3_CTRL_REG_OFFSET      0xE4
-#define AIPU_ADDR_EXT3_HIGH_BASE_REG_OFFSET 0xE8
-#define AIPU_ADDR_EXT3_LOW_BASE_REG_OFFSET  0xEC
-#define ZHOUYI_V2_DTCM_CTRL                 0x180
-#define ZHOUYI_V2_DTCM_HIGH_BASE_ADDR       0x184
-#define ZHOUYI_V2_DTCM_LOW_BASE_ADDR        0x188
-#define ZHOUYI_V2_SOFT_RESET_OFFSET         0x200
-#define ZHOUYI_V2_MAX_REG_OFFSET            (ZHOUYI_V2_SOFT_RESET_OFFSET)
-
-struct aipu_priv_operations *get_v1v2_priv_ops(void);
-struct aipu_operations *get_zhouyi_v2_ops(void);
-
-#endif /* __V2_H__ */
diff --git a/drivers/misc/armchina-npu-r2p0/zhouyi/v3_1.h b/drivers/misc/armchina-npu-r2p0/zhouyi/v3_1.h
deleted file mode 100644
--- a/drivers/misc/armchina-npu-r2p0/zhouyi/v3_1.h
+++ /dev/null
@@ -1,456 +0,0 @@
-/* SPDX-License-Identifier: GPL-2.0 */
-/* Copyright (c) 2023-2024 Arm Technology (China) Co. Ltd. */
-
-#ifndef __V3_1_H__
-#define __V3_1_H__
-
-/**************************************************************************************
- *                             Cluster [N] Registers
- *************************************************************************************/
-/**
- * Cluster Range Base Address
- *
- * 0xC00 ~ 0xC1F    Cluster 0
- * 0xC20 ~ 0xC3F    Cluster 1
- * 0xC40 ~ 0xC5F    Cluster 2
- * ...
- * 0xCE0 ~ 0xCFF    Cluster 7
- */
-#define _GET_CLUSTER_OFFSET_V3_1(id)                     (0xC00 + 0x20 * (id))
-#define MAX_CLUSTER_NUM_V3_1                             8
-
-/**
- * A1.1.1 Cluster Configuration Register (Read-Only)
- * [31-13]  Reserved
- * [12]     set if cluster is present
- * [11:8]   number of available AIPU cores
- * [7:4]    number of available AIFF in every core
- * [3:0]    number of available TEC in every core
- */
-#define IS_CLUSTER_PRESENT_V3_1(config_32)               (((config_32) >> 12) & 0x1)
-#define GET_AIPU_CORE_NUM_V3_1(config_32)                (((config_32) >> 8) & 0xF)
-#define GET_AIFF_NUM_V3_1(config_32)                     (((config_32) >> 4) & 0xF)
-#define GET_TEC_NUM_V3_1(config_32)                      ((config_32) & 0xF)
-#define GET_NUMS_V3_1(config_32)                         ((config_32) & 0xFFF)
-#define CLUSTER_CONFIG_REG_V3_1(id)                      (_GET_CLUSTER_OFFSET_V3_1(id) + 0x0)
-
-/*
- * A1.1.2 Cluster Control Register
- * [31:24] core enable bits (27:24: core 3 - core 0; )
- * [23:13] reserved (RO)
- * [12]    cluster enable (RO)
- * [11:8]  Core enable number (RO)
- * [7:4]   AIFF enable number (RO)
- * [3:0]   TEC enable number (RO)
- * [3:0] number of TEC enabled in every core (RO)
- */
-#define _ENABLE_CORE_V3_1(core_en)                      ((core_en) << 24)
-#define _ENABLE_CLUSTER_V3_1                             (0x1 << 12)
-#define _DISABLE_CLUSTER_V3_1                            (0x0 << 12)
-#define _EN_CORE_NUM_V3_1(num)                           (((num) & 0xF) << 8)
-#define _EN_AIFF_NUM_V3_1(num)                           (((num) & 0xF) << 4)
-#define _EN_TEC_NUM_V3_1(num)                            ((num) & 0xF)
-#define EN_NUMS_V3_1(nums)                               ((nums) & 0xFFF)
-#define ENABLE_CLUSTER_V3_1(partition, nums) \
-	(EN_NUMS_V3_1(nums) | _ENABLE_CLUSTER_V3_1)
-#define DISABLE_CLUSTER_V3_1                             _DISABLE_CLUSTER_V3_1
-#define CONFIG_CLUSTER_V3_1(core_en, en_core_num, en_aiff, en_tec, par_mode) \
-	(_ENABLE_CORE_V3_1(core_en) | \
-	 _EN_CORE_NUM_V3_1(en_core_num) | \
-	 _EN_AIFF_NUM_V3_1(en_aiff) | \
-	 _EN_TEC_NUM_V3_1(en_tec) | \
-	 _ENABLE_CLUSTER_V3_1 | (par_mode))
-
-#define CLUSTER_CONTROL_REG_V3_1(id)                     (_GET_CLUSTER_OFFSET_V3_1(id) + 0x4)
-
-/*****************************************************************************
- *                             TSM Registers
- *****************************************************************************/
-/**
- * TSM Command Schedule Control Handle Register
- *
- * [31:28] Write to select a cluster for direct dispatching
- * [27:24] Write to select a core for direct dispatching
- * [23:20] Reserved (RO)
- * [19]    Command pool type
- *         0: PCP
- * [18:16] Write to select a command pool
- *         0 command pool 0 sellected
- * [15:13] Reserved (RO)
- * [12]    0 to map one cluster to this command pool (RO)
- *         1) Only works for command pool creation
- *         2) R/O for single cluster TSM
- * [11:10] Reserved (RO)
- * [9:8]   QOS 0 slow, 1 Media, 2 fast (reserved in LIN), 3 reserved
- * [7:4]   Reserved (RO)
- * [3:0]   command tpye:0 none, 1 create, 2 destroy, 3 Abortion,
- *                      4 Dispatch,5 direct dispatch, 6 Others.
- */
-#define _TSM_DIRECT_CLUSTER_V3_1(cluser)                  ((cluster) << 28)
-#define _TSM_DIRECT_CORE_V3_1(core)                       ((core) << 24)
-#define _TSM_POOL_V3_1(pool)                              ((pool) << 16)
-#define _TSM_MAP_V3_1(map)                                ((map) << 12)
-#define _TSM_QOS_V3_1(qos)                                ((qos) << 8)
-#define _TSM_CREATE_V3_1                                  0x1
-#define _TSM_DESTROY_V3_1                                 0x2
-#define _TSM_ABORT_V3_1                                   0x3
-#define _TSM_DISPATCH_V3_1                                0x4
-#define _TSM_DEBUG_DISPATCH_V3_1                          0x5
-
-#define TSM_MAP_SINGLE_V3_1                              _TSM_MAP_V3_1(0x0)
-#define TSM_MAP_ALL_V3_1                                 _TSM_MAP_V3_1(0x1)
-#define TSM_CREATE_CMD_POOL_V3_1(pool, map)              (_TSM_CREATE_V3_1 | _TSM_POOL_V3_1(pool) | (map))
-#define TSM_DESTROY_CMD_POOL_V3_1(pool)                  (_TSM_DESTROY_V3_1 | _TSM_POOL_V3_1(pool))
-#define TSM_ABORT_CMD_POOL_V3_1(pool)                    (_TSM_ABORT_V3_1 | _TSM_POOL_V3_1(pool))
-#define TSM_DBG_DISPATCH_CMD_POOL_V3_1(pool, qos, core)  (_TSM_DEBUG_DISPATCH_V3_1 | (qos) | \
-						     _TSM_POOL_V3_1(pool) | _TSM_DIRECT_CORE_V3_1(core))
-
-#define TSM_CMD_SCHD_CTRL_HANDLE_REG_V3_1             0x0
-#define TSM_DISPATCH_CMD_POOL_V3_1(pool, qos)         (_TSM_DISPATCH_V3_1 | (qos) | (pool))
-
-/**
- * A1.1.6 TSM Command Schedule Address High Register
- *
- * [31:0] Write to set the address[63:32] of head of a TCB chain to be dispatched
- */
-#define TSM_CMD_SCHD_ADDR_HIGH_REG_V3_1               0x8
-
-/**
- * A1.1.7 TSM Command Schedule Address Low Register
- *
- * [31:12] Write to set the address[31:12] of head of a TCB chain to
- *         be dispatched
- *         1) this address shall be 4k page aligned
- */
-#define TSM_CMD_SCHD_ADDR_LOW_REG_V3_1                0xC
-
-/**
- * A1.1.9 TSM Configuration Register
- * [31:4] Reserved (RO)
- * [3:0]  TSM scheduling policy
- */
-#define TSM_CONFIG_REG_V3_1                           0x10
-
-/**
- * TSM Build Info Register (Read-Only)
- *
- * [25:24] Partition mode: 0
- * [19:16] Supported command pool number: 1/2
- *
- * other fields are the same as v3
- */
-#define TSM_BUILD_INFO_REG_V3_1                       0x14
-#define GET_CMD_POOL_NUM_V3_1(build_info_32)          ((((build_info_32) >> 16) & 0xF) + 1)
-
-/**
- * TSM Status Register
- * [31]    scheduling cmd fail
- * [30:24] Reserved (RO)
- * [23:16] TSM abort command pool done
- * [15:12] Reserved
- * [11:8]  TSM PCP (QoS high) is full
- * [7:4]   Reserved
- * [3:0]   TSM PCP (QoS low) is full
- */
-#define PCP_ABORT_DONE_BIT                          16
-#define IS_ABORT_DONE(status_32, done_bit)          (((status_32) >> (done_bit)) & 0x1)
-#define CLEAR_ABORT(done_bit)                       ((~(1 << (done_bit))) & (0x7FFFFFFF))
-#define IS_PCP_FULL(status, high)                   (((status) >> ((high) ? 8 : 0)) & 0x1)
-#define IS_CMD_FAIL_V3_1(status_32)                   ((status_32) >> 31)
-#define IS_CMD_POOL_FULL_V3_1(status_32)              ((status_32) & 0xFF)
-#define CLEAR_CMD_FAIL_V3_1(status_32)                ((status_32) & 0x7FFFFFFF)
-#define TSM_STATUS_REG_V3_1                           0x18
-
-/**
- * A1.1.8 TSM Command Schedule TCB Number Register
- *
- * [31:0] TCB number of chain
- */
-#define TSM_COMMAND_SCHEDULE_TCB_NUM_REG_V3_1         0x1C
-
-/**************************************************************************************
- *                             Debug Link Access Registers
- *************************************************************************************/
-/**
- * A1.1.28 AHB Internal CSR Selection Control Register
- *
- * [12]   debug enable
- * [11:4] cluster selection
- * [3:0]  core selection
- */
-#define _ENABLE_DEBUG_V3_1                            BIT(12)
-#define _SET_DBG_CLUSTER_V3_1(id)                     (((id) & 0xFF) << 4)
-#define _SET_DBG_CORE_V3_1(id)                        ((id) & 0xF)
-#define SELECT_DEBUG_CORE_V3_1(cluster, core)         (_ENABLE_DEBUG_V3_1 | \
-			_SET_DBG_CLUSTER_V3_1(cluster) | _SET_DBG_CORE_V3_1(core))
-#define DISABLE_DEBUG_V3_1                            0
-#define AHB_INTERNAL_CSR_SELECTION_CTRL_REG         0x1F00
-#define CLUSTER_MMR_WIN_BASE                        (0x2000)
-#define CORE_MMR_WIN_BASE                           (0x3000)
-
-/**
- * A1.1.8 TSM Revision Register (Read-Only)
- *
- * [27:24] implementation ID
- * [23:16] primary part number
- * [15:8]  secondary part number
- * [7:4]   major revision ID
- * [3:0]   minor number
- */
-#define _IS_TSM_ARCH_V3_1(rev_32)                     (((rev_32) >> 16) & 0x1)
-#define _IS_V3_1_ARCH(rev_32)                         ((((rev_32) >> 8) & 0x1))
-
-#define TSM_REVISION_REG_V3_1                         0x50
-
-/**
- * A1.1.22 Tick Counter Control Status Register
- *
- * [8] counter overflow flag
- * [1] clear
- * [0] enable
- */
-#define IS_COUNTER_OVERFLOW_V3_1(status_32)           (((status_32) >> 8) & 0x1)
-#define CLEAR_COUNTER_V3_1                            BIT(1)
-#define ENABLE_COUNTER_V3_1                           0x1
-#define DISABLE_COUNTER_V3_1                          ((CLEAR_COUNTER_V3_1))
-
-#define TICK_COUNTER_CONTROL_STATUS_REG_V3_1          0x68
-
-/**
- * TSM Interrupt Status Register
- *
- * Bit 0 - 15: TSM interrupt 0 - 15
- */
-#define TSM_CLEAR_INTERRUPT_V3_1(i)                   BIT(i)
-#define GET_V3_1_IRQS_BITS(status)                    ((status) & 0xFFFF)
-
-#define TSM_INTERRUPT_STATUS_REG                    0x80
-#define TSM_IRQ_MAX_NUM                             16
-
-/**************************************************************************************
- *                             TSM Command Pool (PCP) Registers
- *************************************************************************************/
-#define _TSM_PCP_REGISTERS_BASE                     0x800
-
-/**************************************************************************************
- *                             HOST TSM PMU Register
- *************************************************************************************/
-/**
- * A1.1.32 PMU TOP Soft Reset
- *
- * [1] soft reset status bit
- *     1) host write 0 to launch AIPU soft reset
- *     2) AIPU write 1 when a soft reset is done
- * [0] soft reset config bit
- *     1) host write 1 to launch AIPU soft reset
- *     2) AIPU write 0 when a soft reset is done
- */
-#define PMU_TOP_SOFT_RESET_REG                      (0x20)
-
-/**
- * A1.1.33 PMU Cluster Soft Reset
- *
- * [7:4] soft reset status bit
- *     1) host write 0 to clear it after reading
- *     2) AIPU write 1 when a soft reset is done
- * [3:0] cluster soft reset config bit
- *     1) host write 1 to launch AIPU soft reset
- *     2) AIPU write 0 when a soft reset is done
- */
-#define CONFIG_PMU_CLUSTER_RESET(cluster)           (1 << ((cluster) & 0xF))
-#define PMU_CLUSTER_RESET_DONE(cluster)			(1 << (((cluster) & 0xF) + 4))
-#define PMU_CLUSTER_RESET_REG                       (0x24)
-
-/**
- * A1.1.34 PMU Core Soft Reset
- *
- * [31:16] soft reset status bit
- *     1) host write 0 to clear it after reading
- *     2) AIPU write 1 when a soft reset is done
- * [15:0] cluster soft reset config bit
- *     1) host write 1 to launch AIPU soft reset
- *     2) AIPU write 0 when a soft reset is done
- */
-#define CONFIG_PMU_CORE_RESET(cluster, core)	(1 << (((core) & 0xFF) + \
-								((cluster) & 0xF) * 4))
-#define PMU_CORE_RESET_DONE(cluster, core)		(1 << (((core) & 0xFF) + \
-								(((cluster) & 0xF) * 4) + 16))
-#define PMU_CORE_RESET_REG                          (0x28)
-
-/**
- * A1.1.35 PMU Reset Timeout
- *
- * [31:16] core reset timeout flag bit
- *     1) host write 0 to clear it after reading
- *     2) AIPU write 1 when there is reset request.
- * [3:0] Cluster reset timeout flag
- *     1) host write 0 to clear it after reading
- *     2) AIPU write 1 when there is reset request.
- */
-#define PMU_RESET_TIMEOUT_REG                       (0x2c)
-
-/**
- * A1.1.36 PMU Top Clock and Power Control Register
- *
- * [31:3] reserved
- * [2]    secure/non-secure mode (0/1)
- * [1]    clock gating disabled/enabled (0/1)
- * [0]    PMU disabled/enabled (0/1)
- */
-#define PMU_SECURE_MODE                             BIT(2)
-#define PMU_NONSECURE_MODE                          0
-#define PMU_ENABLE_CLOCK_GATING                     BIT(1)
-#define PMU_DISABLE_CLOCK_GATING                    0
-#define PMU_ENABLE                                  BIT(0)
-#define PMU_DISABLE                                 0
-#define PMU_TOP_CLOCK_POWER_CTRL_REG                0x30
-
-/**
- * TSM Command Pool [N] Status PCP Register
- *
- * [31:24]: cluster physical ID
- * [6]: idle (0: busy; 1: idle)
- * [5]: timeout (0: none; 1: MIF timeout)
- * [4]: error (0: none; 1: pool error)
- * [0]: done (0: none; 1: pool done)
- */
-#define IS_POOL_DONE(val)                           ((val) & 0x1)
-#define IS_POOL_ERROR(val)                          (((val) >> 4) & 0x1)
-#define IS_POOL_TIMEOUT(val)                        (((val) >> 5) & 0x1)
-#define IS_POOL_BUSY(val)                           (!(((val) >> 6) & 0x1))
-#define IS_POOL_CID(val)                            ((val) >> 24)
-#define CLEAR_POOL_DONE                             BIT(0)
-
-#define COMMAND_POOL_PCP_STATUS_REG                 (_TSM_PCP_REGISTERS_BASE + 0x4)
-
-/**
- * A1.1.20 TSM Command Pool [N] Interrupt Control PCP Register
- * [31:12] Reserved (RO)
- * [11] command pool interrupt enable
- * [10] cluster interrupt enable
- * [9]  core interrupt enable
- * [8]  tec interrupt enable
- * [6]  timeout interrupt enable
- * [5]  signal interrupt enable
- * [4]  error interrupt enable
- * [3]  fault interrupt enable
- * [2]  exception interrupt enable
- * [0]  done interrupt enable
- */
-#define EN_CMD_POOL_INTR_V3_1                         BIT(11)
-#define EN_CLUSTER_INTR_V3_1                          BIT(10)
-#define EN_CORE_INTR_V3_1                             BIT(9)
-#define EN_TEC_INTR_V3_1                              BIT(8)
-#define EN_TIMEOUT_INTR_V3_1                          BIT(5)
-#define EN_ERROR_INTR_V3_1                            BIT(4)
-#define EN_FAULT_INTR_V3_1                            BIT(3)
-#define EN_EXCEPTION_INTR_V3_1                        BIT(2)
-#define EN_SIGNAL_INTR_V3_1                           BIT(1)
-#define EN_DONE_INTR_V3_1                             BIT(0)
-
-#define EN_ALL_TYPE_INTRS_V3_1 \
-						(EN_SIGNAL_INTR_V3_1 | EN_ERROR_INTR_V3_1 | \
-						EN_FAULT_INTR_V3_1 | EN_EXCEPTION_INTR_V3_1 | \
-						EN_DONE_INTR_V3_1 | EN_TIMEOUT_INTR_V3_1)
-
-#define EN_ALL_LEVEL_INTRS_V3_1 \
-						(EN_CMD_POOL_INTR_V3_1 | EN_CLUSTER_INTR_V3_1 | \
-						 EN_CORE_INTR_V3_1 | EN_TEC_INTR_V3_1)
-#define EN_CMD_POOL_ALL_INTRS_V3_1  (EN_CMD_POOL_INTR_V3_1 | EN_ALL_TYPE_INTRS_V3_1)
-#define EN_ALL_INTRS_V3_1           (EN_ALL_LEVEL_INTRS_V3_1 | EN_ALL_TYPE_INTRS_V3_1)
-#define DISABLE_ALL_INTRS_V3_1                           0
-
-#define COMMAND_POOL_PCP_INTERRUPT_CONTROL_REG      (_TSM_PCP_REGISTERS_BASE + 0x8)
-
-/**************************************************************************************
- *                             Interrupt Registers (Per Interrupt)
- *************************************************************************************/
-/**
- * Per interrupt registers
- *
- * 0xA00: interrupt 0
- * 0xA10: interrupt 1
- * 0xA20: interrupt 2
- * ...
- * 0xAf0: interrupt 15
- */
-
-#define _GET_PER_INTERRUPT_REGISTER_OFFSET(id)      (0xA00 + 0x10 * (id))
-
-/**
- * A1.1.14 TSM Interrupt Type Info Register [N]
- * [31:28] cmd pool ID
- * [27:24] Cluster physical ID
- * [23:20] core physical ID
- * [19:16] TEC physical ID
- * [11]    pool interrupt status
- * [10]    cluster interrupt status
- * [9]     core interrupt status
- * [8]     TEC interrupt status
- * [5]     timeout bit
- * [4]     pool error bit
- * [3]     fault bit
- * [2]     exception bit
- * [1]     irq_signal bit
- * [0]     done bit
- */
-#define IS_RESET(status_32)                         (((status_32) >> 3) & 0x7)
-#define GET_INTR_CLUSTER_ID_V3_1(status_32)           (((status_32) >> 24) & 0xFF)
-#define GET_INTR_CORE_ID_V3_1(status_32)              (((status_32) >> 20) & 0xF)
-#define GET_INTR_TEC_ID_V3_1(status_32)               (((status_32) >> 16) & 0xF)
-#define IS_POOL_IRQ_V3_1(status_32)                   (((status_32) >> 11) & 0x1)
-#define IS_CLUSTER_IRQ_V3_1(status_32)                (((status_32) >> 10) & 0x1)
-#define IS_CORE_IRQ_V3_1(status_32)                   (((status_32) >> 9) & 0x1)
-#define IS_TEC_IRQ_V3_1(status_32)                    (((status_32) >> 8) & 0x1)
-#define IS_TIMEOUT_IRQ_V3_1(status_32)                (((status_32) >> 5) & 0x1)
-#define IS_ERROR_IRQ_V3_1(status_32)                  (((status_32) >> 4) & 0x1)
-#define IS_FAULT_IRQ_V3_1(status_32)                  (((status_32) >> 3) & 0x1)
-#define IS_EXCEPTION_IRQ_V3_1(status_32)              (((status_32) >> 2) & 0x1)
-#define IS_SIGNAL_IRQ_V3_1(status_32)                 (((status_32) >> 1) & 0x1)
-#define IS_DONE_IRQ_V3_1(status_32)                   ((status_32) & 0x1)
-#define IS_ABNORMAL_V3_1(status_32)                   (((status_32) >> 2) & 0xF)
-#define IS_SERIOUS_ERR_V3_1(status_32)                (((status_32) >> 3) & 0x7)
-#define GET_INTR_TYPE_V3_1(status_32)                 ((status_32) & 0x3F)
-#define IS_IRQ_TO_HANDLE_V3_1(status_32)              ((status_32) & 0xF1F)
-
-#define INTERRUPT_TYPE_INFO_REG(id)                 (_GET_PER_INTERRUPT_REGISTER_OFFSET(id) + 0x0)
-
-/**
- * TSM Interrupt TCB Pointer Register [N]
- *
- * [31:0] TCBP (RO)
- */
-#define INTERRUPT_TCB_PTR_REG(id)                   (_GET_PER_INTERRUPT_REGISTER_OFFSET(id) + 0x4)
-
-/**
- * TSM Interrupt Signal Register [N]
- *
- * [31:15]   reserved
- * [14:0]    irq signal id
- */
-#define INTERRUPT_SIGNAL_REG(id)                    (_GET_PER_INTERRUPT_REGISTER_OFFSET(id) + 0x8)
-
-/**
- * TSM Interrupt Signal flag Register [N]
- *
- * [31:0]    irq signal flag
- */
-#define IS_HW_EXCEPTION_SIGNAL_V3_1(flag)             ((flag) >> 31)
-#define IS_SW_EXCEPTION_SIGNAL_V3_1(flag)             (((flag) >> 30) & 0x1)
-#define IS_EXCEPTION_SIGNAL_V3_1(flag)                ((flag) >> 30)
-#define IS_PRINTF_SIGNAL_V3_1(flag)                   (((flag) >> 29) & 0x1)
-#define IS_PROFILER_SIGNAL_V3_1(flag)                 (((flag) >> 28) & 0x1)
-#define IS_COREDUMP_SIGNAL_V3_1(flag)                 (((flag) >> 27) & 0x1)
-#define GET_ERR_CODE_V3_1(flag)                       ((flag) & 0xFFFF)
-#define GET_PRINF_SIZE_V3_1(flag)                     ((flag) & 0xFFFF)
-#define GET_PROFILER_BUF_PA_V3_1(flag)                (((flag) & 0xFFFFF) << 12)
-
-#define INTERRUPT_SIGNAL_FLAG_REG(id)               (_GET_PER_INTERRUPT_REGISTER_OFFSET(id) + 0xc)
-
-#define ZHOUYI_V3_1_MAX_REG_OFFSET                    0x322C /* to be updated */
-
-u64 get_gm_size(u32 val);
-struct aipu_operations *get_zhouyi_v3_1_ops(void);
-struct aipu_priv_operations *get_v3_1_priv_ops(void);
-
-#endif /* __V3_1_H__ */
diff --git a/drivers/misc/armchina-npu-r2p0/zhouyi/v3_priv.c b/drivers/misc/armchina-npu-r2p0/zhouyi/v3_priv.c
--- a/drivers/misc/armchina-npu-r2p0/zhouyi/v3_priv.c
+++ b/drivers/misc/armchina-npu-r2p0/zhouyi/v3_priv.c
@@ -74,7 +74,6 @@ static struct aipu_partition *v3_create_partitions(struct aipu_priv *aipu,
 {
 	int ret = 0;
 	int version = 0;
-	int config = 0;
 	struct aipu_partition *partitions = NULL;
 	u32 *cluster_arr = NULL;
 	u32 cluster_cnt = 0;
@@ -88,7 +87,7 @@ static struct aipu_partition *v3_create_partitions(struct aipu_priv *aipu,
 	if (!aipu || !p_dev)
 		return ERR_PTR(-EINVAL);
 
-	zhouyi_detect_aipu_version(p_dev, &version, &config, NULL);
+	zhouyi_detect_aipu_version(p_dev, &version, NULL);
 	dev_info(&p_dev->dev, "AIPU detected: zhouyi-v3\n");
 
 	WARN_ON(!aipu->is_init);
diff --git a/drivers/misc/armchina-npu-r2p0/zhouyi/zhouyi.c b/drivers/misc/armchina-npu-r2p0/zhouyi/zhouyi.c
--- a/drivers/misc/armchina-npu-r2p0/zhouyi/zhouyi.c
+++ b/drivers/misc/armchina-npu-r2p0/zhouyi/zhouyi.c
@@ -36,13 +36,13 @@ void zhouyi_io_rw(struct io_region *io, struct aipu_io_req *io_req)
 		aipu_write32(io, io_req->offset, io_req->value);
 }
 
-int zhouyi_detect_aipu_version(struct platform_device *p_dev, int *version, int *config, int *rev)
+int zhouyi_detect_aipu_version(struct platform_device *p_dev, int *version, int *rev)
 {
 	struct resource *res = NULL;
 	struct io_region io;
 
 	/* rev == NULL is allowed */
-	if (!p_dev || !version || !config)
+	if (!p_dev || !version)
 		return -EINVAL;
 
 	res = platform_get_resource(p_dev, IORESOURCE_MEM, 0);
@@ -57,10 +57,6 @@ int zhouyi_detect_aipu_version(struct platform_device *p_dev, int *version, int
 	}
 
 	*version = zhouyi_get_hw_version_number(&io, rev);
-	if (*version > 0 && *version < AIPU_ISA_VERSION_ZHOUYI_V3)
-		*config = zhouyi_get_hw_config_number(&io);
-	else
-		*config = 0;
 
 	deinit_aipu_ioregion(&io);
 	return 0;
@@ -117,50 +113,15 @@ int zhouyi_get_hw_version_number(struct io_region *io, int *rev)
 		*rev = revision_id;
 
 	switch (revision_id) {
-	case ZHOUYI_V1_REVISION_ID:
-		return AIPU_ISA_VERSION_ZHOUYI_V1;
-	case ZHOUYI_V2_0_REVISION_ID:
-		return AIPU_ISA_VERSION_ZHOUYI_V2_0;
-	case ZHOUYI_V2_1_REVISION_ID:
-		return AIPU_ISA_VERSION_ZHOUYI_V2_1;
-	case ZHOUYI_V2_2_REVISION_ID:
-		return AIPU_ISA_VERSION_ZHOUYI_V2_2;
 	case ZHOUYI_V3_REVISION_ID_R0P2:
 		return AIPU_ISA_VERSION_ZHOUYI_V3;
 	case ZHOUYI_V3_REVISION_ID_R0P3:
 		return AIPU_ISA_VERSION_ZHOUYI_V3;
-	case ZHOUYI_V3_1_REVISION_ID_R0P0:
-		return AIPU_ISA_VERSION_ZHOUYI_V3_1;
 	}
 
 	return 0;
 }
 
-int zhouyi_get_hw_config_number(struct io_region *io)
-{
-	int high = 0;
-	int low = 0;
-	int isa_version = 0;
-	int aiff_feature = 0;
-	int tpc_feature = 0;
-
-	if (!io)
-		return 0;
-
-	isa_version = aipu_read32(io, ZHOUYI_ISA_VERSION_REG_OFFSET);
-	aiff_feature = aipu_read32(io, ZHOUYI_HWA_FEATURE_REG_OFFSET);
-	tpc_feature = aipu_read32(io, ZHOUYI_TPC_FEATURE_REG_OFFSET);
-
-	if (isa_version == 0)
-		high = (aiff_feature & 0xF) + 6;
-	else if (isa_version == 1)
-		high = (aiff_feature & 0xF) + 8;
-
-	low = (tpc_feature) & 0x1F;
-
-	return high * 100 + low;
-}
-
 int zhouyi_soft_reset(struct io_region *io, int offset, int delay_us)
 {
 	int ret = 0;
diff --git a/drivers/misc/armchina-npu-r2p0/zhouyi/zhouyi.h b/drivers/misc/armchina-npu-r2p0/zhouyi/zhouyi.h
--- a/drivers/misc/armchina-npu-r2p0/zhouyi/zhouyi.h
+++ b/drivers/misc/armchina-npu-r2p0/zhouyi/zhouyi.h
@@ -24,16 +24,9 @@
 
 #define ZHOUYI_ASID_COUNT                     32
 
-/*
- * Revision ID for V1 ~ V3_1
- */
-#define ZHOUYI_V1_REVISION_ID                 0x0
-#define ZHOUYI_V2_0_REVISION_ID               0x100
-#define ZHOUYI_V2_1_REVISION_ID               0x200
-#define ZHOUYI_V2_2_REVISION_ID               0x300
+/* Sky1 V3 revisions. */
 #define ZHOUYI_V3_REVISION_ID_R0P2            0x10000
 #define ZHOUYI_V3_REVISION_ID_R0P3            0x10003
-#define ZHOUYI_V3_1_REVISION_ID_R0P0          0x10100
 
 /*
  * Soft Reset
@@ -52,7 +45,6 @@
  */
 #define ZHOUYI_COMMAND_POOL_DEFAULT           0
 #define ZHOUYI_COMMAND_POOL_PCP               0
-#define ZHOUYI_V3_1_COMMAND_POOL_FULL         0xFFFF
 
 #define PARTITION_MODE_NONE                   0
 
@@ -91,13 +83,12 @@ void zhouyi_clear_qempty_interrupt(struct io_region *io);
 void zhouyi_clear_done_interrupt(struct io_region *io);
 void zhouyi_clear_excep_interrupt(struct io_region *io);
 void zhouyi_io_rw(struct io_region *io, struct aipu_io_req *io_req);
-int zhouyi_detect_aipu_version(struct platform_device *p_dev, int *version, int *config, int *rev);
+int zhouyi_detect_aipu_version(struct platform_device *p_dev, int *version, int *rev);
 #ifdef CONFIG_SYSFS
 int zhouyi_print_reg_info(struct io_region *io, char *buf, const char *name, int offset);
 int zhouyi_sysfs_show(struct io_region *io, char *buf);
 #endif
 int zhouyi_get_hw_version_number(struct io_region *io, int *rev);
-int zhouyi_get_hw_config_number(struct io_region *io);
 int zhouyi_soft_reset(struct io_region *io, int offset, int delay_us);
 int get_qos(u32 exec_flag);
 
diff --git a/drivers/misc/armchina-npu/aipu.c b/drivers/misc/armchina-npu/aipu.c
--- a/drivers/misc/armchina-npu/aipu.c
+++ b/drivers/misc/armchina-npu/aipu.c
@@ -99,21 +99,17 @@ static int aipu_cancel_file_jobs(struct aipu_file_ctx *ctx, struct file *filp)
 	struct aipu_priv *aipu = ctx->aipu;
 	struct aipu_job_manager *manager = &aipu->job_manager;
 	struct aipu_partition *part;
-	bool hardware;
 	int ret;
 
 	ret = aipu_job_manager_cancel_jobs(manager, filp);
-	if (!ret || manager->version < AIPU_ISA_VERSION_ZHOUYI_V3 ||
-	    !manager->partitions || manager->partition_cnt < 1)
+	if (!ret || !manager->partitions || manager->partition_cnt < 1)
 		return ret;
 	if (ret == -EBUSY)
 		return ret;
 
 	/* A failed pool abort leaves the shared engine faulted; reset it globally. */
 	part = &manager->partitions[0];
-	hardware = manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0 &&
-		aipu->reset_gpio && part->ops && part->ops->hw_reset;
-	ret = aipu_job_manager_reset(manager, part, hardware);
+	ret = aipu_job_manager_reset(manager, part, false);
 	if (ret)
 		dev_warn(aipu->dev, "release recovery reset failed: %d\n", ret);
 
@@ -278,12 +274,10 @@ static long aipu_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)
 	struct aipu_config_clusters config_clusters;
 	struct aipu_dma_buf_request dmabuf_req;
 	struct aipu_dma_buf dmabuf_info;
-	struct aipu_id_desc id_desc;
 	struct aipu_cluster_status cluster_status;
 	struct aipu_running_job_query running_job_query;
 	int fd = 0;
 
-	u32 job_id;
 	struct aipu_partition *cls;
 
 	if (aipu_priv_is_removing(aipu))
@@ -386,14 +380,7 @@ static long aipu_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)
 		aipu_job_manager_finish_job_status(manager, &status_snapshot, !ret);
 		break;
 	case AIPU_IOCTL_KILL_TIMEOUT_JOB:
-		if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
-			ret = -EOPNOTSUPP;
-			break;
-		}
-		if (!copy_from_user(&job_id, (u32 __user *)arg, sizeof(job_id)))
-			ret = aipu_job_manager_invalidate_timeout_job(manager, job_id, filp);
-		else
-			ret = -EFAULT;
+		ret = -EOPNOTSUPP;
 		break;
 	case AIPU_IOCTL_REQ_IO:
 		if (!capable(CAP_SYS_RAWIO)) {
@@ -437,11 +424,6 @@ static long aipu_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)
 			ret = -EFAULT;
 		break;
 	case AIPU_IOCTL_ALLOC_DMA_BUF:
-		if (aipu->mm.version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0 &&
-		    aipu->mm.has_iommu) {
-			ret = -EOPNOTSUPP;
-			break;
-		}
 		if (!copy_from_user(&dmabuf_req, (struct aipu_dma_buf_request __user *)arg,
 				    sizeof(dmabuf_req))) {
 			dmabuf_req.exec_id = 0;
@@ -474,11 +456,6 @@ static long aipu_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)
 		}
 		break;
 	case AIPU_IOCTL_ATTACH_DMA_BUF:
-		if (aipu->mm.version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0 &&
-		    aipu->mm.has_iommu) {
-			ret = -EOPNOTSUPP;
-			break;
-		}
 		if (!copy_from_user(&dmabuf_info, (struct aipu_dma_buf __user *)arg,
 				    sizeof(dmabuf_info))) {
 			dmabuf_info.exec_id = 0;
@@ -526,27 +503,8 @@ static long aipu_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)
 		}
 		break;
 	case AIPU_IOCTL_ALLOC_GROUP_ID:
-		if (!copy_from_user(&id_desc, (struct aipu_id_desc __user *)arg,
-				    sizeof(id_desc))) {
-			ret = aipu_job_manager_alloc_group_id(manager, &id_desc,
-						      aipu_file_ctx(filp));
-			if (!ret && copy_to_user((char __user *)arg, &id_desc,
-						 sizeof(id_desc))) {
-				aipu_job_manager_free_group_id(manager, &id_desc,
-						       aipu_file_ctx(filp));
-				ret = -EFAULT;
-			}
-		} else {
-			ret = -EFAULT;
-		}
-		break;
 	case AIPU_IOCTL_FREE_GROUP_ID:
-		if (!copy_from_user(&id_desc, (struct aipu_id_desc __user *)arg,
-				    sizeof(id_desc)))
-			ret = aipu_job_manager_free_group_id(manager, &id_desc,
-						     aipu_file_ctx(filp));
-		else
-			ret = -EFAULT;
+		ret = -EOPNOTSUPP;
 		break;
 	case AIPU_IOCTL_GET_CLUSTER_STATUS:
 		cls = aipu->partitions;
@@ -579,27 +537,8 @@ static long aipu_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)
 		}
 		break;
 	case AIPU_IOCTL_ALLOC_SFLAG_ID:
-		if (!copy_from_user(&id_desc, (struct aipu_id_desc __user *)arg,
-				    sizeof(id_desc))) {
-			ret = aipu_job_manager_alloc_sflag_id(manager, &id_desc,
-						      aipu_file_ctx(filp));
-			if (!ret && copy_to_user((char __user *)arg, &id_desc,
-						 sizeof(id_desc))) {
-				aipu_job_manager_free_sflag_id(manager, &id_desc,
-						       aipu_file_ctx(filp));
-				ret = -EFAULT;
-			}
-		} else {
-			ret = -EFAULT;
-		}
-		break;
 	case AIPU_IOCTL_FREE_SFLAG_ID:
-		if (!copy_from_user(&id_desc, (struct aipu_id_desc __user *)arg,
-				    sizeof(id_desc)))
-			ret = aipu_job_manager_free_sflag_id(manager, &id_desc,
-						     aipu_file_ctx(filp));
-		else
-			ret = -EFAULT;
+		ret = -EOPNOTSUPP;
 		break;
 	case AIPU_IOCTL_REBIND_DMA_BUF:
 	case AIPU_IOCTL_BIND_DMA_BUF:
@@ -783,7 +722,6 @@ int armchina_aipu_probe(struct platform_device *p_dev, struct aipu_soc *soc,
 	goto unlock;
 
 fail_new_priv:
-	/* A later V1/V2 core failing to probe must not remove earlier cores. */
 	if (new_priv) {
 		aipu_priv_begin_remove(priv);
 		aipu_priv_drop_driver_ref_and_wait(priv);
diff --git a/drivers/misc/armchina-npu/aipu_dma_buf.c b/drivers/misc/armchina-npu/aipu_dma_buf.c
--- a/drivers/misc/armchina-npu/aipu_dma_buf.c
+++ b/drivers/misc/armchina-npu/aipu_dma_buf.c
@@ -273,11 +273,6 @@ int aipu_alloc_dma_buf(struct aipu_priv *aipu, struct aipu_dma_buf_request *requ
 		return -ENODEV;
 
 	mm = &aipu->mm;
-	if (mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0 && mm->has_iommu) {
-		ret = -EOPNOTSUPP;
-		goto put_aipu;
-	}
-
 	memset(&inter_req, 0, sizeof(inter_req));
 	inter_req.bytes = request->bytes;
 	inter_req.align_in_page = 1;
@@ -396,8 +391,6 @@ int aipu_attach_dma_buf(struct aipu_memory_manager *mm, struct aipu_dma_buf *dma
 
 	if (!mm || !dmabuf_info || !owner || dmabuf_info->fd < 0)
 		return -EINVAL;
-	if (mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0 && mm->has_iommu)
-		return -EOPNOTSUPP;
 
 	dmabuf = dma_buf_get(dmabuf_info->fd);
 	if (IS_ERR_OR_NULL(dmabuf))
diff --git a/drivers/misc/armchina-npu/aipu_job_manager.c b/drivers/misc/armchina-npu/aipu_job_manager.c
--- a/drivers/misc/armchina-npu/aipu_job_manager.c
+++ b/drivers/misc/armchina-npu/aipu_job_manager.c
@@ -15,7 +15,6 @@
 #include "aipu_partition.h"
 #include "zhouyi.h"
 #include "v3.h"
-#include "v3_2.h"
 
 static void aipu_job_manager_finish_irq_bottom_half(struct aipu_partition *core,
 						     bool process_event,
@@ -23,8 +22,6 @@ static void aipu_job_manager_finish_irq_bottom_half(struct aipu_partition *core,
 
 enum aipu_id_type {
 	AIPU_ID_GRID,
-	AIPU_ID_GROUP,
-	AIPU_ID_SFLAG,
 };
 
 struct aipu_id_allocation {
@@ -35,27 +32,6 @@ struct aipu_id_allocation {
 	struct list_head node;
 };
 
-static bool aipu_owner_has_group_id(struct aipu_job_manager *manager,
-				    struct aipu_file_ctx *owner, u32 id)
-{
-	struct aipu_id_allocation *allocation;
-	bool found = false;
-
-	if (!manager || !owner)
-		return false;
-	mutex_lock(&manager->id_lock);
-	list_for_each_entry(allocation, &manager->id_allocations, node) {
-		if (allocation->type == AIPU_ID_GROUP && allocation->owner == owner &&
-		    id >= allocation->first &&
-		    id - allocation->first < allocation->size) {
-			found = true;
-			break;
-		}
-	}
-	mutex_unlock(&manager->id_lock);
-	return found;
-}
-
 static bool aipu_job_is_hw_active(const struct aipu_job *job)
 {
 	return job->state == AIPU_JOB_STATE_RUNNING ||
@@ -420,16 +396,10 @@ static void delete_job_queue(struct aipu_job_manager *manager, struct aipu_job *
 
 inline bool is_job_version_match(struct aipu_partition *core, struct aipu_job_desc *user_job)
 {
-	if (core->arch == user_job->aipu_arch && user_job->version_compatible)
-		return true;
-
-	if (core->arch != user_job->aipu_arch ||
-	    core->version != user_job->aipu_version)
-		return false;
-
-	/* The configuration number is defined only for Zhouyi v1/v2 jobs. */
-	return core->version >= AIPU_ISA_VERSION_ZHOUYI_V3 ||
-		core->config == user_job->aipu_config;
+	return core->arch == user_job->aipu_arch &&
+		core->version == AIPU_ISA_VERSION_ZHOUYI_V3 &&
+		user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3 &&
+		!user_job->version_compatible;
 }
 
 inline bool is_job_ok_for_core(struct aipu_partition *core, struct aipu_job_desc *user_job)
@@ -450,8 +420,7 @@ inline bool is_job_ok_for_core(struct aipu_partition *core, struct aipu_job_desc
 }
 
 static bool is_user_job_valid(struct aipu_job_manager *manager,
-			      struct aipu_job_desc *user_job,
-			      struct aipu_file_ctx *owner)
+			      struct aipu_job_desc *user_job)
 {
 	const u32 v3_exec_mask = AIPU_JOB_EXEC_FLAG_SRAM_MUTEX |
 		AIPU_JOB_EXEC_FLAG_QOS_SLOW | AIPU_JOB_EXEC_FLAG_QOS_FAST |
@@ -462,10 +431,12 @@ static bool is_user_job_valid(struct aipu_job_manager *manager,
 	u32 core_id = user_job->core_id;
 	int core_cnt = 0;
 	struct aipu_partition *partition = NULL;
-	struct aipu_partition *core = NULL;
 
 	if (!manager->partitions || manager->partition_cnt <= 0)
 		return false;
+	if (manager->version != AIPU_ISA_VERSION_ZHOUYI_V3 ||
+	    user_job->aipu_version != AIPU_ISA_VERSION_ZHOUYI_V3)
+		return false;
 	/* The vendor coredump lifecycle has no safe report-without-reap protocol. */
 	if (user_job->is_coredump_en) {
 		dev_err(manager->dev, "job coredump mode is not safely supported\n");
@@ -480,97 +451,35 @@ static bool is_user_job_valid(struct aipu_job_manager *manager,
 			return false;
 		}
 	}
-	if (user_job->aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3 &&
-	    user_job->is_defer_run)
+	if (user_job->is_defer_run)
+		return false;
+	if (user_job->version_compatible || user_job->exec_flag & ~v3_exec_mask ||
+	    (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_QOS_SLOW &&
+	     user_job->exec_flag & AIPU_JOB_EXEC_FLAG_QOS_FAST) ||
+	    (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_SINGLE_GROUP &&
+	     user_job->exec_flag & AIPU_JOB_EXEC_FLAG_MULTI_GROUP)) {
+		dev_err(manager->dev, "invalid v3 execution flags or compatibility mode\n");
 		return false;
-	if (user_job->aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
-		if (user_job->version_compatible || user_job->exec_flag & ~v3_exec_mask ||
-		    (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_QOS_SLOW &&
-		     user_job->exec_flag & AIPU_JOB_EXEC_FLAG_QOS_FAST) ||
-		    (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_SINGLE_GROUP &&
-		     user_job->exec_flag & AIPU_JOB_EXEC_FLAG_MULTI_GROUP)) {
-			dev_err(manager->dev, "invalid v3 execution flags or compatibility mode\n");
-			return false;
-		}
 	}
-
-	if (user_job->aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-		if (manager->version != user_job->aipu_version ||
-		    partition_id != AIPU_PARTITION_ID_0 ||
-		    partition_id >= (u32)manager->partition_cnt ||
-		    user_job->group_id > U8_MAX) {
-			dev_err(manager->dev, "invalid version number (%d) or partition ID (%u)",
-				user_job->aipu_version, partition_id);
-			return false;
-		}
-		partition = &manager->partitions[partition_id];
-		if (!is_job_ok_for_core(partition, user_job))
-			return false;
-		if (!aipu_owner_has_group_id(manager, owner, user_job->group_id)) {
-			dev_err(manager->dev, "group ID %u is not owned by this file\n",
-				user_job->group_id);
-			return false;
-		}
-		if (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_BIND_DISPATCH) {
-			if (partition->cluster_cnt != 1)
-				return false;
-			core_cnt = atomic_read(&partition->clusters[0].en_core_cnt);
-			if (core_cnt <= 0 || core_id >= (u32)core_cnt)
-				return false;
-		}
-
-		return true;
+	if (partition_id >= (u32)manager->partition_cnt) {
+		dev_err(manager->dev, "invalid partition ID %u", partition_id);
+		return false;
 	}
-
-	/* for v3: partition->clusters->cores arch */
-	if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		if (manager->version != AIPU_ISA_VERSION_ZHOUYI_V3 ||
-		    partition_id >= (u32)manager->partition_cnt) {
-			dev_err(manager->dev, "invalid version number (%d) or partition ID (%u)",
-				user_job->aipu_version, partition_id);
-			return false;
-		}
-		partition = &manager->partitions[partition_id];
-		if (!is_job_ok_for_core(partition, user_job))
+	partition = &manager->partitions[partition_id];
+	if (!is_job_ok_for_core(partition, user_job))
+		return false;
+	if (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_BIND_DISPATCH) {
+		if (partition->cluster_cnt != 1)
 			return false;
-
-		if (user_job->exec_flag & AIPU_JOB_EXEC_FLAG_BIND_DISPATCH) {
-			if (partition->cluster_cnt != 1)
-				return false;
-			core_cnt = atomic_read(&partition->clusters[0].en_core_cnt);
-			if (core_cnt <= 0 || core_id >= (u32)core_cnt) {
-				dev_err(partition->dev, "invalid core ID (%u) >= enabled core count (%d)",
-					core_id, core_cnt);
-				return false;
-			}
-		}
-
-		return true;
-	}
-
-	/* for non-v3 archs, a partition just means an AIPU core */
-	core_cnt = manager->partition_cnt;
-
-	/* debugger deferred execution: reserve the specified core ID */
-	if (user_job->is_defer_run) {
-		if (core_id >= (u32)core_cnt) {
-			dev_err(manager->dev, "invalid core ID (%u) >= core count (%d)",
+		core_cnt = atomic_read(&partition->clusters[0].en_core_cnt);
+		if (core_cnt <= 0 || core_id >= (u32)core_cnt) {
+			dev_err(partition->dev, "invalid core ID (%u) >= enabled core count (%d)",
 				core_id, core_cnt);
 			return false;
 		}
-
-		core = &manager->partitions[core_id];
-		return is_job_ok_for_core(core, user_job);
 	}
 
-	/* for non-v3 normal execution: find an available core ID */
-	for (core_id = 0; core_id < core_cnt; core_id++) {
-		core = &manager->partitions[core_id];
-		if (is_job_ok_for_core(core, user_job))
-			return true;
-	}
-
-	return false;
+	return true;
 }
 
 static int pin_job_tcb_allocation(struct aipu_job_manager *manager,
@@ -580,16 +489,11 @@ static int pin_job_tcb_allocation(struct aipu_job_manager *manager,
 	struct aipu_tcb *first;
 	struct aipu_tcb *head;
 	struct aipu_tcb *last;
-	u64 tcb_count;
 	u64 offset;
 	size_t tcb_bytes;
 	int ret;
 
-	if (desc->aipu_version < AIPU_ISA_VERSION_ZHOUYI_V3)
-		return 0;
-
-	tcb_bytes = desc->aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0 ?
-			sizeof(struct aipu_tcb_v32) : sizeof(struct aipu_tcb);
+	tcb_bytes = sizeof(struct aipu_tcb);
 	if (!desc->head_tcb_pa || desc->head_tcb_pa > desc->first_task_tcb_pa ||
 	    desc->first_task_tcb_pa > desc->last_task_tcb_pa ||
 	    desc->last_task_tcb_pa > desc->tail_tcb_pa ||
@@ -599,16 +503,11 @@ static int pin_job_tcb_allocation(struct aipu_job_manager *manager,
 	    !IS_ALIGNED(desc->last_task_tcb_pa, tcb_bytes) ||
 	    !IS_ALIGNED(desc->tail_tcb_pa, tcb_bytes))
 		return -EINVAL;
-	tcb_count = (desc->tail_tcb_pa - desc->head_tcb_pa) / tcb_bytes + 1;
-	if (desc->aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0 &&
-	    tcb_count > U32_MAX)
-		return -E2BIG;
-
 	desc->asid0_base = manager->asid0_base;
 	ret = aipu_mm_pin_job_tcb(manager->mm, filp, desc->head_tcb_pa,
 				  desc->tail_tcb_pa, tcb_bytes,
 				  &job->tcb_mapping);
-	if (ret || desc->aipu_version != AIPU_ISA_VERSION_ZHOUYI_V3)
+	if (ret)
 		return ret;
 
 	/* Validate the documented V3 envelope after pinning its owner's allocation. */
@@ -628,65 +527,6 @@ static int pin_job_tcb_allocation(struct aipu_job_manager *manager,
 	return 0;
 }
 
-static int get_available_core_no_lock(struct aipu_job_manager *manager, struct aipu_job *job)
-{
-	int id = 0;
-	struct aipu_partition *core = NULL;
-
-	if (unlikely(!manager))
-		return -1;
-
-	for (id = 0; id < manager->partition_cnt; id++) {
-		core = &manager->partitions[id];
-		if (!atomic_read(&core->disable) && manager->idle_bmap[id] &&
-		    !aipu_partition_is_faulted_no_lock(manager, id) &&
-		    is_job_ok_for_core(core, &job->desc))
-			return id;
-	}
-
-	return -1;
-}
-
-static int reserve_core_for_job_no_lock(struct aipu_job_manager *manager, struct aipu_job *job,
-					int do_trigger)
-{
-	struct aipu_partition *sched_core = NULL;
-	int ret = 0;
-
-	if (job->core_id < 0 || job->core_id >= manager->partition_cnt)
-		return -EINVAL;
-	if (aipu_partition_is_faulted_no_lock(manager, job->core_id))
-		return -EIO;
-
-	sched_core = &manager->partitions[job->core_id];
-	manager->idle_bmap[job->core_id] = 0;
-	if (job->desc.enable_prof) {
-		get_soc_ops(sched_core)->start_bw_profiling(sched_core->dev, get_soc(sched_core));
-		job->sched_time = ktime_get();
-	}
-
-	ret = sched_core->ops->reserve(sched_core, &job->desc, do_trigger, 0);
-	if (ret) {
-		manager->idle_bmap[job->core_id] = 1;
-		if (job->desc.enable_prof)
-			get_soc_ops(sched_core)->stop_bw_profiling(sched_core->dev,
-							       get_soc(sched_core));
-		return ret;
-	}
-
-	if (do_trigger)
-		job->state = AIPU_JOB_STATE_RUNNING;
-
-	if (do_trigger)
-		dev_dbg(sched_core->dev, "[Job %lld of Thread %d] trigger job running done\n",
-			job->desc.job_id, job->uthread_id);
-	else
-		dev_dbg(sched_core->dev, "[Job %lld of Thread %d] reserve for deferred job done\n",
-			job->desc.job_id, job->uthread_id);
-
-	return 0;
-}
-
 static void check_enable_tec_interrupts(struct aipu_job_manager *manager, struct aipu_job *job)
 {
 	struct aipu_tcb *tcb = NULL;
@@ -717,52 +557,6 @@ static void check_enable_tec_interrupts(struct aipu_job_manager *manager, struct
 	manager->tec_intr_en = true;
 }
 
-static int schedule_v3_2_job_no_lock(struct aipu_job_manager *manager, struct aipu_job_desc *desc)
-{
-	int ret = 0;
-	struct aipu_partition *cluster;
-	int pool_type = ZHOUYI_COMMAND_POOL_PCP;
-	u32 partition_id = desc->partition_id;
-	int trigger_type = ZHOUYI_TRIGGER_TYPE_CREATE;
-	struct command_pool *pool;
-
-	if (!manager->partitions || !manager->pools ||
-	    partition_id >= (u32)manager->partition_cnt)
-		return -EINVAL;
-	if (aipu_partition_is_faulted_no_lock(manager, partition_id))
-		return -EIO;
-	cluster = &manager->partitions[0];
-	pool = &manager->pools[partition_id];
-
-	if (desc->partition_id == AIPU_PARTITION_ID_0) {
-		pool_type = ZHOUYI_COMMAND_POOL_PCP;
-	} else {
-		dev_err(manager->dev, "invalid partition_id: %d\n", desc->partition_id);
-		return -EINVAL;
-	}
-
-	if (!pool->created)
-		trigger_type = ZHOUYI_TRIGGER_TYPE_CREATE;
-	else if (pool->aborted)
-		trigger_type = ZHOUYI_TRIGGER_TYPE_UPDATE_DISPATCH;
-	else
-		trigger_type = ZHOUYI_TRIGGER_TYPE_DISPATCH;
-
-	ret = cluster->ops->reserve(cluster, desc, trigger_type, pool_type);
-	if (!ret) {
-		if (trigger_type == ZHOUYI_TRIGGER_TYPE_CREATE)
-			pool->created = true;
-
-		if (desc->exec_flag & AIPU_JOB_EXEC_FLAG_BIND_DISPATCH)
-			pool->bind = true;
-
-		if (pool->aborted)
-			pool->aborted = false;
-	}
-
-	return ret;
-}
-
 static int schedule_v3_job_no_lock(struct aipu_job_manager *manager, struct aipu_job *job)
 {
 	int ret = 0;
@@ -895,12 +689,7 @@ static int schedule_new_job(struct aipu_job_manager *manager, struct aipu_job_de
 	struct aipu_job *kern_job = NULL;
 	struct aipu_thread_wait_queue *queue = NULL;
 	unsigned long flags;
-	struct aipu_job *curr, *next;
-	bool find = false;
 	bool listed = false;
-	bool sram_claimed = false;
-	struct aipu_tcb_v32 *tcb;
-	u64 tcb_offset;
 
 	mutex_lock(&manager->wq_lock);
 	if (user_job->enable_poll_opt)
@@ -927,28 +716,10 @@ static int schedule_new_job(struct aipu_job_manager *manager, struct aipu_job_de
 	if (ret)
 		goto fail_job;
 
-	/* Without a done interrupt there is no safe point to release the DMA pin. */
-	if (user_job->aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-		tcb_offset = kern_job->desc.head_tcb_pa - kern_job->tcb_mapping.dev_pa;
-		if (tcb_offset > kern_job->tcb_mapping.bytes - sizeof(*tcb)) {
-			ret = -EINVAL;
-			goto fail_job;
-		}
-		tcb = (struct aipu_tcb_v32 *)((char *)kern_job->tcb_mapping.va +
-						 tcb_offset);
-		if (!(tcb->grid.interrupt_en & EN_DONE_INTR_V3_2) ||
-		    tcb->grid.group_id != user_job->group_id) {
-			ret = -EOPNOTSUPP;
-			goto fail_job;
-		}
-	}
-
-	if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		ret = aipu_mm_hold_tcb_buf_alloc(manager->mm, kern_job);
-		if (ret) {
-			dev_err(manager->dev, "malloc placeholder tcb failed.\n");
-			goto fail_job;
-		}
+	ret = aipu_mm_hold_tcb_buf_alloc(manager->mm, kern_job);
+	if (ret) {
+		dev_err(manager->dev, "malloc placeholder tcb failed.\n");
+		goto fail_job;
 	}
 
 	spin_lock_irqsave(&manager->lock, flags);
@@ -959,81 +730,18 @@ static int schedule_new_job(struct aipu_job_manager *manager, struct aipu_job_de
 
 	if (do_trigger) {
 		kern_job->state = AIPU_JOB_STATE_PENDING;
-
-		if (user_job->aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-			ret = schedule_v3_2_job_no_lock(manager, &kern_job->desc);
-			if (!ret) {
-				list_for_each_entry_safe(curr, next, &manager->scheduled_head->node, node) {
-					if (curr->state == AIPU_JOB_STATE_PENDING) {
-						find = true;
-						break;
-					}
-				}
-				if (find)
-					list_add_tail(&kern_job->node, &curr->node);
-				else
-					list_add_tail(&kern_job->node, &manager->scheduled_head->node);
-				listed = true;
-				kern_job->state = AIPU_JOB_STATE_RUNNING;
-			} else if (ret == ZHOUYI_V3_2_COMMAND_POOL_FULL) {
-				list_add_tail(&kern_job->node, &manager->scheduled_head->node);
-				listed = true;
-				kern_job->state = AIPU_JOB_STATE_PENDING;
-				ret = 0;
-			}
-		} else if (user_job->aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-			list_add_tail(&kern_job->node, &manager->scheduled_head->node);
-			listed = true;
-			ret = schedule_v3_job_no_lock(manager, kern_job);
-			if (!ret)
-				kern_job->state = AIPU_JOB_STATE_RUNNING;
-		} else {
-			/*
-			 * For a job using SRAM managed by AIPU Gbuilder, it should be
-			 * executed exclusively in serial with other Gbuilder-managed-SRAM ones,
-			 * and parallel scheduling is not allowed.
-			 *
-			 * Pending it if there has been a Gbuilder-managed-SRAM job running;
-			 * otherwise mark the flag and reserve a core for running.
-			 */
-			list_add_tail(&kern_job->node, &manager->scheduled_head->node);
-			listed = true;
-			if (kern_job->desc.exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX) {
-				if (manager->exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX) {
-					goto unlock;
-				} else {
-					manager->exec_flag |= AIPU_JOB_EXEC_FLAG_SRAM_MUTEX;
-					sram_claimed = true;
-				}
-			}
-			kern_job->core_id = get_available_core_no_lock(manager, kern_job);
-			if (kern_job->core_id >= 0)
-				ret = reserve_core_for_job_no_lock(manager, kern_job, do_trigger);
-		}
-	} else {
-		if (user_job->core_id >= (u32)manager->partition_cnt ||
-		    !manager->idle_bmap[user_job->core_id] ||
-		    aipu_partition_is_faulted_no_lock(manager, user_job->core_id) ||
-		    atomic_read(&manager->partitions[user_job->core_id].disable)) {
-			dev_err(manager->dev, "schedule new job (0x%llx) failed: invalid core ID %u",
-				kern_job->desc.job_id, user_job->core_id);
-			ret = -EINVAL;
-			goto unlock;
-		}
-
-		kern_job->state = AIPU_JOB_STATE_DEFERRED;
-		kern_job->core_id = user_job->core_id;
 		list_add_tail(&kern_job->node, &manager->scheduled_head->node);
 		listed = true;
-
-		ret = reserve_core_for_job_no_lock(manager, kern_job, do_trigger);
+		ret = schedule_v3_job_no_lock(manager, kern_job);
+		if (!ret)
+			kern_job->state = AIPU_JOB_STATE_RUNNING;
+	} else {
+		ret = -EOPNOTSUPP;
 	}
 unlock:
 	if (ret && listed) {
 		list_del_init(&kern_job->node);
 		listed = false;
-		if (sram_claimed)
-			manager->exec_flag &= ~AIPU_JOB_EXEC_FLAG_SRAM_MUTEX;
 	}
 	spin_unlock_irqrestore(&manager->lock, flags);
 	if (ret)
@@ -1045,63 +753,6 @@ static int schedule_new_job(struct aipu_job_manager *manager, struct aipu_job_de
 	return ret;
 }
 
-static int trigger_deferred_job_run(struct aipu_job_manager *manager,
-				    struct aipu_job_desc *user_job,
-				    struct file *filp)
-{
-	struct aipu_file_ctx *owner = aipu_file_ctx(filp);
-	unsigned long flags;
-	struct aipu_job *curr = NULL;
-	struct aipu_partition *sched_core = NULL;
-	int ret = -ENOENT;
-
-	spin_lock_irqsave(&manager->lock, flags);
-	if (atomic_read(&manager->is_suspend) || manager->quiescing) {
-		ret = atomic_read(&manager->is_suspend) ? -ENODEV : -EBUSY;
-		goto unlock;
-	}
-	list_for_each_entry(curr, &manager->scheduled_head->node, node) {
-		if (curr->owner == owner &&
-		    curr->desc.job_id == user_job->job_id &&
-		    curr->state == AIPU_JOB_STATE_DEFERRED) {
-			if (curr->desc.aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-				ret = schedule_v3_2_job_no_lock(manager, &curr->desc);
-			} else if (curr->desc.aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-				/**
-				 * for debugger: it should ensure that the NPUs are free to accept
-				 * new jobs before the deferred job is scheduled.
-				 */
-				ret = schedule_v3_job_no_lock(manager, curr);
-			} else {
-				if (curr->core_id < 0 || curr->core_id >= manager->partition_cnt) {
-					ret = -EINVAL;
-					break;
-				}
-				if (aipu_partition_is_faulted_no_lock(manager, curr->core_id) ||
-				    atomic_read(&manager->partitions[curr->core_id].disable)) {
-					ret = -EIO;
-					break;
-				}
-				sched_core = &manager->partitions[curr->core_id];
-				sched_core->ops->trigger(sched_core);
-				ret = 0;
-			}
-			if (!ret)
-				curr->state = AIPU_JOB_STATE_RUNNING;
-			break;
-		}
-	}
-unlock:
-	spin_unlock_irqrestore(&manager->lock, flags);
-
-	if (ret) {
-		dev_err(manager->dev, "trigger deferred job (0x%llx) failed",
-			user_job->job_id);
-	}
-
-	return ret;
-}
-
 /**
  * init_aipu_job_manager - initialize an existing job manager struct during driver probe phase
  * @manager: pointer to the struct job_manager struct to be initialized
@@ -1132,8 +783,6 @@ int init_aipu_job_manager(struct aipu_job_manager *manager, struct aipu_memory_m
 	manager->job_cache = NULL;
 	manager->prof_cache = NULL;
 	manager->grid_id_bmap = NULL;
-	manager->group_id_bmap = NULL;
-	manager->sflag_id_bmap = NULL;
 	INIT_LIST_HEAD(&manager->id_allocations);
 	manager->mm = mm;
 	manager->priv = priv;
@@ -1181,37 +830,18 @@ int init_aipu_job_manager(struct aipu_job_manager *manager, struct aipu_memory_m
 
 	memset(&manager->exit_tcb_desc, 0, sizeof(manager->exit_tcb_desc));
 
-	manager->exec_id = AIPU_EXEC_ID;
-	if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
-		manager->grid_id_num = U16_MAX + 1U;
-		manager->grid_id_bmap = bitmap_zalloc(manager->grid_id_num, GFP_KERNEL);
-		if (!manager->grid_id_bmap) {
-			ret = -ENOMEM;
-			goto err;
-		}
-		__set_bit(0, manager->grid_id_bmap);
-	}
-	if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-		/* Completion interrupt metadata carries only an eight-bit group ID. */
-		manager->group_id_num = U8_MAX + 1U;
-		manager->sflag_id_num = U16_MAX + 1U;
-		manager->group_id_bmap = bitmap_zalloc(manager->group_id_num, GFP_KERNEL);
-		manager->sflag_id_bmap = bitmap_zalloc(manager->sflag_id_num, GFP_KERNEL);
-		if (!manager->group_id_bmap || !manager->sflag_id_bmap) {
-			ret = -ENOMEM;
-			goto err;
-		}
-		__set_bit(0, manager->sflag_id_bmap);
+	manager->grid_id_num = U16_MAX + 1U;
+	manager->grid_id_bmap = bitmap_zalloc(manager->grid_id_num, GFP_KERNEL);
+	if (!manager->grid_id_bmap) {
+		ret = -ENOMEM;
+		goto err;
 	}
+	__set_bit(0, manager->grid_id_bmap);
 
 	manager->is_init = 1;
 	return 0;
 
 err:
-	bitmap_free(manager->sflag_id_bmap);
-	manager->sflag_id_bmap = NULL;
-	bitmap_free(manager->group_id_bmap);
-	manager->group_id_bmap = NULL;
 	bitmap_free(manager->grid_id_bmap);
 	manager->grid_id_bmap = NULL;
 	WARN_ON(!list_empty(&manager->id_allocations));
@@ -1265,10 +895,6 @@ void deinit_aipu_job_manager(struct aipu_job_manager *manager)
 
 	bitmap_free(manager->grid_id_bmap);
 	manager->grid_id_bmap = NULL;
-	bitmap_free(manager->group_id_bmap);
-	manager->group_id_bmap = NULL;
-	bitmap_free(manager->sflag_id_bmap);
-	manager->sflag_id_bmap = NULL;
 	list_for_each_entry_safe(allocation, next, &manager->id_allocations, node) {
 		list_del(&allocation->node);
 		kfree(allocation);
@@ -1306,13 +932,11 @@ int aipu_job_manager_set_partitions_info(struct aipu_job_manager *manager, int p
 		return -ENOMEM;
 	}
 	memset(new_idle, 1, partition_cnt * sizeof(*new_idle));
-	if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
-		new_pools = kcalloc(partition_cnt, sizeof(*new_pools), GFP_KERNEL);
-		if (!new_pools) {
-			kfree(new_faulted);
-			kfree(new_idle);
-			return -ENOMEM;
-		}
+	new_pools = kcalloc(partition_cnt, sizeof(*new_pools), GFP_KERNEL);
+	if (!new_pools) {
+		kfree(new_faulted);
+		kfree(new_idle);
+		return -ENOMEM;
 	}
 
 	spin_lock_irqsave(&manager->lock, flags);
@@ -1357,7 +981,7 @@ int aipu_job_manager_scheduler(struct aipu_job_manager *manager, struct aipu_job
 		return -EINVAL;
 	mutex_lock(&manager->op_lock);
 
-	if (unlikely(!is_user_job_valid(manager, user_job, aipu_file_ctx(filp)))) {
+	if (unlikely(!is_user_job_valid(manager, user_job))) {
 		dev_err(manager->dev, "[scheduler] invalid user job (0x%llx)", user_job->job_id);
 		ret = -EINVAL;
 		goto unlock;
@@ -1369,12 +993,7 @@ int aipu_job_manager_scheduler(struct aipu_job_manager *manager, struct aipu_job
 		goto unlock;
 	}
 
-	if (!user_job->is_defer_run)
-		ret = schedule_new_job(manager, user_job, filp, 1);
-	else if (!user_job->do_trigger)
-		ret = schedule_new_job(manager, user_job, filp, 0);
-	else
-		ret = trigger_deferred_job_run(manager, user_job, filp);
+	ret = schedule_new_job(manager, user_job, filp, 1);
 
 	if (ret)
 		dev_err(manager->dev,
@@ -1402,10 +1021,9 @@ static void *aipu_job_mapping_ptr(const struct aipu_tcb_mapping *mapping,
 
 static void aipu_real_time_printk(struct aipu_job_manager *manager,
 				  const struct aipu_tcb_mapping *mapping,
-				  int version, u64 asid0_base,
+				  u64 asid0_base,
 				  struct job_irq_info *info)
 {
-	struct aipu_tcb_v32 *tcb_v32;
 	struct aipu_tcb *tcb;
 	char *buf;
 	u64 tcb_pa;
@@ -1415,23 +1033,15 @@ static void aipu_real_time_printk(struct aipu_job_manager *manager,
 	u32 print_offset;
 	u32 print_size;
 
-	print_size = version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0 ?
-		GET_PRINF_SIZE_V3_2(info->sig_flag) : GET_PRINF_SIZE(info->sig_flag);
+	print_size = GET_PRINF_SIZE(info->sig_flag);
 	if (!print_size || check_add_overflow(asid0_base, (u64)info->tail_tcbp,
 					      &tcb_pa))
 		return;
 
-	if (version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-		tcb_v32 = aipu_job_mapping_ptr(mapping, tcb_pa, sizeof(*tcb_v32));
-		if (!tcb_v32)
-			return;
-		print_offset = tcb_v32->task.pprint;
-	} else {
-		tcb = aipu_job_mapping_ptr(mapping, tcb_pa, sizeof(*tcb));
-		if (!tcb)
-			return;
-		print_offset = tcb->task.pprint;
-	}
+	tcb = aipu_job_mapping_ptr(mapping, tcb_pa, sizeof(*tcb));
+	if (!tcb)
+		return;
+	print_offset = tcb->task.pprint;
 
 	if (check_add_overflow(asid0_base, (u64)print_offset, &buf_pa) ||
 	    buf_pa < mapping->dev_pa)
@@ -1571,22 +1181,12 @@ static bool is_curr_irq_job(struct aipu_job *job, struct job_irq_info *info, u64
 	       (u64)info->tail_tcbp <= job->desc.last_task_tcb_pa - asid_base;
 }
 
-static bool is_v3x_job_done_or_excep(struct aipu_job *job, struct job_irq_info *info,
-					 int flag)
-{
-	if (!job || !info || job->desc.last_task_tcb_pa < job->desc.asid0_base)
-		return false;
-	return info->tail_tcbp == (u32)(job->desc.last_task_tcb_pa - job->desc.asid0_base) &&
-		   (IS_DONE_IRQ(flag) || IS_EXCEPTION_IRQ(flag));
-}
-
-static bool is_v3_2_1_job_done_or_excep(struct aipu_job *job, struct job_irq_info *info,
-					 int flag)
+static bool is_v3_job_done_or_excep(struct aipu_job *job, struct job_irq_info *info,
+				    int flag)
 {
 	if (!job || !info || job->desc.last_task_tcb_pa < job->desc.asid0_base)
 		return false;
 	return info->tail_tcbp == (u32)(job->desc.last_task_tcb_pa - job->desc.asid0_base) &&
-		   info->group_id == job->desc.group_id &&
 		   (IS_DONE_IRQ(flag) || IS_EXCEPTION_IRQ(flag));
 }
 
@@ -1598,87 +1198,20 @@ static bool do_abortion(int flag, struct job_irq_info *info)
 	return IS_SERIOUS_ERR(flag) || IS_EXCEPTION_SIGNAL(info->sig_flag);
 }
 
-static bool do_abortion_V3_2(int flag, struct job_irq_info *info)
-{
-	if (!info)
-		return false;
-
-	if (IS_TEC_IRQ_V3_2(flag) && (IS_FAULT_IRQ_V3_2(flag)))
-		return true;
-
-	return IS_EXCEPTION_SIGNAL_V3_2(info->sig_flag);
-}
-
-static enum irq_event_type aipu_v3_2_recovery_event(int flag,
-						     struct job_irq_info *info)
-{
-	if (IS_ERROR_IRQ_V3_2(flag) || IS_TIMEOUT_IRQ_V3_2(flag) ||
-	    IS_FAULT_IRQ_V3_2(flag))
-		return IS_CORE_IRQ_V3_2(flag) || IS_TEC_IRQ_V3_2(flag) ?
-			AIPU_IRQ_EVENT_ABORT : AIPU_IRQ_EVENT_RESET;
-	if (info && IS_EXCEPTION_SIGNAL_V3_2(info->sig_flag))
-		return AIPU_IRQ_EVENT_ABORT;
-
-	return AIPU_IRQ_EVENT_NONE;
-}
-
-static bool is_job_end(struct aipu_job *job, struct aipu_partition *partition,
-		       struct job_irq_info *info, int flag)
+static bool is_job_end(struct aipu_job *job, struct job_irq_info *info, int flag)
 {
 	if (job->state != AIPU_JOB_STATE_RUNNING)
 		return false;
 
-	if (job->desc.aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_1) {
-		return is_v3_2_1_job_done_or_excep(job, info, flag);
-	} else if (job->desc.aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-		return is_v3x_job_done_or_excep(job, info, flag);
-	} else if (job->desc.aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		return is_v3x_job_done_or_excep(job, info, flag) ||
-			do_abortion(flag, info);
-
-	}
-
-	return job->core_id == partition->id;
+	return is_v3_job_done_or_excep(job, info, flag) ||
+		do_abortion(flag, info);
 }
 
 static bool is_job_abnormal(struct aipu_job *job, int flag,
 			    struct job_irq_info *info)
 {
-	if (job->desc.aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3)
-		return IS_ABNORMAL(flag) ||
-			(info && IS_EXCEPTION_SIGNAL(info->sig_flag));
-	else if (job->desc.aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0)
-		return IS_ABNORMAL_V3_2(flag) ||
-			(info && IS_EXCEPTION_SIGNAL_V3_2(info->sig_flag));
-
-	return flag != 0;
-}
-
-/**
- * aipu_sched_hqos_job_no_lock - schedule high qos job
- * @m: pointer to job manager struct
- */
-static void aipu_sched_hqos_job_no_lock(struct aipu_job_manager *m)
-{
-	struct aipu_job *curr = NULL;
-	struct aipu_job *next = NULL;
-	struct list_head *h = NULL;
-	int ret;
-
-	if (unlikely(!m))
-		return;
-
-	h = &m->scheduled_head->node;
-
-	list_for_each_entry_safe(curr, next, h, node) {
-		if (!curr->canceling && curr->state == AIPU_JOB_STATE_PENDING &&
-		    (curr->desc.exec_flag & AIPU_JOB_EXEC_FLAG_BIND_DISPATCH ||
-		     curr->desc.exec_flag & AIPU_JOB_EXEC_FLAG_QOS_FAST)) {
-			ret = schedule_v3_2_job_no_lock(m, &curr->desc);
-			if (!ret)
-				curr->state = AIPU_JOB_STATE_RUNNING;
-		}
-	}
+	return IS_ABNORMAL(flag) ||
+		(info && IS_EXCEPTION_SIGNAL(info->sig_flag));
 }
 
 static void aipu_job_manager_print_signal(struct aipu_job_manager *manager,
@@ -1686,7 +1219,6 @@ static void aipu_job_manager_print_signal(struct aipu_job_manager *manager,
 {
 	struct aipu_tcb_mapping mapping = { 0 };
 	struct aipu_job *curr;
-	int version = 0;
 	u64 asid0_base = 0;
 
 	spin_lock(&manager->lock);
@@ -1695,7 +1227,6 @@ static void aipu_job_manager_print_signal(struct aipu_job_manager *manager,
 		    is_curr_irq_job(curr, info, curr->desc.asid0_base) &&
 		    !aipu_mm_clone_tcb_mapping(manager->mm, &curr->tcb_mapping,
 					       &mapping)) {
-			version = curr->desc.aipu_version;
 			asid0_base = curr->desc.asid0_base;
 			break;
 		}
@@ -1704,7 +1235,7 @@ static void aipu_job_manager_print_signal(struct aipu_job_manager *manager,
 
 	if (!mapping.tbuf)
 		return;
-	aipu_real_time_printk(manager, &mapping, version, asid0_base, info);
+	aipu_real_time_printk(manager, &mapping, asid0_base, info);
 	aipu_mm_unpin_job_tcb(manager->mm, &mapping);
 }
 
@@ -1720,9 +1251,6 @@ void aipu_job_manager_irq_upper_half(struct aipu_partition *partition, int flag,
 	struct aipu_job *curr = NULL;
 	struct aipu_job_manager *manager = NULL;
 	struct list_head *h = NULL;
-	enum irq_event_type recovery_event = AIPU_IRQ_EVENT_NONE;
-	int triggered = 0;
-	int ret = 0;
 
 	if (unlikely(!partition))
 		return;
@@ -1731,82 +1259,41 @@ void aipu_job_manager_irq_upper_half(struct aipu_partition *partition, int flag,
 	if (!manager || !manager->scheduled_head)
 		return;
 	h = &manager->scheduled_head->node;
-	if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-		if (info && IS_SIGNAL_IRQ_V3_2(flag) &&
-		    !IS_EXCEPTION_SIGNAL_V3_2(info->sig_flag)) {
-			if (IS_PRINTF_SIGNAL_V3_2(info->sig_flag))
-				aipu_job_manager_print_signal(manager, info);
-#if AIPU_CONFIG_ENABLE_INTR_PROFILING
-			if (IS_PROFILER_SIGNAL_V3_2(info->sig_flag)) {
-				pr_debug("AIPU profiler signal interrupt\n");
-				aipu_job_manager_real_time_get_pdata(manager, info);
-			}
-#endif
-			if (IS_COREDUMP_SIGNAL_V3_2(info->sig_flag)) {
-				spin_lock(&manager->lock);
-				aipu_mark_fault_event_no_lock(manager, partition,
-							      AIPU_IRQ_EVENT_ABORT);
-				spin_unlock(&manager->lock);
-			}
-			return;
-		}
-	} else if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		if (info && IS_SIGNAL_IRQ(flag) && !IS_EXCEPTION_SIGNAL(info->sig_flag)) {
-			if (IS_PRINTF_SIGNAL(info->sig_flag))
-				aipu_job_manager_print_signal(manager, info);
+	if (info && IS_SIGNAL_IRQ(flag) && !IS_EXCEPTION_SIGNAL(info->sig_flag)) {
+		if (IS_PRINTF_SIGNAL(info->sig_flag))
+			aipu_job_manager_print_signal(manager, info);
 
 #if AIPU_CONFIG_ENABLE_INTR_PROFILING
-			if (IS_PROFILER_SIGNAL(info->sig_flag)) {
-				pr_debug("AIPU profiler signal interrupt\n");
-				aipu_job_manager_real_time_get_pdata(manager, info);
-			}
+		if (IS_PROFILER_SIGNAL(info->sig_flag)) {
+			pr_debug("AIPU profiler signal interrupt\n");
+			aipu_job_manager_real_time_get_pdata(manager, info);
+		}
 #endif
-			if (IS_COREDUMP_SIGNAL(info->sig_flag)) {
-				spin_lock(&manager->lock);
-				aipu_mark_fault_event_no_lock(manager, partition,
-							      AIPU_IRQ_EVENT_ABORT);
-				spin_unlock(&manager->lock);
-			}
-			return;
+		if (IS_COREDUMP_SIGNAL(info->sig_flag)) {
+			spin_lock(&manager->lock);
+			aipu_mark_fault_event_no_lock(manager, partition,
+						      AIPU_IRQ_EVENT_ABORT);
+			spin_unlock(&manager->lock);
 		}
+		return;
 	}
 
 	spin_lock(&manager->lock);
-	if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0)
-		recovery_event = aipu_v3_2_recovery_event(flag, info);
-	if (recovery_event == AIPU_IRQ_EVENT_ABORT &&
-	    (!manager->pools || partition->id >= (u32)manager->partition_cnt ||
-	     !manager->pools[partition->id].created))
-		recovery_event = AIPU_IRQ_EVENT_RESET;
-	if (recovery_event != AIPU_IRQ_EVENT_NONE ||
-	    partition->event_type == AIPU_IRQ_EVENT_RESET) {
-		aipu_mark_fault_event_no_lock(manager, partition,
-					      recovery_event == AIPU_IRQ_EVENT_NONE ?
-					      AIPU_IRQ_EVENT_RESET : recovery_event);
+	if (partition->event_type == AIPU_IRQ_EVENT_RESET) {
+		aipu_mark_fault_event_no_lock(manager, partition, AIPU_IRQ_EVENT_RESET);
 		spin_unlock(&manager->lock);
 		return;
 	}
 
-	if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-		if (do_abortion_V3_2(flag, info) && manager->pools &&
-		    partition->id < (u32)manager->partition_cnt &&
-		    manager->pools[partition->id].created) {
-			aipu_mark_fault_event_no_lock(manager, partition,
-						      AIPU_IRQ_EVENT_ABORT);
-			spin_unlock(&manager->lock);
-			return;
-		}
-	} else if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		if (do_abortion(flag, info)) {
-			aipu_mark_fault_event_no_lock(manager, partition,
-						      AIPU_IRQ_EVENT_ABORT);
-			spin_unlock(&manager->lock);
-			return;
-		}
+	if (do_abortion(flag, info)) {
+		aipu_mark_fault_event_no_lock(manager, partition,
+					      AIPU_IRQ_EVENT_ABORT);
+		spin_unlock(&manager->lock);
+		return;
 	}
 
 	list_for_each_entry(curr, h, node) {
-		if (is_job_end(curr, partition, info, flag)) {
+		if (is_job_end(curr, info, flag)) {
 			if (unlikely(is_job_abnormal(curr, flag, info)))
 				curr->state = AIPU_JOB_STATE_EXCEP;
 			else
@@ -1833,40 +1320,6 @@ void aipu_job_manager_irq_upper_half(struct aipu_partition *partition, int flag,
 		}
 	}
 
-	if (!manager->quiescing && !atomic_read(&partition->disable) &&
-	    !aipu_partition_is_faulted_no_lock(manager, partition->id)) {
-		//schedule hqos job first
-		if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0)
-			aipu_sched_hqos_job_no_lock(manager);
-
-		//then schedule other jobs;
-		list_for_each_entry(curr, h, node) {
-			if (!curr->canceling && curr->state == AIPU_JOB_STATE_PENDING &&
-			    is_job_ok_for_core(partition, &curr->desc)) {
-				if (curr->desc.exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX) {
-					if (manager->exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX)
-						continue;
-					else
-						manager->exec_flag |= AIPU_JOB_EXEC_FLAG_SRAM_MUTEX;
-				}
-				curr->core_id = partition->id;
-				ret = reserve_core_for_job_no_lock(manager, curr, 1);
-				if (!ret) {
-					triggered = 1;
-				} else {
-					curr->core_id = -1;
-					if (curr->desc.exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX)
-						manager->exec_flag &= ~AIPU_JOB_EXEC_FLAG_SRAM_MUTEX;
-				}
-				break;
-			}
-		}
-	}
-
-	if (!triggered && manager->idle_bmap &&
-	    partition->id < (u32)manager->partition_cnt)
-		manager->idle_bmap[partition->id] = 1;
-
 	spin_unlock(&manager->lock);
 }
 
@@ -1901,13 +1354,9 @@ static int aipu_job_manager_destroy_command_pool(struct aipu_job_manager *manage
 
 	spin_lock_irqsave(&manager->lock, flags);
 	if (!ret) {
-		if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-			memset(pool->qlist, 0,
-			       sizeof(*pool->qlist) * AIPU_JOB_QOS_MAX);
-			clear_htbuf = true;
-		} else if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-			pool->aborted = false;
-		}
+		memset(pool->qlist, 0,
+		       sizeof(*pool->qlist) * AIPU_JOB_QOS_MAX);
+		clear_htbuf = true;
 		manager->tec_intr_en = false;
 		pool->created = false;
 		pool->bind = false;
@@ -1932,7 +1381,6 @@ static void aipu_job_manager_finish_irq_bottom_half(struct aipu_partition *core,
 	struct aipu_job_manager *manager = NULL;
 	unsigned long flags;
 	bool do_destroy = false;
-	struct aipu_priv *aipu = NULL;
 	int destroy_ret = 0;
 	int ret = 0;
 	struct list_head *h = NULL;
@@ -1940,8 +1388,6 @@ static void aipu_job_manager_finish_irq_bottom_half(struct aipu_partition *core,
 	enum irq_event_type event = AIPU_IRQ_EVENT_NONE;
 	bool event_success = false;
 	bool reschedule_event = false;
-	bool restore_tick = false;
-	int saved_core_count = 0;
 
 	if (unlikely(!core))
 		return;
@@ -1950,12 +1396,10 @@ static void aipu_job_manager_finish_irq_bottom_half(struct aipu_partition *core,
 	if (!manager || !manager->scheduled_head)
 		return;
 	h = &manager->scheduled_head->node;
-	if (core->version >= AIPU_ISA_VERSION_ZHOUYI_V3 && manager->pools &&
-	    core->id < (u32)manager->partition_cnt)
+	if (manager->pools && core->id < (u32)manager->partition_cnt)
 		pool = &manager->pools[core->id];
 
-	if (core->version >= AIPU_ISA_VERSION_ZHOUYI_V3)
-		do_destroy = true;
+	do_destroy = true;
 
 	spin_lock_irqsave(&manager->lock, flags);
 	if (process_event && pool) {
@@ -1969,38 +1413,7 @@ static void aipu_job_manager_finish_irq_bottom_half(struct aipu_partition *core,
 	/* Reset/abort may poll hardware, so serialize them outside spinlocks. */
 	if (event == AIPU_IRQ_EVENT_RESET || event == AIPU_IRQ_EVENT_ABORT) {
 		mutex_lock(&manager->hw_lock);
-		if (event == AIPU_IRQ_EVENT_RESET &&
-		    core->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-			aipu = core->priv;
-			if (!aipu || !core->ops || !core->ops->initialize ||
-			    !core->ops->enable_core_cnt || !core->cluster_cnt) {
-				ret = -ENODEV;
-			} else {
-				saved_core_count = atomic_read(
-					&core->clusters[0].en_core_cnt);
-				restore_tick = atomic_read(&manager->tick_counter) > 0;
-				if (aipu->reset_gpio && core->ops->hw_reset) {
-					core->ops->hw_reset(core);
-					ret = 0;
-				} else if (core->ops->soft_reset) {
-					ret = core->ops->soft_reset(core, false);
-				} else {
-					ret = -EOPNOTSUPP;
-				}
-				if (!ret) {
-					core->ops->initialize(core);
-					core->ops->enable_core_cnt(core, 0,
-							   saved_core_count);
-					if (core->irq_obj &&
-					    READ_ONCE(core->irq_obj->published))
-						core->ops->enable_interrupt(core,
-								    false);
-					if (restore_tick &&
-					    core->ops->enable_tick_counter)
-						core->ops->enable_tick_counter(core);
-				}
-			}
-		} else if (event == AIPU_IRQ_EVENT_ABORT) {
+		if (event == AIPU_IRQ_EVENT_ABORT) {
 			spin_lock_irqsave(&manager->lock, flags);
 			ret = pool->created ? 1 : 0;
 			spin_unlock_irqrestore(&manager->lock, flags);
@@ -2048,15 +1461,12 @@ static void aipu_job_manager_finish_irq_bottom_half(struct aipu_partition *core,
 	spin_lock_irqsave(&manager->lock, flags);
 
 	list_for_each_entry_safe(curr, next, h, node) {
-		if (aipu_job_is_reapable(curr) && !curr->wake_up &&
-		    (curr->desc.aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3 ||
-		     curr->core_id == core->id)) {
+		if (aipu_job_is_reapable(curr) && !curr->wake_up) {
 			if (curr->desc.enable_prof)
 				curr->pdata.execution_time_ns =
 				(long)ktime_to_ns(ktime_sub(curr->done_time, curr->sched_time));
 
-			if (curr->desc.aipu_version == AIPU_ISA_VERSION_ZHOUYI_V3 &&
-			    curr->curr_hold_tcb &&
+			if (curr->curr_hold_tcb &&
 			    !aipu_mm_unlink_tcb(manager->mm, curr->curr_hold_tcb, false))
 				curr->curr_hold_tcb = 0;
 			aipu_mm_unpin_job_tcb(manager->mm, &curr->tcb_mapping);
@@ -2098,9 +1508,7 @@ static void aipu_job_manager_finish_irq_bottom_half(struct aipu_partition *core,
 		aipu_has_pending_event_no_lock(manager);
 
 	list_for_each_entry_safe(curr, next, h, node) {
-		if (aipu_job_is_reapable(curr) && !curr->wake_up &&
-		    (curr->desc.aipu_version >= AIPU_ISA_VERSION_ZHOUYI_V3 ||
-		     curr->core_id == core->id)) {
+		if (aipu_job_is_reapable(curr) && !curr->wake_up) {
 			wake_up_interruptible(curr->thread_queue);
 			curr->wake_up = 1;
 #ifdef DEBUG
@@ -2281,14 +1689,10 @@ void aipu_job_manager_irq_bottom_half(struct aipu_partition *core)
 	if (!manager)
 		return;
 
-	/* V3 has one shared IRQ worker whose payload is partition zero. */
-	if (manager->version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		for (id = 0; id < (u32)manager->partition_cnt; id++)
-			aipu_job_manager_finish_irq_bottom_half(&manager->partitions[id],
-							   true, false);
-	} else {
-		aipu_job_manager_finish_irq_bottom_half(core, true, false);
-	}
+	/* Sky1 V3 has one shared IRQ worker whose payload is partition zero. */
+	for (id = 0; id < (u32)manager->partition_cnt; id++)
+		aipu_job_manager_finish_irq_bottom_half(&manager->partitions[id],
+							true, false);
 }
 
 static bool aipu_owner_has_active_job(struct aipu_job_manager *manager,
@@ -2304,10 +1708,7 @@ static bool aipu_owner_has_active_job(struct aipu_job_manager *manager,
 		if (job->owner != owner || !aipu_job_is_hw_active(job) ||
 		    (match_job_id && job->desc.job_id != (u64)job_id))
 			continue;
-		if ((manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3 &&
-		     job->desc.partition_id == id) ||
-		    (manager->version < AIPU_ISA_VERSION_ZHOUYI_V3 &&
-		     job->core_id == (int)id)) {
+		if (job->desc.partition_id == id) {
 			found = true;
 			break;
 		}
@@ -2348,11 +1749,7 @@ static int aipu_abort_cmd_pool(struct aipu_job_manager *manager, u32 pool_id)
 	    manager->partition_cnt <= 0 ||
 	    pool_id >= (u32)manager->partition_cnt)
 		return -ENODEV;
-	if (manager->version < AIPU_ISA_VERSION_ZHOUYI_V3)
-		return -EOPNOTSUPP;
-
-	partition = manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0 ?
-			&manager->partitions[0] : &manager->partitions[pool_id];
+	partition = &manager->partitions[pool_id];
 	if (!partition->ops || !partition->ops->abort_command_pool)
 		return -EOPNOTSUPP;
 
@@ -2421,83 +1818,6 @@ static int aipu_abort_cmd_pool(struct aipu_job_manager *manager, u32 pool_id)
 }
 
 /* The caller serializes this operation with manager->op_lock. */
-static int aipu_quiesce_legacy_core(struct aipu_job_manager *manager, u32 core_id)
-{
-	struct aipu_partition *core;
-	struct aipu_job *job;
-	unsigned long flags;
-	int ret;
-
-	if (!manager->partitions || !manager->idle_bmap || !manager->faulted_bmap ||
-	    core_id >= (u32)manager->partition_cnt)
-		return -EINVAL;
-	core = &manager->partitions[core_id];
-	if (!core->ops || !core->ops->is_idle)
-		return -EOPNOTSUPP;
-	spin_lock_irqsave(&manager->lock, flags);
-	ret = manager->quiescing ? 1 : 0;
-	spin_unlock_irqrestore(&manager->lock, flags);
-	if (ret)
-		aipu_irq_flush_workqueue(core->irq_obj);
-
-	mutex_lock(&manager->hw_lock);
-	spin_lock_irqsave(&manager->lock, flags);
-	if (manager->quiescing) {
-		spin_unlock_irqrestore(&manager->lock, flags);
-		mutex_unlock(&manager->hw_lock);
-		return -EBUSY;
-	}
-	manager->quiescing = true;
-	manager->external_quiesce = true;
-	manager->faulted_bmap[core_id] = true;
-	spin_unlock_irqrestore(&manager->lock, flags);
-
-	if (!core->ops->is_idle(core)) {
-		if (!core->ops->soft_reset) {
-			ret = -EOPNOTSUPP;
-			goto clear_quiescing;
-		}
-		ret = core->ops->soft_reset(core, true);
-		if (ret)
-			goto clear_quiescing;
-		if (!core->ops->is_idle(core)) {
-			ret = -EBUSY;
-			goto clear_quiescing;
-		}
-	}
-
-	spin_lock_irqsave(&manager->lock, flags);
-	list_for_each_entry(job, &manager->scheduled_head->node, node) {
-		if (job->core_id != (int)core_id ||
-		    aipu_job_is_reapable(job))
-			continue;
-		job->state = AIPU_JOB_STATE_EXCEP;
-		if (job->desc.exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX)
-			manager->exec_flag &= ~AIPU_JOB_EXEC_FLAG_SRAM_MUTEX;
-	}
-	manager->idle_bmap[core_id] = true;
-	spin_unlock_irqrestore(&manager->lock, flags);
-	mutex_unlock(&manager->hw_lock);
-
-	aipu_job_manager_finish_irq_bottom_half(core, false, true);
-	spin_lock_irqsave(&manager->lock, flags);
-	if (core->event_type == AIPU_IRQ_EVENT_NONE)
-		manager->faulted_bmap[core_id] = false;
-	ret = manager->faulted_bmap[core_id] ? -EIO : 0;
-	manager->external_quiesce = false;
-	manager->quiescing = aipu_has_pending_event_no_lock(manager);
-	spin_unlock_irqrestore(&manager->lock, flags);
-	return ret;
-
-clear_quiescing:
-	spin_lock_irqsave(&manager->lock, flags);
-	manager->external_quiesce = false;
-	manager->quiescing = aipu_has_pending_event_no_lock(manager);
-	spin_unlock_irqrestore(&manager->lock, flags);
-	mutex_unlock(&manager->hw_lock);
-	return ret;
-}
-
 int aipu_job_manager_abort_cmd_pool(struct aipu_job_manager *manager)
 {
 	u32 pool_id;
@@ -2548,12 +1868,7 @@ int aipu_job_manager_cancel_jobs(struct aipu_job_manager *manager, struct file *
 		curr->canceling = true;
 		if (!aipu_job_is_hw_active(curr))
 			continue;
-		if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
-			if (curr->desc.partition_id >= (u32)manager->partition_cnt) {
-				ret = -EINVAL;
-				break;
-			}
-		} else if (curr->core_id < 0 || curr->core_id >= manager->partition_cnt) {
+		if (curr->desc.partition_id >= (u32)manager->partition_cnt) {
 			ret = -EINVAL;
 			break;
 		}
@@ -2565,15 +1880,11 @@ int aipu_job_manager_cancel_jobs(struct aipu_job_manager *manager, struct file *
 	for (id = 0; id < (u32)manager->partition_cnt; id++) {
 		if (!aipu_owner_has_active_job(manager, owner, false, 0, id))
 			continue;
-		if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3 &&
-		    aipu_pool_has_foreign_job(manager, owner, id)) {
+		if (aipu_pool_has_foreign_job(manager, owner, id)) {
 			ret = -EBUSY;
 			goto clear_canceling;
 		}
-		if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3)
-			ret = aipu_abort_cmd_pool(manager, id);
-		else
-			ret = aipu_quiesce_legacy_core(manager, id);
+		ret = aipu_abort_cmd_pool(manager, id);
 		/* A completion interrupt can win the race with the reset/abort. */
 		if (ret && !aipu_owner_has_active_job(manager, owner, false, 0, id))
 			ret = 0;
@@ -2585,11 +1896,6 @@ int aipu_job_manager_cancel_jobs(struct aipu_job_manager *manager, struct file *
 	list_for_each_entry_safe(curr, next, &manager->scheduled_head->node, node) {
 		if (curr->owner != owner)
 			continue;
-		if (manager->version < AIPU_ISA_VERSION_ZHOUYI_V3 &&
-		    (curr->state == AIPU_JOB_STATE_DEFERRED ||
-		     aipu_job_is_hw_active(curr)) &&
-		    curr->core_id >= 0 && curr->core_id < manager->partition_cnt)
-			manager->idle_bmap[curr->core_id] = true;
 		if (curr->desc.exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX)
 			manager->exec_flag &= ~AIPU_JOB_EXEC_FLAG_SRAM_MUTEX;
 		list_move_tail(&curr->node, &delete_list);
@@ -2618,103 +1924,6 @@ int aipu_job_manager_cancel_jobs(struct aipu_job_manager *manager, struct file *
 	return ret;
 }
 
-/**
- * aipu_job_manager_invalidate_timeout_job - invalidate/kill a timeout job
- * @manager: pointer to the struct job_manager initialized in init_aipu_job_manager()
- * @job_id:  zero-extended 32-bit job ID from the UAPI
- * @filp: file struct pointer that owns the job
- *
- * Return: 0 on success and error code otherwise.
- */
-int aipu_job_manager_invalidate_timeout_job(struct aipu_job_manager *manager, u32 job_id,
-						struct file *filp)
-{
-	struct aipu_file_ctx *owner = aipu_file_ctx(filp);
-	struct aipu_job *curr;
-	struct aipu_job *victim = NULL;
-	unsigned long flags;
-	bool was_active = false;
-	u32 id = 0;
-	int ret = 0;
-
-	if (!manager || !owner)
-		return -EINVAL;
-	mutex_lock(&manager->op_lock);
-
-	spin_lock_irqsave(&manager->lock, flags);
-	list_for_each_entry(curr, &manager->scheduled_head->node, node) {
-		if (curr->owner == owner && curr->desc.job_id == (u64)job_id) {
-			victim = curr;
-			break;
-		}
-	}
-	if (victim && aipu_job_is_hw_active(victim)) {
-		was_active = true;
-		if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
-			id = victim->desc.partition_id;
-			if (id >= (u32)manager->partition_cnt)
-				ret = -EINVAL;
-		} else if (victim->core_id < 0 || victim->core_id >= manager->partition_cnt) {
-			ret = -EINVAL;
-		} else {
-			id = victim->core_id;
-		}
-	}
-	if (victim)
-		victim->canceling = true;
-	spin_unlock_irqrestore(&manager->lock, flags);
-	if (!victim) {
-		ret = -ENOENT;
-		goto unlock;
-	}
-	if (ret)
-		goto unlock;
-
-	if (was_active &&
-	    aipu_owner_has_active_job(manager, owner, true, job_id, id)) {
-		if (manager->version >= AIPU_ISA_VERSION_ZHOUYI_V3)
-			ret = aipu_abort_cmd_pool(manager, id);
-		else
-			ret = aipu_quiesce_legacy_core(manager, id);
-		if (ret && !aipu_owner_has_active_job(manager, owner, true, job_id, id))
-			ret = 0;
-		if (ret)
-			goto unlock;
-	}
-
-	spin_lock_irqsave(&manager->lock, flags);
-	if (!list_empty(&victim->node))
-		list_del_init(&victim->node);
-	if (manager->version < AIPU_ISA_VERSION_ZHOUYI_V3 &&
-	    (victim->state == AIPU_JOB_STATE_DEFERRED ||
-	     victim->state == AIPU_JOB_STATE_RUNNING) &&
-	    victim->core_id >= 0 && victim->core_id < manager->partition_cnt)
-		manager->idle_bmap[victim->core_id] = true;
-	if (victim->desc.exec_flag & AIPU_JOB_EXEC_FLAG_SRAM_MUTEX)
-		manager->exec_flag &= ~AIPU_JOB_EXEC_FLAG_SRAM_MUTEX;
-	spin_unlock_irqrestore(&manager->lock, flags);
-
-	destroy_aipu_job_with_wait(manager, victim);
-unlock:
-	if (ret && victim) {
-		spin_lock_irqsave(&manager->lock, flags);
-		if (!list_empty(&victim->node))
-			victim->canceling = false;
-		spin_unlock_irqrestore(&manager->lock, flags);
-	}
-	mutex_unlock(&manager->op_lock);
-	return ret;
-}
-
-/**
- * aipu_job_manager_take_job_status - reserve completed jobs for a status query
- * @manager: pointer to the struct job_manager initialized in init_aipu_job_manager()
- * @job_status: query parameters and returned status count
- * @snapshot: kernel-owned status records and reserved jobs
- * @filp: file struct pointer
- *
- * Return: 0 on success and error code otherwise.
- */
 int aipu_job_manager_take_job_status(struct aipu_job_manager *manager,
 				     struct aipu_job_status_query *job_status,
 				     struct aipu_job_status_snapshot *snapshot,
@@ -3424,102 +2633,6 @@ int aipu_job_manager_free_grid_id(struct aipu_job_manager *manager, u16 id,
 	return ret;
 }
 
-int aipu_job_manager_alloc_exec_id(struct aipu_job_manager *manager, u64 *exec_id)
-{
-	if (!manager || !exec_id)
-		return -EINVAL;
-
-	mutex_lock(&manager->id_lock);
-	*exec_id = manager->exec_id++;
-	mutex_unlock(&manager->id_lock);
-
-	return 0;
-}
-
-int aipu_job_manager_alloc_group_id(struct aipu_job_manager *manager,
-				    struct aipu_id_desc *desc,
-				    struct aipu_file_ctx *owner)
-{
-	u32 first;
-	int ret;
-
-	if (!manager || !desc)
-		return -EINVAL;
-	mutex_lock(&manager->op_lock);
-	ret = aipu_record_id_allocation(manager, AIPU_ID_GROUP, owner,
-					manager->group_id_bmap,
-					manager->group_id_num, desc->size, 0,
-					&first);
-	mutex_unlock(&manager->op_lock);
-	if (ret)
-		desc->first_id = 0;
-	else
-		desc->first_id = first;
-
-	return ret;
-}
-
-int aipu_job_manager_free_group_id(struct aipu_job_manager *manager,
-				   struct aipu_id_desc *desc,
-				   struct aipu_file_ctx *owner)
-{
-	int ret;
-
-	if (!manager || !desc)
-		return -EINVAL;
-	ret = aipu_job_manager_owner_guard_begin(manager, owner);
-	if (ret)
-		return ret;
-	ret = aipu_release_id_allocation(manager, AIPU_ID_GROUP, owner,
-					 manager->group_id_bmap,
-					 manager->group_id_num,
-					 desc->first_id, desc->size);
-	aipu_job_manager_owner_guard_end(manager);
-	return ret;
-}
-
-int aipu_job_manager_alloc_sflag_id(struct aipu_job_manager *manager,
-				    struct aipu_id_desc *desc,
-				    struct aipu_file_ctx *owner)
-{
-	u32 first;
-	int ret;
-
-	if (!manager || !desc)
-		return -EINVAL;
-	mutex_lock(&manager->op_lock);
-	ret = aipu_record_id_allocation(manager, AIPU_ID_SFLAG, owner,
-					manager->sflag_id_bmap,
-					manager->sflag_id_num, desc->size, 1,
-					&first);
-	mutex_unlock(&manager->op_lock);
-	if (ret)
-		desc->first_id = 0;
-	else
-		desc->first_id = first;
-
-	return ret;
-}
-
-int aipu_job_manager_free_sflag_id(struct aipu_job_manager *manager,
-				   struct aipu_id_desc *desc,
-				   struct aipu_file_ctx *owner)
-{
-	int ret;
-
-	if (!manager || !desc)
-		return -EINVAL;
-	ret = aipu_job_manager_owner_guard_begin(manager, owner);
-	if (ret)
-		return ret;
-	ret = aipu_release_id_allocation(manager, AIPU_ID_SFLAG, owner,
-					 manager->sflag_id_bmap,
-					 manager->sflag_id_num,
-					 desc->first_id, desc->size);
-	aipu_job_manager_owner_guard_end(manager);
-	return ret;
-}
-
 void aipu_job_manager_free_file_ids(struct aipu_job_manager *manager,
 				    struct aipu_file_ctx *owner)
 {
@@ -3534,20 +2647,8 @@ void aipu_job_manager_free_file_ids(struct aipu_job_manager *manager,
 	list_for_each_entry_safe(allocation, next, &manager->id_allocations, node) {
 		if (allocation->owner != owner)
 			continue;
-		switch (allocation->type) {
-		case AIPU_ID_GRID:
-			bitmap = manager->grid_id_bmap;
-			break;
-		case AIPU_ID_GROUP:
-			bitmap = manager->group_id_bmap;
-			break;
-		case AIPU_ID_SFLAG:
-			bitmap = manager->sflag_id_bmap;
-			break;
-		default:
-			bitmap = NULL;
-			break;
-		}
+		bitmap = allocation->type == AIPU_ID_GRID ?
+			manager->grid_id_bmap : NULL;
 		if (bitmap)
 			bitmap_clear(bitmap, allocation->first, allocation->size);
 		list_del(&allocation->node);
diff --git a/drivers/misc/armchina-npu/aipu_job_manager.h b/drivers/misc/armchina-npu/aipu_job_manager.h
--- a/drivers/misc/armchina-npu/aipu_job_manager.h
+++ b/drivers/misc/armchina-npu/aipu_job_manager.h
@@ -13,7 +13,6 @@
 #include "aipu_partition.h"
 #include "aipu_mm.h"
 
-#define AIPU_EXEC_ID (0x208ULL)
 struct aipu_file_ctx;
 
 enum aipu_state_kern {
@@ -59,7 +58,6 @@ struct job_irq_info {
 	u32 tag_id;
 	u32 tail_tcbp;
 	u32 sig_flag;
-	u32 group_id;
 	u64 tick_counter;
 };
 
@@ -200,10 +198,8 @@ struct command_pool {
  * @tec_intr_en:     is TEC interrupts enabled or not
  * @grid_id_bmap:    grid ID bitmap
  * @grid_id_num:     number of grid IDs available
- * @group_id_bmap:   group ID bitmap
- * @group_id_num:    number of group IDs available
- * @id_allocations:  owner-tagged grid/group/signal ID allocations
- * @id_lock:         lock to protect the grid Id & group IDs
+ * @id_allocations:  owner-tagged grid ID allocations
+ * @id_lock:         lock to protect grid IDs
  */
 struct aipu_job_manager {
 	int version;
@@ -237,13 +233,8 @@ struct aipu_job_manager {
 	bool tec_intr_en;
 	unsigned long *grid_id_bmap;
 	u32 grid_id_num;
-	unsigned long *group_id_bmap;
-	u32 group_id_num;
-	unsigned long *sflag_id_bmap;
-	u32 sflag_id_num;
 	struct list_head id_allocations;
-	u64 exec_id;
-	struct mutex id_lock; /* Protect grid & group IDs */
+	struct mutex id_lock; /* Protect grid IDs */
 };
 
 int init_aipu_job_manager(struct aipu_job_manager *manager, struct aipu_memory_manager *mm,
@@ -257,8 +248,6 @@ void aipu_job_manager_irq_upper_half(struct aipu_partition *core, int exception_
 				     struct job_irq_info *info);
 void aipu_job_manager_irq_bottom_half(struct aipu_partition *core);
 int aipu_job_manager_cancel_jobs(struct aipu_job_manager *manager, struct file *filp);
-int aipu_job_manager_invalidate_timeout_job(struct aipu_job_manager *manager, u32 job_id,
-						struct file *filp);
 int aipu_job_manager_take_job_status(struct aipu_job_manager *manager,
 				     struct aipu_job_status_query *job_status,
 				     struct aipu_job_status_snapshot *snapshot,
@@ -285,23 +274,8 @@ int aipu_job_manager_alloc_grid_id(struct aipu_job_manager *manager,
 				   struct aipu_file_ctx *owner);
 int aipu_job_manager_free_grid_id(struct aipu_job_manager *manager, u16 id,
 				  struct aipu_file_ctx *owner);
-int aipu_job_manager_alloc_group_id(struct aipu_job_manager *manager,
-				    struct aipu_id_desc *desc,
-				    struct aipu_file_ctx *owner);
-int aipu_job_manager_free_group_id(struct aipu_job_manager *manager,
-				   struct aipu_id_desc *desc,
-				   struct aipu_file_ctx *owner);
-int aipu_job_manager_alloc_sflag_id(struct aipu_job_manager *manager,
-				    struct aipu_id_desc *desc,
-				    struct aipu_file_ctx *owner);
-int aipu_job_manager_free_sflag_id(struct aipu_job_manager *manager,
-				   struct aipu_id_desc *desc,
-				   struct aipu_file_ctx *owner);
 void aipu_job_manager_free_file_ids(struct aipu_job_manager *manager,
 				    struct aipu_file_ctx *owner);
-
-int aipu_job_manager_alloc_exec_id(struct aipu_job_manager *manager,
-				  u64 *job_id);
 int aipu_job_manager_reset(struct aipu_job_manager *manager,
 			   struct aipu_partition *part, bool hardware);
 int aipu_job_manager_owner_guard_begin(struct aipu_job_manager *manager,
diff --git a/drivers/misc/armchina-npu/aipu_mm.c b/drivers/misc/armchina-npu/aipu_mm.c
--- a/drivers/misc/armchina-npu/aipu_mm.c
+++ b/drivers/misc/armchina-npu/aipu_mm.c
@@ -2,7 +2,6 @@
 /* Copyright (c) 2023-2025 Arm Technology (China) Co. Ltd. */
 
 #include <linux/types.h>
-#include <asm/cacheflush.h>
 #include <linux/mm.h>
 #include <linux/of.h>
 #include <linux/of_device.h>
@@ -20,7 +19,6 @@
 #include <linux/module.h>
 #include <linux/overflow.h>
 #include <linux/iova.h>
-#include <linux/dma-mapping.h>
 #include <linux/acpi.h>
 #include <linux/refcount.h>
 #include "config.h"
@@ -29,7 +27,6 @@
 #include "aipu_mm.h"
 #include "aipu_common.h"
 #include "aipu_dma_buf.h"
-#include "v2.h"
 
 #define EXIT_ENCODE_BUF_SIZE (1024)
 #define IOVA_FREE  false
@@ -66,16 +63,6 @@ struct iommu_dma_cookie {
 };
 #endif
 
-struct exporter_dma_buf {
-	void *vaddr;
-	dma_addr_t paddr;
-	size_t size;
-	struct dma_buf *dmabuf;
-	struct list_head list;
-	struct page **pages;
-	int fd;
-};
-
 struct aipu_vma_ctx {
 	struct aipu_memory_manager *mm;
 	struct aipu_file_ctx *owner;
@@ -94,10 +81,6 @@ static struct aipu_tcb_buf *aipu_mm_find_tbuf_in_region_no_lock(struct aipu_memo
 								u64 iova);
 static struct aipu_mem_region *aipu_mm_find_region(struct aipu_memory_manager *mm,
 						   u64 iova, char *log_str);
-static int init_cache_ops(struct aipu_memory_manager *mm);
-static void aipu_debug_print_buffers(struct aipu_memory_manager *mm);
-static int aipu_free_all_dma_iova_phy(struct aipu_memory_manager *mm, struct file *filp);
-static struct aipu_iova_buffer *aipu_get_iova_buffer(struct aipu_memory_manager *mm, u64 iova, char* str);
 static struct device *aipu_mm_create_child_dev(struct device *dev, u32 idx)
 {
 	struct device *child = NULL;
@@ -772,9 +755,8 @@ static struct aipu_mem_region *aipu_mm_create_region(struct aipu_memory_manager
 		}
 	}
 
-	if (!reserved && mm->has_iommu &&
-	    mm->version == AIPU_ISA_VERSION_ZHOUYI_V3 &&
-	    has_acpi_companion(mm->dev) && !aipu_acpi_prealloc_iova_regions) {
+	if (!reserved && mm->has_iommu && has_acpi_companion(mm->dev) &&
+	    !aipu_acpi_prealloc_iova_regions) {
 		reg->dma_sgt = dma_alloc_noncontiguous(reg->dev, reg->bytes,
 						   DMA_BIDIRECTIONAL,
 						   GFP_KERNEL | __GFP_ZERO,
@@ -1197,19 +1179,13 @@ static int aipu_mm_reserved_iova_for_never_map(struct aipu_memory_manager *mm, b
 		return -EINVAL;
 
 	iommu_domain = mm->iommu_domain;
-	if (mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-		iova_len = SZ_512M;
-
-		iovad = &mm->iova_domain;
-	} else {
-		if (!iommu_domain || !iommu_domain->iova_cookie) {
-			dev_warn(mm->dev, "IOMMU DMA cookie unavailable; skipping IOVA %s\n",
-				 flag ? "reservation" : "release");
-			return -ENODEV;
-		}
-		cookie = iommu_domain->iova_cookie;
-		iovad = &cookie->iovad;
+	if (!iommu_domain || !iommu_domain->iova_cookie) {
+		dev_warn(mm->dev, "IOMMU DMA cookie unavailable; skipping IOVA %s\n",
+			 flag ? "reservation" : "release");
+		return -ENODEV;
 	}
+	cookie = iommu_domain->iova_cookie;
+	iovad = &cookie->iovad;
 
 #if (KERNEL_VERSION(5, 5, 0) <= LINUX_VERSION_CODE)
 	bus_dma_limit = mm->dev->bus_dma_limit;
@@ -1239,21 +1215,13 @@ static int aipu_mm_add_iova_region(struct aipu_memory_manager *mm)
 	struct aipu_mem_region_obj *obj = NULL;
 	struct aipu_mem_region *reg = NULL;
 	int iova_region = 1;
-	unsigned long iova_region_size = 0xc0000000;
+	unsigned long iova_region_size = 0x3ffff000;
 	int asid_idx = 0;
 	int region_idx = 0;
 
 	if (!mm)
 		return -EINVAL;
 
-	if (mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0)
-		iova_region_size = 0xe0000000;
-
-#if defined(CONFIG_ARCH_CIX)
-	if (mm->version == AIPU_ISA_VERSION_ZHOUYI_V3)
-		iova_region_size = 0x3ffff000;
-#endif
-
 #if (KERNEL_VERSION(5, 5, 0) <= LINUX_VERSION_CODE)
 	mm->dev->bus_dma_limit = 0x800000000;
 #elif (KERNEL_VERSION(4, 19, 0) <= LINUX_VERSION_CODE)
@@ -1279,10 +1247,7 @@ static int aipu_mm_add_iova_region(struct aipu_memory_manager *mm)
 		reg = obj->reg;
 
 		list_add(&obj->list, &mm->mem.head->list);
-		if (mm->version > AIPU_ISA_VERSION_ZHOUYI_V1)
-			add_region_list(mm, asid_idx, reg);
-		else
-			add_region_list(mm, AIPU_BUF_ASID_0, reg);
+		add_region_list(mm, asid_idx, reg);
 		asid_idx++;
 		region_idx++;
 	} while (region_idx < iova_region);
@@ -1344,13 +1309,6 @@ static int aipu_mm_add_reserved_regions(struct aipu_memory_manager *mm)
 			continue;
 		}
 
-		if (type == AIPU_MEM_REGION_TYPE_DTCM &&
-		    mm->version == AIPU_ISA_VERSION_ZHOUYI_V1) {
-			dev_err(mm->dev, "dts: bypass because v1 does not support dtcm\n");
-			of_node_put(np);
-			continue;
-		}
-
 		ret = of_address_to_resource(np, 0, &res);
 		if (ret) {
 			of_node_put(np);
@@ -1359,12 +1317,6 @@ static int aipu_mm_add_reserved_regions(struct aipu_memory_manager *mm)
 		}
 
 		size = res.end - res.start + 1;
-		if (type == AIPU_MEM_REGION_TYPE_DTCM && size > ZHOUYI_V2_DTCM_MAX_BYTES) {
-			dev_dbg(mm->dev,
-				"the DTCM will be clipped to the maximum configurable value\n");
-			size = ZHOUYI_V2_DTCM_MAX_BYTES;
-		}
-
 		if (of_property_read_u64(np, "host-aipu-offset", &offset))
 			offset = 0;
 
@@ -1379,35 +1331,30 @@ static int aipu_mm_add_reserved_regions(struct aipu_memory_manager *mm)
 		list_add(&obj->list, &mm->mem.head->list);
 
 		/* get asid of this region */
-		if (mm->version > AIPU_ISA_VERSION_ZHOUYI_V1) {
-			for (asid_idx = 0; asid_idx < ZHOUYI_ASID_COUNT; asid_idx++) {
-				u32 asid = 0;
-
-				if (!of_property_read_u32_index(np, "asid", asid_idx, &asid)) {
-					if (asid >= ZHOUYI_ASID_COUNT) {
-						dev_err(mm->dev, "dts: invalid asid: %u\n", asid);
-						continue;
-					}
-
-					add_region_list(mm, asid, reg);
-					if (asid >= mm->valid_asid_cnt)
-						mm->valid_asid_cnt = asid + 1;
-
-					asid_set = true;
-				} else {
-					break;
+		for (asid_idx = 0; asid_idx < ZHOUYI_ASID_COUNT; asid_idx++) {
+			u32 asid = 0;
+
+			if (!of_property_read_u32_index(np, "asid", asid_idx, &asid)) {
+				if (asid >= ZHOUYI_ASID_COUNT) {
+					dev_err(mm->dev, "dts: invalid asid: %u\n", asid);
+					continue;
 				}
-			}
 
-			if (!asid_set) {
-				dev_err(mm->dev, "dts: reg asid is not set, use default asid\n");
-				mm->valid_asid_cnt = 2;
-				add_region_list(mm, AIPU_BUF_ASID_0, reg);
-				add_region_list(mm, AIPU_BUF_ASID_1, reg);
+				add_region_list(mm, asid, reg);
+				if (asid >= mm->valid_asid_cnt)
+					mm->valid_asid_cnt = asid + 1;
+
+				asid_set = true;
+			} else {
+				break;
 			}
-		} else {
-			/* use ASID 0 for V1 */
+		}
+
+		if (!asid_set) {
+			dev_err(mm->dev, "dts: reg asid is not set, use default asid\n");
+			mm->valid_asid_cnt = 2;
 			add_region_list(mm, AIPU_BUF_ASID_0, reg);
+			add_region_list(mm, AIPU_BUF_ASID_1, reg);
 		}
 
 		of_node_put(np);
@@ -1483,7 +1430,6 @@ int aipu_init_mm(struct aipu_memory_manager *mm, struct platform_device *p_dev,
 	int ret = 0;
 	struct iommu_group *group = NULL;
 	int asid = 0;
-	unsigned long order;
 
 	if (!mm || !p_dev)
 		return -EINVAL;
@@ -1543,22 +1489,13 @@ int aipu_init_mm(struct aipu_memory_manager *mm, struct platform_device *p_dev,
 		goto finish;
 
 	/* we accept at maximum 2 GM regions: for QoS fast & slow */
-	if (mm->version == AIPU_ISA_VERSION_ZHOUYI_V3)
-		mm->gm_max_cnt = 2;
-
-	/* currently, we only support one DTCM region in v2_2 */
-	if (mm->version == AIPU_ISA_VERSION_ZHOUYI_V2_2)
-		mm->dtcm_max_cnt = 1;
-
-	if (version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		mm->hold_tcb_head = kmem_cache_zalloc(mm->hold_tbuf_cache, GFP_KERNEL);
-		if (!mm->hold_tcb_head) {
-			ret = -ENOMEM;
-			goto finish;
-		}
-		INIT_LIST_HEAD(&mm->hold_tcb_head->node);
-		mm->hold_tcb_head->nums = 0;
+	mm->gm_max_cnt = 2;
+	mm->hold_tcb_head = kmem_cache_zalloc(mm->hold_tbuf_cache, GFP_KERNEL);
+	if (!mm->hold_tcb_head) {
+		ret = -ENOMEM;
+		goto finish;
 	}
+	INIT_LIST_HEAD(&mm->hold_tcb_head->node);
 	/**
 	 * Device tree binding for Zhouyi V3:
 	 *
@@ -1569,25 +1506,18 @@ int aipu_init_mm(struct aipu_memory_manager *mm, struct platform_device *p_dev,
 	 * 1: GM is shared by all tasks in 1 cluster (by default if this attribute is not provided)
 	 * 2: GM is divided half-by-half: for QoS slow & fast tasks, respectively
 	 */
-	if (version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
-		ret = of_property_read_u32(mm->dev->of_node, "gm-policy", &mm->gm_policy);
-		if (ret || mm->gm_policy > AIPU_GM_POLICY_HALF_DIVIDED) {
-			mm->gm_policy = AIPU_GM_POLICY_SHARED;
-			ret = 0;
-		}
-
-		if (IS_ERR(aipu_common_create_attr(mm->dev, &mm->gm_policy_attr, "gm_policy", 0444,
-						   aipu_gm_policy_sysfs_show, NULL))) {
-			mm->gm_policy_attr = NULL;
-			dev_err(mm->dev, "create gm_policy attr failed");
-		}
-
-		dev_dbg(mm->dev, "GM policy is %s",
-			mm->gm_policy == AIPU_GM_POLICY_SHARED ? "shared" : "half-by-half");
-	} else {
-		mm->gm_policy = AIPU_GM_POLICY_NONE;
+	ret = of_property_read_u32(mm->dev->of_node, "gm-policy", &mm->gm_policy);
+	if (ret || mm->gm_policy > AIPU_GM_POLICY_HALF_DIVIDED) {
+		mm->gm_policy = AIPU_GM_POLICY_SHARED;
+		ret = 0;
+	}
+	if (IS_ERR(aipu_common_create_attr(mm->dev, &mm->gm_policy_attr, "gm_policy", 0444,
+					   aipu_gm_policy_sysfs_show, NULL))) {
 		mm->gm_policy_attr = NULL;
+		dev_err(mm->dev, "create gm_policy attr failed");
 	}
+	dev_dbg(mm->dev, "GM policy is %s",
+		mm->gm_policy == AIPU_GM_POLICY_SHARED ? "shared" : "half-by-half");
 
 	group = iommu_group_get(mm->dev);
 	if (group) {
@@ -1597,34 +1527,7 @@ int aipu_init_mm(struct aipu_memory_manager *mm, struct platform_device *p_dev,
 			ret = -ENODEV;
 			goto put_iommu_group;
 		}
-		if (mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-			if (!mm->iommu_domain->pgsize_bitmap) {
-				dev_err(mm->dev, "IOMMU domain has no supported page sizes\n");
-				ret = -EINVAL;
-				goto put_iommu_group;
-			}
-			mm->valid_asid_cnt = 0;
-			mm->iova_base = 0x100000000ULL;   // start from 4GB
-			mm->iova_size = 0x800000000ULL;  // total szie 32GB
-
-			order = __ffs(mm->iommu_domain->pgsize_bitmap);
-#if (KERNEL_VERSION(4, 15, 0) <= LINUX_VERSION_CODE)
-			init_iova_domain(&mm->iova_domain, 1UL << order,
-					mm->iova_base >> PAGE_SHIFT);
-#else
-			init_iova_domain(&mm->iova_domain, 1UL << order,
-					mm->iova_base >> PAGE_SHIFT,
-					DMA_BIT_MASK(36)>>PAGE_SHIFT);
-#endif
-			mm->buffer_count = 0;
-			mutex_init(&mm->buffer_mutex);
-			INIT_LIST_HEAD(&mm->buffer_list);
-			mm->host_aipu_offset = 0;
-			init_cache_ops(mm);
-			mm->dma_mask = 36;
-		} else {
-			mm->valid_asid_cnt = 1;
-		}
+		mm->valid_asid_cnt = 1;
 		mm->has_iommu = true;
 	}
 put_iommu_group:
@@ -1646,16 +1549,11 @@ int aipu_init_mm(struct aipu_memory_manager *mm, struct platform_device *p_dev,
 		dev_dbg(mm->dev, "using DMA API allocation path on ACPI\n");
 	} else if (!mm->has_iommu) {
 		mm->res_cnt = aipu_mm_add_reserved_regions(mm);
-	}
-#if AIPU_USE_STANDARD_DMA_API_FOR_V3_2
-	else if (mm->version <= AIPU_ISA_VERSION_ZHOUYI_V3_2_0)
-		mm->res_cnt = aipu_mm_add_iova_region(mm);
-#else
-	else if (mm->version <= AIPU_ISA_VERSION_ZHOUYI_V3)
+	} else {
 		mm->res_cnt = aipu_mm_add_iova_region(mm);
-#endif
+	}
 
-	if (version > AIPU_ISA_VERSION_ZHOUYI_V1 && mm->res_cnt)
+	if (mm->res_cnt)
 		aipu_mm_set_asid_base(mm);
 
 	dev_info(mm->dev, "memory mode: %s, %s on %s; preallocated IOVA %s\n",
@@ -1702,11 +1600,9 @@ int aipu_deinit_mm(struct aipu_memory_manager *mm)
 
 	aipu_mm_release_sram_disables_no_lock(mm, NULL);
 
-	if (mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
-		if (mm->gm_policy_attr) {
-			aipu_common_destroy_attr(mm->dev, &mm->gm_policy_attr);
-			mm->gm_policy_attr = NULL;
-		}
+	if (mm->gm_policy_attr) {
+		aipu_common_destroy_attr(mm->dev, &mm->gm_policy_attr);
+		mm->gm_policy_attr = NULL;
 	}
 
 	if (mm->hold_tbuf_cache) {
@@ -1741,7 +1637,7 @@ int aipu_deinit_mm(struct aipu_memory_manager *mm)
 		}
 	}
 
-	if (mm->has_iommu && mm->version == AIPU_ISA_VERSION_ZHOUYI_V3)
+	if (mm->has_iommu)
 		aipu_mm_reserved_iova_for_never_map(mm, IOVA_FREE);
 	mm->res_cnt = 0;
 	if (mm->obj_cache)
@@ -1753,8 +1649,6 @@ int aipu_deinit_mm(struct aipu_memory_manager *mm)
 	if (mm->tbuf_cache)
 		kmem_cache_destroy(mm->tbuf_cache);
 	mm->tbuf_cache = NULL;
-	if (mm->has_iommu && mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0)
-		put_iova_domain(&mm->iova_domain);
 	mm->iommu_domain = NULL;
 	return 0;
 }
@@ -1807,36 +1701,26 @@ static bool aipu_mm_check_address_validity(struct aipu_memory_manager *mm,
 	u64 asid0_base = 0;
 	u64 size_4G = 0x100000000ULL;
 
-	if (mm->version == AIPU_ISA_VERSION_ZHOUYI_V3) {
-		remainder = desc->pa % size_4G;
-		if ((remainder + desc->bytes) >= size_4G)
-			return false;
-	}
+	remainder = desc->pa % size_4G;
+	if ((remainder + desc->bytes) >= size_4G)
+		return false;
 
 	asid_size = aipu_mm_get_asid_size(mm, desc->asid);
 	if (asid_size <= SZ_2G)
 		return true;
 
 	asid_base = aipu_mm_get_asid_base(mm, desc->asid);
-	if (mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
-		lower_bound = asid_base + size_4G - SZ_1G;
-		upper_bound = asid_base + size_4G;
-
-		if (mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
+	lower_bound = asid_base + size_4G - SZ_1G;
+	upper_bound = asid_base + size_4G;
+	if (desc->asid > AIPU_BUF_ASID_0) {
+		asid0_base = aipu_mm_get_asid_base(mm, AIPU_BUF_ASID_0);
+		if (asid0_base != asid_base)
 			lower_bound += SZ_512M;
-		} else {
-			if (desc->asid > AIPU_BUF_ASID_0) {
-				asid0_base = aipu_mm_get_asid_base(mm, AIPU_BUF_ASID_0);
-				if (asid0_base != asid_base)
-					lower_bound += SZ_512M;
-			}
-		}
-
-		if (check_add_overflow(desc->pa, desc->bytes, &desc_end))
-			return false;
-		if (desc->pa < upper_bound && desc_end > lower_bound)
-			return false;
 	}
+	if (check_add_overflow(desc->pa, desc->bytes, &desc_end))
+		return false;
+	if (desc->pa < upper_bound && desc_end > lower_bound)
+		return false;
 
 	return true;
 }
@@ -1873,9 +1757,6 @@ int aipu_mm_alloc(struct aipu_memory_manager *mm, struct aipu_buf_request *buf_r
 	}
 	alignment = (u64)buf_req->align_in_page << PAGE_SHIFT;
 
-	if (mm->version == AIPU_ISA_VERSION_ZHOUYI_V1)
-		buf_req->asid = AIPU_BUF_ASID_0;
-
 	if (buf_req->region < AIPU_MEM_REGION_TYPE_MAX) {
 		type = buf_req->region;
 	} else {
@@ -1884,12 +1765,7 @@ int aipu_mm_alloc(struct aipu_memory_manager *mm, struct aipu_buf_request *buf_r
 		return -EINVAL;
 	}
 
-	/* fall back to SRAM if DTCM/GM is not applicable for certain archs */
-	if (mm->version < AIPU_ISA_VERSION_ZHOUYI_V2_2 && type == AIPU_MEM_REGION_TYPE_DTCM)
-		type = AIPU_MEM_REGION_TYPE_SRAM;
-
-	if ((mm->version < AIPU_ISA_VERSION_ZHOUYI_V3 || mm->gm_policy == AIPU_GM_POLICY_NONE) &&
-	    type == AIPU_MEM_REGION_TYPE_GM)
+	if (mm->gm_policy == AIPU_GM_POLICY_NONE && type == AIPU_MEM_REGION_TYPE_GM)
 		type = AIPU_MEM_REGION_TYPE_SRAM;
 
 	/* Support the smmu dynamic weight buffer allocation. */
@@ -1947,12 +1823,10 @@ int aipu_mm_alloc(struct aipu_memory_manager *mm, struct aipu_buf_request *buf_r
 		spin_unlock_irqrestore(&mm->slock, flags);
 	}
 
-	if (mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3) {
-		if (!aipu_mm_check_address_validity(mm, &buf_req->desc)) {
-			aipu_mm_free(mm, &buf_req->desc, filp, true);
-			allocated = false;
-			ret = -ENOMEM;
-		}
+	if (!aipu_mm_check_address_validity(mm, &buf_req->desc)) {
+		aipu_mm_free(mm, &buf_req->desc, filp, true);
+		allocated = false;
+		ret = -ENOMEM;
 	}
 
 	if (!allocated) {
@@ -2267,8 +2141,6 @@ void aipu_mm_free_buffers(struct aipu_memory_manager *mm, struct file *filp)
 
 	if (!mm || !owner)
 		return;
-	if (mm->version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0 && mm->has_iommu)
-		aipu_free_all_dma_iova_phy(mm, filp);
 	aipu_mm_free_owner_buffers(mm, owner);
 }
 
@@ -2929,1027 +2801,25 @@ void get_dtcm(struct aipu_memory_manager *mm, u64 *base, u32 *size)
 	*size = 0;
 }
 
-static void aipu_debug_print_buffers(struct aipu_memory_manager *mm)
-{
-	struct aipu_iova_buffer *buffer;
-	struct aipu_phy_block *block;
-	int count = 0, index = 0;
-
-	if (!mm)
-		return;
-	dev_dbg(mm->dev, "buffer dump begin\n");
-
-	//mutex_lock(&mm->buffer_mutex);
-	dev_dbg(mm->dev, "total buffers: %u\n", mm->buffer_count);
-	list_for_each_entry(buffer, &mm->buffer_list, node) {
-		dev_dbg(mm->dev, "buffer %d: exec id 0x%llx IOVA=0x%llx, size=0x%llx, allocated=0x%llx ref_count %d\n",
-			count++, buffer->exec_id, buffer->iova_start, buffer->iova_size, buffer->allocated_size, buffer->ref_count);
-		index = 0;
-		list_for_each_entry(block, &buffer->phy_blocks, list) {
-			if (block) {
-				dev_dbg(mm->dev, "block %d: IOVA=0x%llx, size=0x%llx\n",
-					index++, block->iova_start, block->size);
-			}
-		}
-	}
-	//mutex_unlock(&mm->buffer_mutex);
-	dev_dbg(mm->dev, "buffer dump end\n");
-	return;
-}
-static inline size_t get_cache_line_size(void)
-{
-#ifdef CONFIG_ARM64
-	unsigned long cache_line_size;
-	asm volatile("mrs %0, ctr_el0" : "=r" (cache_line_size));
-	cache_line_size = 4 << ((cache_line_size >> 16) & 0xf);
-		return cache_line_size;
-#elif defined(CONFIG_ARM)
-	return L1_CACHE_BYTES;
-#elif defined(CONFIG_X86_64)
-	return boot_cpu_data.x86_cache_alignment;
-#elif defined(CONFIG_RISCV)
-	return L1_CACHE_BYTES;
-#else
-	return L1_CACHE_BYTES;
-#endif
-}
-
-// For ARM64 ISA
-#ifdef CONFIG_ARM64
-static void arm64_flush_cache_range(void *vaddr, size_t size)
-{
-	unsigned long start = (unsigned long)vaddr;
-	unsigned long end = start + size;
-	unsigned long cache_line_size = get_cache_line_size();
-	unsigned long addr;
-
-	//pr_info("cache_line_size %ld\n", cache_line_size);
-	start &= ~(cache_line_size - 1);
-	end = (end + cache_line_size - 1) & ~(cache_line_size - 1);
-
-	for (addr = start; addr < end; addr += cache_line_size) {
-		asm volatile("dc civac, %0" : : "r" (addr) : "memory");
-	}
-	asm volatile("dsb sy" ::: "memory");
-}
-
-static void arm64_clean_cache_range(void *vaddr, size_t size)
-{
-	unsigned long start = (unsigned long)vaddr;
-	unsigned long end = start + size;
-	unsigned long cache_line_size = get_cache_line_size();
-	unsigned long addr;
-
-	start &= ~(cache_line_size - 1);
-	end = (end + cache_line_size - 1) & ~(cache_line_size - 1);
-
-	for (addr = start; addr < end; addr += cache_line_size) {
-		asm volatile("dc cvac, %0" : : "r" (addr) : "memory");
-	}
-	asm volatile("dsb sy" ::: "memory");
-}
-
-static void arm64_invalidate_cache_range(void *vaddr, size_t size)
-{
-	unsigned long start = (unsigned long)vaddr;
-	unsigned long end = start + size;
-	unsigned long cache_line_size = get_cache_line_size();
-	unsigned long addr;
-
-	//pr_info("cache_line_size %ld\n", cache_line_size);
-	start &= ~(cache_line_size - 1);
-	end = (end + cache_line_size - 1) & ~(cache_line_size - 1);
-
-	for (addr = start; addr < end; addr += cache_line_size) {
-		asm volatile("dc ivac, %0" : : "r" (addr) : "memory");
-	}
-	asm volatile("dsb sy" ::: "memory");
-}
-
-// For ARM32 ISA
-#elif defined(CONFIG_ARM)
-static void arm_flush_cache_range(void *vaddr, size_t size)
-{
-	unsigned long start = (unsigned long)vaddr;
-	unsigned long end = start + size;
-
-	__cpuc_flush_dcache_area(vaddr, size);
-	outer_flush_range(__pa(start), __pa(end));
-}
-
-static void arm_clean_cache_range(void *vaddr, size_t size)
-{
-	unsigned long start = (unsigned long)vaddr;
-	unsigned long end = start + size;
-
-	__cpuc_clean_dcache_area(vaddr, size);
-	outer_clean_range(__pa(start), __pa(end));
-}
-
-static void arm_invalidate_cache_range(void *vaddr, size_t size)
-{
-	__cpuc_flush_dcache_area(vaddr, size);
-	outer_inv_range(__pa(vaddr), __pa(vaddr) + size);
-}
-
-// For x86_64 ISA
-#elif defined(CONFIG_X86_64)
-static void x86_flush_cache_range(void *vaddr, size_t size)
-{
-	clflush_cache_range(vaddr, size);
-}
-
-static void x86_clean_cache_range(void *vaddr, size_t size)
+int aipu_alloc_dma_iova_phy(struct aipu_memory_manager *mm,
+			    struct aipu_buf_request *buf_req, struct file *filp)
 {
-	clflush_cache_range(vaddr, size);
-}
-
-static void x86_invalidate_cache_range(void *vaddr, size_t size)
-{
-	clflush_cache_range(vaddr, size);
-}
-
-// For RISC-V ISA
-#elif defined(CONFIG_RISCV)
-static void riscv_flush_cache_range(void *vaddr, size_t size)
-{
-	flush_icache_range((unsigned long)vaddr, (unsigned long)vaddr + size);
-}
-
-static void riscv_clean_cache_range(void *vaddr, size_t size)
-{
-	flush_icache_range((unsigned long)vaddr, (unsigned long)vaddr + size);
-}
-
-static void riscv_invalidate_cache_range(void *vaddr, size_t size)
-{
-	flush_icache_range((unsigned long)vaddr, (unsigned long)vaddr + size);
-}
-#else
-	#error "Unsupported architecture."
-#endif
-
-// Initialize cache operation pointer
-static int __init init_cache_ops(struct aipu_memory_manager *mm)
-{
-	struct __cache_ops *cache_ops = &mm->cache_ops;
-	cache_ops->cache_line_size = get_cache_line_size();
-
-#ifdef CONFIG_ARM64
-	cache_ops->flush_range = arm64_flush_cache_range;
-	cache_ops->clean_range = arm64_clean_cache_range;
-	cache_ops->invalidate_range = arm64_invalidate_cache_range;
-#elif defined(CONFIG_ARM)
-	cache_ops->flush_range = arm_flush_cache_range;
-	cache_ops->clean_range = arm_clean_cache_range;
-	cache_ops->invalidate_range = arm_invalidate_cache_range;
-#elif defined(CONFIG_X86_64)
-	cache_ops->flush_range = x86_flush_cache_range;
-	cache_ops->clean_range = x86_clean_cache_range;
-	cache_ops->invalidate_range = x86_invalidate_cache_range;
-#elif defined(CONFIG_RISCV)
-	cache_ops->flush_range = riscv_flush_cache_range;
-	cache_ops->clean_range = riscv_clean_cache_range;
-	cache_ops->invalidate_range = riscv_invalidate_cache_range;
-#else
-	#error "Unsupported architecture"
-#endif
-
-	return 0;
-}
-//For external api
-void aipu_cache_flush(struct aipu_memory_manager *mm, void *vaddr, size_t size)
-{
-	if (!mm || !vaddr || !size)
-		return;
-
-	if(mm->cache_ops.flush_range)
-		mm->cache_ops.flush_range(vaddr, size);
-	return;
-}
-
-void aipu_cache_clean(struct aipu_memory_manager *mm, void *vaddr, size_t size)
-{
-	if (!mm || !vaddr || !size)
-		return;
-
-	if(mm->cache_ops.clean_range)
-			mm->cache_ops.clean_range(vaddr, size);
-	return;
-}
-
-void aipu_cache_invalidate(struct aipu_memory_manager *mm, void *vaddr, size_t size)
-{
-	if (!mm || !vaddr || !size)
-		return;
-
-	if(mm->cache_ops.invalidate_range)
-		mm->cache_ops.invalidate_range(vaddr, size);
-	return;
-}
-
-static struct aipu_iova_buffer *aipu_get_iova_buffer(struct aipu_memory_manager *mm, u64 iova, char* str)
-{
-	//struct rb_node *node = mm->buffer_tree.rb_node;
-	struct aipu_iova_buffer *buffer = NULL;
-
-	if (!mm) {
-		dev_err(mm->dev, "Invalid parameter to get iova buffer %s.\n", str);
-		return NULL;
-	}
-
-	//pr_info("%s iova 0x%llx +\n", __FUNCTION__, iova);
-	mutex_lock(&mm->buffer_mutex);
-	list_for_each_entry(buffer, &mm->buffer_list, node) {
-		if ((iova - buffer->iova_start) < buffer->iova_size) {
-			mutex_unlock(&mm->buffer_mutex);
-			return buffer;
-		}
-	}
-	dev_err(mm->dev, "tid %d pid %d can't find iova 0x%llx %s\n",
-		task_tgid_nr(current), task_pid_nr(current), iova, str);
-	aipu_debug_print_buffers(mm);
-	mutex_unlock(&mm->buffer_mutex);
-	return NULL;
-}
-
-struct aipu_phy_block *aipu_get_block_buffer(struct aipu_memory_manager *mm, u64 iova, char* str)
-{
-	struct aipu_iova_buffer *buffer;
-	struct aipu_phy_block *block;
-	bool find = false;
-
-	if (!mm) {
-		dev_err(mm->dev, "Invalid parameter to get iova buffer %s.\n", str);
-		return NULL;
-	}
-
-	mutex_lock(&mm->buffer_mutex);
-	list_for_each_entry(buffer, &mm->buffer_list, node) {
-		if ((iova - buffer->iova_start) < buffer->iova_size) {
-			find = true;
-			break;
-		}
-	}
-	mutex_unlock(&mm->buffer_mutex);
-
-	if (find) {
-		list_for_each_entry(block, &buffer->phy_blocks, list) {
-			if (block) {
-				if ((iova - block->iova_start) < block->size)
-					return block;
-			}
-		}
-	}
-
-	return NULL;
-}
-
-struct aipu_iova_buffer *aipu_get_iova_buffer_by_exec_id(struct aipu_memory_manager *mm,
-						u64 exec_id, char* str)
-{
-	struct aipu_iova_buffer *buffer = NULL;
-
-	if (!mm) {
-		dev_err(mm->dev, "Invalid parameter to get iova buffer %s.\n", str);
-		return NULL;
-	}
-
-	mutex_lock(&mm->buffer_mutex);
-	list_for_each_entry(buffer, &mm->buffer_list, node) {
-		if (buffer->exec_id == exec_id) {
-			mutex_unlock(&mm->buffer_mutex);
-			return buffer;
-		}
-	}
-	dev_err(mm->dev, "tid %d pid %d can't find exec_id 0x%llx %s\n",
-		 task_tgid_nr(current), task_pid_nr(current), exec_id, str);
-	mutex_unlock(&mm->buffer_mutex);
-	return NULL;
-}
-
-static int aipu_insert_buffer_safe(struct aipu_memory_manager *mm,
-                                   struct aipu_iova_buffer *new_buffer)
-{
-	if (!mm || !new_buffer) {
-		if (mm && mm->dev)
-		dev_err(mm->dev, "Invalid parameters for buffer insertion\n");
-		return -EINVAL;
-	}
-
-	mutex_lock(&mm->buffer_mutex);
-	list_add_tail(&new_buffer->node, &mm->buffer_list);
-	mm->buffer_count++;
-	mutex_unlock(&mm->buffer_mutex);
-	//dev_info(mm->dev, "Successfully inserted buffer: IOVA=0x%llx\n",
-	//	new_buffer->iova_start);
-
-	return 0;
-}
-
-static struct aipu_iova_buffer* aipu_alloc_iova(struct aipu_memory_manager *mm,
-						struct aipu_buf_request *buf_req,
-						struct aipu_file_ctx *owner)
-{
-	struct iova_domain *iovad = &mm->iova_domain;
-	struct aipu_iova_buffer* buffer = NULL;
-	unsigned long pfn_hi;
-	u64 reserved_iova;
-	u64 iova_addr;
-	u64 iova_size = 0;
-	struct iova *iova;
-
-	//Parameter check
-	iova_size = ALIGN(buf_req->bytes, PAGE_SIZE);
-	reserved_iova = ALIGN(buf_req->reserve_iova_size, PAGE_SIZE);
-	pfn_hi = (mm->iova_base + mm->iova_size - 1) >> PAGE_SHIFT;
-
-	if ((iova_size + reserved_iova) > mm->iova_size ||
-	    (iova_size + reserved_iova) > (3UL * SZ_1G + SZ_512M)) {
-		dev_err(mm->dev, "iova_size is larger than total size or 3.5GB.\n");
-		return NULL;
-	}
-
-	buffer = kzalloc(sizeof(*buffer), GFP_KERNEL);
-	if (!buffer)
-		return NULL;
-
-	iova = alloc_iova(iovad, (iova_size + reserved_iova) >> PAGE_SHIFT, pfn_hi, true);
-	if (!iova) {
-		mutex_lock(&mm->buffer_mutex);
-		aipu_debug_print_buffers(mm);
-		mutex_unlock(&mm->buffer_mutex);
-		kfree(buffer);
-		dev_err(mm->dev, "Failed to allocate IOVA\n");
-		return NULL;
-	}
-
-	// init iova buffer
-	iova_addr = iova->pfn_lo << PAGE_SHIFT;
-	buffer->iova_start = iova_addr;
-	buffer->iova_size = iova_size + reserved_iova;
-	buffer->allocated_size = 0;
-	buffer->region = buf_req->region;
-	buffer->asid = buf_req->asid;
-	buffer->ref_count = 0;
-	buffer->owner = owner;
-	buffer->tgid = task_tgid_nr(current);
-	buffer->pid = task_pid_nr(current);
-	buffer->exec_id = buf_req->exec_id;
-	INIT_LIST_HEAD(&buffer->phy_blocks);
-	mutex_init(&buffer->phy_mutex);
-
-	if(aipu_insert_buffer_safe(mm, buffer)){
-		dev_err(mm->dev, "Failed to insert IOVA buffer.\n");
-		free_iova(iovad, iova->pfn_lo);
-		kfree(buffer);
-		return NULL;
-	}
-
-	buf_req->desc.pa = iova_addr - mm->host_aipu_offset;
-	buf_req->desc.region = AIPU_BUF_REGION_DEFAULT;
-	buf_req->desc.dev_offset = iova_addr - mm->host_aipu_offset;
-	buf_req->desc.asid = buf_req->asid;
-	buf_req->desc.bytes = (iova->pfn_hi - iova->pfn_lo + 1) << PAGE_SHIFT;
-	buf_req->desc.exec_id = buf_req->exec_id;
-	//dev_info(mm->dev, "Allocated IOVA: 0x%llx, size: 0x%llx\n", iova_addr, buf_req->desc.bytes);
-
-	return buffer;
-}
-
-static void aipu_free_iova(struct aipu_memory_manager *mm, struct aipu_iova_buffer* buffer)
-{
-	unsigned long pfn_lo = (buffer->iova_start + mm->host_aipu_offset) >> PAGE_SHIFT;
-
-	mutex_lock(&mm->buffer_mutex);
-	if (buffer->ref_count) {
-		dev_warn_ratelimited(mm->dev,
-				     "freeing buffer 0x%llx with ref_count %d\n",
-				     buffer->iova_start, buffer->ref_count);
-	}
-
-	list_del(&buffer->node);
-	mm->buffer_count--;
-	free_iova(&mm->iova_domain, pfn_lo);
-	kfree(buffer);
-	mutex_unlock(&mm->buffer_mutex);
-	//dev_info(mm->dev, "Freed IOVA: 0x%llx, size: 0x%llx ref_count %d\n", iova_addr, size, buffer->ref_count);
-}
-
-static void aipu_dma_free_pages(struct page **pages, int count)
-{
-	if (!pages)
-		return;
-
-	while (count--)
-		__free_page(pages[count]);
-	kvfree(pages);
-}
-
-static struct page **aipu_dma_alloc_pages(struct device *dev,
-		unsigned int count, unsigned long order_mask, gfp_t gfp)
-{
-	struct page **pages;
-	unsigned int i = 0, nid = dev_to_node(dev);
-
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 8, 0)
-	order_mask &= (2U << MAX_PAGE_ORDER) - 1;
-#else
-	order_mask &= (2U << MAX_ORDER) - 1;
-#endif
-	if (!order_mask)
-		return NULL;
-
-	pages = kvzalloc(count * sizeof(*pages), GFP_KERNEL);
-	if (!pages)
-		return NULL;
-
-	/* IOMMU can map any pages, so himem can also be used here */
-	gfp |= __GFP_NOWARN | GFP_KERNEL;
-
-	/* It makes no sense to muck about with huge pages */
-	gfp &= ~__GFP_COMP;
-
-	while (count) {
-		struct page *page = NULL;
-		unsigned int order_size;
-
-		/*
-		 * Higher-order allocations are a convenience rather
-		 * than a necessity, hence using __GFP_NORETRY until
-		 * falling back to minimum-order allocations.
-		 */
-		for (order_mask &= (2U << __fls(count)) - 1;
-		     order_mask; order_mask &= ~order_size) {
-			unsigned int order = __fls(order_mask);
-			gfp_t alloc_flags = gfp;
-
-			order_size = 1U << order;
-			if (order_mask > order_size)
-				alloc_flags |= __GFP_NORETRY;
-
-			page = alloc_pages_node(nid, alloc_flags, order);
-			if (!page)
-				continue;
-			if (order)
-				split_page(page, order);
-			break;
-		}
-		if (!page) {
-			aipu_dma_free_pages(pages, i);
-			return NULL;
-		}
-		count -= order_size;
-		while (order_size--)
-			pages[i++] = page++;
-	}
-
-	return pages;
-}
-
-static int aipu_add_block_into_buffer(struct aipu_iova_buffer *buffer, struct aipu_phy_block *block)
-{
-	struct aipu_dma_buf_priv *priv;
-	if (!buffer || !block)
-		return -EINVAL;
-
-	mutex_lock(&buffer->phy_mutex);
-	buffer->ref_count++;
-	block->iova_start = buffer->iova_start + buffer->allocated_size;
-	buffer->allocated_size += block->size;
-	if (block->dmabuf) {
-		priv = (struct aipu_dma_buf_priv *)block->dmabuf->priv;
-		priv->dev_pa = block->iova_start;
-		priv->dma_pa = block->iova_start;
-		priv->bytes = block->size;
-	}
-	list_add_tail(&block->list, &buffer->phy_blocks);
-	mutex_unlock(&buffer->phy_mutex);
-
-	return 0;
-}
-
-static struct aipu_phy_block *aipu_remove_block_node(struct aipu_iova_buffer *buffer, u64 pa)
-{
-	struct aipu_phy_block *block = NULL;
-	struct aipu_phy_block *tmp;
-
-	if (!buffer)
-		return NULL;
-
-	mutex_lock(&buffer->phy_mutex);
-	list_for_each_entry_safe(block, tmp, &buffer->phy_blocks, list) {
-		if (block->iova_start == pa) {
-			list_del(&block->list);
-			buffer->ref_count--;
-			buffer->allocated_size -= block->size;
-			mutex_unlock(&buffer->phy_mutex);
-			return block;
-		}
-	}
-	mutex_unlock(&buffer->phy_mutex);
-
-	return NULL;
-}
-
-static struct aipu_phy_block *aipu_alloc_phy_block(struct aipu_memory_manager *mm,
-						   struct aipu_iova_buffer *buffer,
-						   struct aipu_buf_request *buf_req)
-{
-	struct aipu_phy_block *block;
-	u32 page_count;
-	u64 mem_size = 0;
-	u64 iova_addr = 0;
-	unsigned int min_size;
-	unsigned int alloc_sizes = mm->iommu_domain->pgsize_bitmap;
-
-	iova_addr = buffer->iova_start + buffer->allocated_size;
-	mem_size = ALIGN(buf_req->bytes, PAGE_SIZE);
-	page_count = mem_size >> PAGE_SHIFT;
-
-	min_size = alloc_sizes & -alloc_sizes;
-	if (min_size < PAGE_SIZE)
-		alloc_sizes |= PAGE_SIZE;
-
-	if ((buffer->allocated_size + buf_req->bytes) > buffer->iova_size) {
-		dev_err(mm->dev, "iova space is not enough for phy memory allcation.");
-		return NULL;
-	}
-
-	block = kzalloc(sizeof(*block), GFP_KERNEL);
-	if (!block)
-		return NULL;
-
-	// alloc physical pages
-	block->pages = aipu_dma_alloc_pages(mm->dev, page_count, alloc_sizes>>PAGE_SHIFT, GFP_KERNEL | __GFP_ZERO);
-	if (!block->pages) {
-		dev_err(mm->dev, "Failed to allocate %u pages\n", page_count);
-		kfree(block);
-		return NULL;
-	}
-
-	//block->iova_start = iova_addr;
-	block->size = mem_size;
-	block->page_count = page_count;
-	block->sgt.nents = block->page_count;
-	block->dmabuf = NULL;
-	INIT_LIST_HEAD(&block->list);
-	block->bind_memory = false;
-
-	//Add block into buffer's list
-	aipu_add_block_into_buffer(buffer, block);
-
-	//back map pa and size
-	buf_req->desc.pa = iova_addr;
-	buf_req->desc.dev_offset = iova_addr;
-	buf_req->desc.bytes = mem_size;
-
-	//dev_info(mm->dev, "Allocated phy block: iova=0x%llx size=0x%llx\n",
-	//	iova_addr, mem_size);
-
-	return block;
-}
-
-static void aipu_free_phy_block(struct aipu_memory_manager *mm, struct aipu_phy_block *block)
-{
-	if (block) {
-		vunmap(block->va);
-		if(!block->bind_memory)
-			aipu_dma_free_pages(block->pages, block->page_count);
-		kfree(block);
-	}
-}
-
-static int aipu_map_sg_to_iommu(struct aipu_memory_manager *mm,
-				struct aipu_phy_block *block)
-{
-	size_t mapped_size;
-
-	if (!mm || !block)
-		return -EINVAL;
-
-	// build scatter-gather table
-	if (sg_alloc_table_from_pages(&block->sgt, block->pages, block->page_count, 0, block->size, GFP_KERNEL)) {
-		dev_err(mm->dev, "Failed to allocate sg table from pages.\n");
-		return -EINVAL;
-	}
-
-	// map to iommu
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 4, 0)
-	mapped_size = iommu_map_sg(mm->iommu_domain, block->iova_start,
-				   block->sgt.sgl, block->sgt.nents,
-				   IOMMU_READ | IOMMU_WRITE, GFP_KERNEL);
-#else
-	mapped_size = iommu_map_sg(mm->iommu_domain, block->iova_start,
-				   block->sgt.sgl, block->sgt.nents,
-				   IOMMU_READ | IOMMU_WRITE);
-#endif
-	if (mapped_size < block->size) {
-		dev_err(mm->dev, "IOMMU mapping failed: mapped=0x%zx iova 0x%llx page count 0x%x\n",
-			mapped_size, block->iova_start, block->sgt.nents);
-		sg_free_table(&block->sgt);
-		return -ENOMEM;
-	}
-
-	block->va = vmap(block->pages, block->page_count, VM_MAP, pgprot_noncached(PAGE_KERNEL));
-	sg_free_table(&block->sgt);
-	//dev_info(mm->dev, "Mapped 0x%llx bytes to IOVA 0x%llx\n",
-	//	block->size, block->iova_start);
-	return 0;
-}
-
-int aipu_copy_block_by_dma_fd(struct aipu_memory_manager *mm,
-			      struct aipu_dma_buf *dmabuf_info)
-{
-	struct dma_buf *dmabuf = NULL;
-	struct aipu_dma_buf_priv *priv = NULL;
-	struct aipu_iova_buffer* buffer;
-	struct aipu_phy_block *block, *new;
-	int ret = 0;
-
-	if (!mm || !dmabuf_info)
-		return -EINVAL;
-
-	dmabuf = dma_buf_get(dmabuf_info->fd);
-	if (IS_ERR(dmabuf))
-		return PTR_ERR(dmabuf);
-
-	priv = (struct aipu_dma_buf_priv *)dmabuf->priv;
-	dma_buf_put(dmabuf);
-	block = aipu_get_block_buffer(mm, priv->dev_pa, "attach get block");
-	if (!block) {
-		return -EINVAL;
-	}
-
-	buffer = aipu_get_iova_buffer_by_exec_id(mm, dmabuf_info->exec_id, "dma block malloc iova");
-	if(!buffer) {
-		return -EINVAL;
-	}
-
-	new = kzalloc(sizeof(*new), GFP_KERNEL);
-	if (!new) {
-		return -ENOMEM;
-	}
-	new->pages = block->pages;
-	new->size = block->size;
-	new->page_count = block->page_count;
-	new->sgt.nents = block->page_count;
-	new->dmabuf = NULL;
-	INIT_LIST_HEAD(&new->list);
-	new->bind_memory = true;
-
-	//Add block into buffer's list
-	aipu_add_block_into_buffer(buffer, new);
-
-	//map new iova
-	ret = aipu_map_sg_to_iommu(mm, new);
-	if (ret) {
-		dev_err(mm->dev, "Map block iova 0x%llx size 0x%llx FAILED.\n", new->iova_start, new->size);
-		aipu_remove_block_node(buffer, new->iova_start);
-		kfree(new);
-		return -ENOMEM;
-	}
-	dmabuf_info->pa = new->iova_start;
-	dmabuf_info->bytes = new->size;
-
-	return ret;
-}
-
-int aipu_alloc_dma_iova_phy(struct aipu_memory_manager *mm, struct aipu_buf_request *buf_req,
-		  struct file *filp)
-{
-	struct aipu_file_ctx *owner = aipu_file_ctx(filp);
-	struct aipu_iova_buffer *buffer = NULL;
-	struct aipu_phy_block *block = NULL;
-	int ret = 0;
-
 	if (!mm || !buf_req)
 		return -EINVAL;
+	if (buf_req->alloc_mode != AIPU_DMA_BUF_MALLOC_DEFAULT ||
+	    buf_req->exec_id || buf_req->reserve_iova_size)
+		return -EOPNOTSUPP;
 
-	switch (buf_req->alloc_mode) {
-	case AIPU_DMA_BUF_MALLOC_DEFAULT:
-		//support standard dma api and reserved memory
-		ret = aipu_mm_alloc(mm, buf_req, filp);
-		break;
-
-	case AIPU_DMA_BUF_MALLOC_IOVA:
-		// alloc iova adress only
-		buffer = aipu_alloc_iova(mm, buf_req, owner);
-		if (!buffer) {
-			dev_err(mm->dev, "Malloc iova FAILED size 0x%llx\n", buf_req->bytes + buf_req->reserve_iova_size);
-			ret = -ENOMEM;
-			goto OUT;
-		}
-		break;
-
-	case AIPU_DMA_BUF_MALLOC_PHY:
-		// alloc phy memory and map allocated iova address
-		buffer = aipu_get_iova_buffer_by_exec_id(mm, buf_req->exec_id, "malloc phy");
-		if (!buffer) {
-			dev_err(mm->dev, "Can't find pa 0x%llx in iova domain.\n", buf_req->exec_id);
-			ret = -EINVAL;
-			goto OUT;
-		}
-
-		block = aipu_alloc_phy_block(mm, buffer, buf_req);
-		if (!block) {
-			dev_err(mm->dev, "OOM iova 0x%llx phy size 0x%llx FAILED.\n", buf_req->exec_id, buf_req->bytes);
-			ret = -ENOMEM;
-			goto OUT;
-		}
-
-		//map sg table to iommu
-		ret = aipu_map_sg_to_iommu(mm, block);
-		if (ret) {
-			dev_err(mm->dev, "Map block iova 0x%llx size 0x%llx FAILED.\n", block->iova_start, block->size);
-			aipu_free_phy_block(mm, block);
-			goto OUT;
-		}
-		aipu_cache_invalidate(mm, block->va, block->size);
-		break;
-
-	case AIPU_DMA_BUF_MALLOC_BOTH:
-		buffer = aipu_alloc_iova(mm, buf_req, owner);
-		if (!buffer) {
-			dev_err(mm->dev, "Malloc iova size 0x%llx FAILED.\n",  buf_req->bytes + buf_req->reserve_iova_size);
-			ret = -ENOMEM;
-			goto OUT;
-		}
-
-		block = aipu_alloc_phy_block(mm, buffer, buf_req);
-		if (!block) {
-			dev_err(mm->dev, "OOM iova 0x%llx phy size 0x%llx FAILD.\n", buffer->iova_start, buf_req->bytes);
-			ret = -ENOMEM;
-			goto err_free_iova;
-		}
-
-		//map iova and phy memory
-		ret = aipu_map_sg_to_iommu(mm, block);
-		if (ret) {
-			dev_err(mm->dev, "Map block iova 0x%llx size 0x%llx FAILED.\n", block->iova_start, block->size);
-			goto err_free_phy;
-		}
-		aipu_cache_invalidate(mm, block->va, block->size);
-		break;
-
-	default:
-		ret = -EINVAL;
-		goto OUT;
-	}
-
-	return 0;
-
-err_free_phy:
-	aipu_free_phy_block(mm, block);
-
-err_free_iova:
-	aipu_free_iova(mm, buffer);
-OUT:
-	return ret;
-}
-
-int aipu_free_dma_iova_phy(struct aipu_memory_manager *mm, struct aipu_buf_desc *desc, struct file *filp)
-{
-	u64 exec_id = desc->exec_id;
-	struct aipu_iova_buffer* buffer = NULL;
-	struct aipu_phy_block *block, *tmp;
-	bool free_iova = true;
-
-	if (!desc || !mm)
-		return -EINVAL;
-
-	if (!desc->exec_id || desc->mode == AIPU_DMA_BUF_FREE_DEFAULT)
-		return aipu_mm_free(mm, desc, filp, true);
-
-	if (desc->exec_id >= AIPU_EXEC_ID)
-		buffer = aipu_get_iova_buffer_by_exec_id(mm, exec_id, "free dma iova phy");
-
-	if(!buffer) {
-		buffer = aipu_get_iova_buffer(mm, desc->pa, "free iova phy");
-		if(!buffer) {
-			dev_err(mm->dev, "can't find iova buffer by iova 0x%llx exec id 0x%llx\n", desc->pa, exec_id);
-			return -EINVAL;
-		}
-	}
-
-	//rebind/bind dma buffer free one by one in iova buffer
-	if (desc->exec_id == DMA_BUF_EXEC_ID){
-		list_for_each_entry_safe(block, tmp, &buffer->phy_blocks, list) {
-			if (block->iova_start == desc->pa) {
-				list_del(&block->list);
-				buffer->ref_count--;
-				buffer->allocated_size -= block->size;
-				iommu_unmap(mm->iommu_domain, block->iova_start, block->size);
-				if (block->bind_memory)
-					free_iova = false;
-				aipu_free_phy_block(mm, block);
-				break;
-			}
-		}
-
-		if (free_iova || !buffer->ref_count)
-			aipu_free_iova(mm, buffer);
-	//free physical memory or iova for job buffer once
-	} else {
-		list_for_each_entry_safe(block, tmp, &buffer->phy_blocks, list) {
-			if (!block->bind_memory) {
-				if (desc->mode == AIPU_DMA_BUF_FREE_BOTH  ||
-				    desc->mode == AIPU_DMA_BUF_FREE_PHY) {
-					list_del(&block->list);
-					buffer->ref_count--;
-					buffer->allocated_size -= block->size;
-					iommu_unmap(mm->iommu_domain, block->iova_start, block->size);
-					aipu_free_phy_block(mm, block);
-				}
-			} else {
-				free_iova = false;
-			}
-		}
-
-		if (free_iova && (desc->mode == AIPU_DMA_BUF_FREE_BOTH ||
-		    desc->mode == AIPU_DMA_BUF_FREE_IOVA))
-			aipu_free_iova(mm, buffer);
-	}
-
-	return 0;
-}
-
-int aipu_rebind_dma_iova_phy(struct aipu_memory_manager *mm, struct aipu_rebind_buf_desc *desc, struct file *filp)
-{
-	u64 exec_id = desc->exec_id;
-	u64 pa_unmap = desc->pa_unmap;
-	struct aipu_iova_buffer* buffer;
-	struct aipu_phy_block *block;
-	int ret = 0;
-	size_t mapped_size;
-
-	if (!mm || !desc)
-		return -EINVAL;
-
-	if (!mm->has_iommu) {
-		dev_err(mm->dev, "reserved memory can't support rebind iova.\n");
-		return -EINVAL;
-	}
-
-	//handle before iova buffer;
-	buffer = aipu_get_iova_buffer(mm, pa_unmap, "rebind dma buffer");
-	if (!buffer) {
-		dev_err(mm->dev, "Cant find iova buffer 0x%llx.\n", desc->pa_unmap);
-		return -EINVAL;
-	}
-
-	block = aipu_remove_block_node(buffer, pa_unmap);
-	if (!block) {
-		dev_err(mm->dev, "Can't unbind block.\n");
-		return -ENXIO;
-	}
-
-	mapped_size = iommu_unmap(mm->iommu_domain, block->iova_start, block->size);
-	 if (mapped_size != block->size) {
-		dev_err(mm->dev, "Can't unmap block 0x%llx ret = .\n", block->iova_start);
-		return -ENXIO;
-	}
-
-	//iova buffer free
-	if (!buffer->ref_count)
-		aipu_free_iova(mm, buffer);
-
-	//rebind new iova buffer
-	buffer = aipu_get_iova_buffer_by_exec_id(mm, exec_id, "rebind dma buf");
-	if (!buffer)
-		return -ENXIO;
-
-	aipu_add_block_into_buffer(buffer, block);
-	block->bind_memory = true;
-	ret = aipu_map_sg_to_iommu(mm, block);
-	if (ret) {
-		dev_err(mm->dev, "Map block to new iova 0x%llx size 0x%llx FAILED.\n", block->iova_start, block->size);
-		aipu_free_phy_block(mm, block);
-		goto OUT;
-	}
-
-	desc->pa = block->iova_start;
-	aipu_cache_invalidate(mm, block->va, block->size);
-
-OUT:
-	return ret;
+	return aipu_mm_alloc(mm, buf_req, filp);
 }
 
-int aipu_bind_dma_iova_phy(struct aipu_memory_manager *mm, struct aipu_bind_buf_desc *desc, struct file *filp)
+int aipu_free_dma_iova_phy(struct aipu_memory_manager *mm,
+			   struct aipu_buf_desc *desc, struct file *filp)
 {
-	u64 exec_id = desc->exec_id;
-	struct aipu_iova_buffer* buffer;
-	struct aipu_phy_block *block;
-	int ret = 0;
-	struct dma_buf *dmabuf = NULL;
-	struct exporter_dma_buf *exp_buf = NULL;
-
 	if (!mm || !desc)
 		return -EINVAL;
+	if (desc->mode != AIPU_DMA_BUF_FREE_DEFAULT || desc->exec_id)
+		return -EOPNOTSUPP;
 
-	if (!mm->has_iommu) {
-		dev_err(mm->dev, "reserved memory can't support rebind iova.\n");
-		return -EINVAL;
-	}
-
-	//bind new iova buffer
-	buffer = aipu_get_iova_buffer_by_exec_id(mm, exec_id, "bind dma buf");
-	if (!buffer) {
-		return -EINVAL;
-	}
-
-	//handle before iova buffer;
-	dmabuf = dma_buf_get(desc->fd);
-	if(IS_ERR(dmabuf)) {
-		dev_err(mm->dev, "fail to get dmabuf from fd.\n");
-		return PTR_ERR(dmabuf);
-	}
-
-	if ((buffer->allocated_size + dmabuf->size) > buffer->iova_size) {
-		dev_err(mm->dev, "iova space is not enough for phy memory allcation.");
-		ret = -EINVAL;
-		goto put_dmabuf;
-	}
-
-	block = kzalloc(sizeof(*block), GFP_KERNEL);
-	if (!block) {
-		ret = -ENOMEM;
-		goto put_dmabuf;
-	}
-
-	exp_buf = dmabuf->priv;
-
-	block->size = dmabuf->size;
-	block->page_count = dmabuf->size>>PAGE_SHIFT;
-	block->sgt.nents = block->page_count;
-	//block->dmabuf = dmabuf;
-	block->dmabuf = NULL;
-	block->pages = exp_buf->pages;
-	block->bind_memory = true;
-
-	INIT_LIST_HEAD(&block->list);
-
-	aipu_add_block_into_buffer(buffer, block);
-
-	ret = aipu_map_sg_to_iommu(mm, block);
-	if (ret) {
-		dev_err(mm->dev, "Map block to new iova 0x%llx size 0x%llx FAILED.\n", block->iova_start, block->size);
-		aipu_free_phy_block(mm, block);
-		goto put_dmabuf;
-	}
-	desc->pa = block->iova_start;
-	aipu_cache_invalidate(mm, block->va, block->size);
-
-put_dmabuf:
-	dma_buf_put(dmabuf);
-	return ret;
-}
-
-static int aipu_free_all_dma_iova_phy(struct aipu_memory_manager *mm, struct file *filp)
-{
-	struct aipu_file_ctx *owner = aipu_file_ctx(filp);
-	struct aipu_iova_buffer* buffer, *tmp1;
-	struct aipu_phy_block *block, *tmp;
-	struct aipu_iova_buffer** pbuf;
-	int buffer_count = 0;
-	int index = 0;
-
-	mutex_lock(&mm->buffer_mutex);
-	list_for_each_entry(buffer,&mm->buffer_list, node) {
-		if (buffer->owner == owner)
-			buffer_count++;
-	}
-	mutex_unlock(&mm->buffer_mutex);
-
-	if (buffer_count == 0)
-		return 0;
-
-	pbuf = kzalloc(buffer_count * sizeof(*buffer), GFP_KERNEL);
-	if (!pbuf)
-		return -ENOMEM;
-
-	mutex_lock(&mm->buffer_mutex);
-	list_for_each_entry_safe(buffer, tmp1, &mm->buffer_list, node) {
-		if (buffer->owner == owner) {
-			pbuf[index] = buffer;
-			index++;
-		}
-	}
-	mutex_unlock(&mm->buffer_mutex);
-
-	//unmap sg table and free phy memory
-	for (index = 0; index < buffer_count; index++) {
-		list_for_each_entry_safe(block, tmp, &pbuf[index]->phy_blocks, list) {
-			if (block) {
-				list_del(&block->list);
-				iommu_unmap(mm->iommu_domain, block->iova_start, block->size);
-				aipu_free_phy_block(mm, block);
-			}
-		}
-		aipu_free_iova(mm, pbuf[index]);
-	}
-
-	kfree(pbuf);
-
-	return 0;
+	return aipu_mm_free(mm, desc, filp, true);
 }
diff --git a/drivers/misc/armchina-npu/aipu_mm.h b/drivers/misc/armchina-npu/aipu_mm.h
--- a/drivers/misc/armchina-npu/aipu_mm.h
+++ b/drivers/misc/armchina-npu/aipu_mm.h
@@ -10,7 +10,6 @@
 #include <linux/mutex.h>
 #include <linux/spinlock.h>
 #include <linux/scatterlist.h>
-#include <linux/iova.h>
 #include <linux/mm.h>
 #include <linux/highmem.h>
 #include <asm/cacheflush.h>
@@ -19,7 +18,6 @@
 #include "zhouyi.h"
 
 #define DEFERRED_FREE  1
-#define DMA_BUF_EXEC_ID 1
 
 enum aipu_gm_policy {
 	AIPU_GM_POLICY_NONE         = 0,
@@ -64,42 +62,6 @@ struct aipu_hold_tcb_buf {
 	struct aipu_tcb_buf *prev_tbuf;
 };
 
-struct __cache_ops {
-    void (*flush_range)(void *vaddr, size_t size);
-    void (*clean_range)(void *vaddr, size_t size);
-    void (*invalidate_range)(void *vaddr, size_t size);
-    size_t cache_line_size;
-};
-
-struct aipu_phy_block {
-	struct list_head list;
-	struct page **pages;
-	struct sg_table sgt;
-	dma_addr_t dma_addr;
-	u64 iova_start;
-	u64 size;
-	u32 page_count;
-	void* va;
-	struct dma_buf *dmabuf;
-	bool bind_memory;
-};
-
-struct aipu_iova_buffer {
-	u64 iova_start;
-	u64 iova_size;
-	u64 allocated_size;
-	struct list_head phy_blocks;
-	struct mutex phy_mutex;
-	u32 ref_count;
-	u8 region;
-	u8 asid;
-	struct aipu_file_ctx *owner;
-	int tgid;
-	int pid;
-	struct list_head node;
-	u64 exec_id;
-};
-
 /**
  * struct aipu_tcb_buf - TCB buffer descriptor
  * @pfn: pfn number
@@ -300,14 +262,6 @@ struct aipu_memory_manager {
 	struct aipu_hold_tcb_buf *hold_tcb_head;
 	u64 dma_mask;
 	struct iommu_domain *iommu_domain;
-	struct iova_domain iova_domain;
-	struct mutex buffer_mutex;
-	struct list_head buffer_list;
-	u64 iova_base;
-	u64 iova_size;
-	u32 buffer_count;
-	u64 host_aipu_offset;
-	struct __cache_ops cache_ops;
 };
 
 int aipu_init_mm(struct aipu_memory_manager *mm, struct platform_device *p_dev, int version);
@@ -360,19 +314,4 @@ void aipu_mm_set_final_htbuf_index(struct aipu_memory_manager *mm, int index);
 int aipu_alloc_dma_iova_phy(struct aipu_memory_manager *mm, struct aipu_buf_request *buf_req,
 		  struct file *filp);
 int aipu_free_dma_iova_phy(struct aipu_memory_manager *mm, struct aipu_buf_desc *buf, struct file *filp);
-void aipu_cache_flush(struct aipu_memory_manager *mm, void *vaddr, size_t size);
-void aipu_cache_clean(struct aipu_memory_manager *mm, void *vaddr, size_t size);
-void aipu_cache_invalidate(struct aipu_memory_manager *mm, void *vaddr, size_t size);
-struct aipu_phy_block *aipu_get_block_buffer(struct aipu_memory_manager *mm,
-					     u64 iova, char* str);
-struct aipu_iova_buffer *aipu_get_iova_buffer_by_exec_id(struct aipu_memory_manager *mm,
-							 u64 exec_id, char* str);
-int aipu_rebind_dma_iova_phy(struct aipu_memory_manager *mm,
-			     struct aipu_rebind_buf_desc *desc,
-			     struct file *filp);
-int aipu_bind_dma_iova_phy(struct aipu_memory_manager *mm,
-			   struct aipu_bind_buf_desc *desc,
-			   struct file *filp);
-int aipu_copy_block_by_dma_fd(struct aipu_memory_manager *mm,
-			      struct aipu_dma_buf *dmabuf_info);
 #endif /* __AIPU_MM_H__ */
diff --git a/drivers/misc/armchina-npu/aipu_partition.h b/drivers/misc/armchina-npu/aipu_partition.h
--- a/drivers/misc/armchina-npu/aipu_partition.h
+++ b/drivers/misc/armchina-npu/aipu_partition.h
@@ -104,8 +104,7 @@ struct cluster_info {
  * @dtcm_size:       DTCM size in bytes
  * @cluster_cnt:     cluster count
  * @clusters:        cluster information array
- * @partition_mode:  partition mode in a cluster (for zhouyi v3_2 only)
- * @event_type:      interrupt's event type (for zhouyi v3_2 only)
+ * @event_type:      pending interrupt recovery event
  */
 struct aipu_partition {
 	u32 id;
@@ -127,7 +126,6 @@ struct aipu_partition {
 	u32 dtcm_size;
 	u32 cluster_cnt;
 	struct cluster_info clusters[8];
-	int partition_mode;
 	int event_type;
 	struct io_region *dbg_reg;
 	spinlock_t io_lock;
diff --git a/drivers/misc/armchina-npu/aipu_priv.c b/drivers/misc/armchina-npu/aipu_priv.c
--- a/drivers/misc/armchina-npu/aipu_priv.c
+++ b/drivers/misc/armchina-npu/aipu_priv.c
@@ -5,10 +5,7 @@
 #include <linux/of_address.h>
 #include "aipu_priv.h"
 #include "config.h"
-#include "v1.h"
-#include "v2.h"
 #include "v3.h"
-#include "v3_2.h"
 
 static int init_misc_dev(struct aipu_priv *aipu)
 {
@@ -69,7 +66,6 @@ int init_aipu_priv(struct aipu_priv *aipu, struct platform_device *p_dev,
 {
 	int ret = 0;
 	int version = 0;
-	int config = 0;
 	int revision = 0;
 
 	if (!aipu || !p_dev || !fops)
@@ -104,29 +100,15 @@ int init_aipu_priv(struct aipu_priv *aipu, struct platform_device *p_dev,
 	aipu->dbg_reg.size = 0;
 	aipu->ops = NULL;
 
-	ret = zhouyi_detect_aipu_version(p_dev, &version, &config, &revision);
+	ret = zhouyi_detect_aipu_version(p_dev, &version, &revision);
 	if (ret)
 		return ret;
-	dev_dbg(aipu->dev, "AIPU core0 ISA version %d, configuration %d\n", version, config);
+	dev_dbg(aipu->dev, "AIPU core0 ISA version %d\n", version);
 	aipu->version = version;
 	aipu->revision = revision;
 
-#if defined(CONFIG_ARMCHINA_NPU_ARCH_V3_2)
-	if (version >= AIPU_ISA_VERSION_ZHOUYI_V3_2_0) {
-		aipu->ops = get_v3_2_priv_ops();
-		aipu->core_reset_delay_us = AIPU_CONFIG_CORE_RESET_DELAY_US;
-	}
-#endif
-
-#ifdef CONFIG_ARMCHINA_NPU_ARCH_V3
 	if (version == AIPU_ISA_VERSION_ZHOUYI_V3)
 		aipu->ops = get_v3_priv_ops();
-#endif
-
-#if (defined CONFIG_ARMCHINA_NPU_ARCH_V1) || (defined CONFIG_ARMCHINA_NPU_ARCH_V2)
-	if (version > 0 && version <= AIPU_ISA_VERSION_ZHOUYI_V2_2)
-		aipu->ops = get_v1v2_priv_ops();
-#endif
 
 	if (!aipu->ops) {
 		ret = -EINVAL;
@@ -232,8 +214,7 @@ int aipu_priv_get_version(struct aipu_priv *aipu)
 
 /**
  * aipu_priv_get_partition_cnt - get AIPU partition count
- *        For V1/V2, a *partition* represents an AIPU core
- *        For V3, a *partition* represents a group of AIPU clusters in the same power domain
+ *        For V3, a partition represents AIPU clusters in one power domain
  * @aipu: pointer to the aipu private struct initialized in init_aipu_priv()
  *
  * Return AIPU partition count
diff --git a/drivers/misc/armchina-npu/aipu_priv.h b/drivers/misc/armchina-npu/aipu_priv.h
--- a/drivers/misc/armchina-npu/aipu_priv.h
+++ b/drivers/misc/armchina-npu/aipu_priv.h
@@ -43,7 +43,7 @@ struct aipu_priv_operations {
  * @partitions:    partition/core array
  * @reg:           AIPU register base (v3 only)
  * @irq_obj:       interrupt object (v3 only)
- * @dev:           device struct pointer (of core 0 for v1/v2)
+ * @dev:           device struct pointer
  * @soc:           SoC private data
  * @soc_ops:       SoC operation pointer
  * @aipu_fops:     file operation struct
diff --git a/drivers/misc/armchina-npu/aipu_tcb.h b/drivers/misc/armchina-npu/aipu_tcb.h
--- a/drivers/misc/armchina-npu/aipu_tcb.h
+++ b/drivers/misc/armchina-npu/aipu_tcb.h
@@ -121,80 +121,4 @@ struct aipu_tcb {
 #define IS_TASK_TCB(flag)               (((flag) & 0x3) == TCB_FLAG_TASK_TYPE_TASK)
 #define IS_GRID_END(flag)               ((flag) & TCB_FLAG_END_TYPE_GRID_END)
 
-struct aipu_tcb_v32
-{
-    uint32_t flag;
-    union {
-        struct
-        {
-            uint32_t next;
-            uint32_t rsvd0[1];
-            uint32_t spc;
-            uint32_t interrupt_en;
-            uint16_t group_id;
-            uint16_t grid_id;
-            uint16_t task_id;
-            uint16_t warmup_len;
-            uint16_t grid_dim_x;
-            uint16_t grid_dim_y;
-            uint16_t grid_dim_z;
-            uint16_t group_dim_x;
-            uint16_t group_dim_y;
-            uint16_t group_dim_z;
-            uint16_t group_id_x;
-            uint16_t group_id_y;
-            uint16_t group_id_z;
-            uint16_t task_id_x;
-            uint16_t task_id_y;
-            uint16_t task_id_z;
-            uint32_t sp;
-            uint32_t pp;
-            uint32_t dp;
-            uint32_t cp;
-            uint32_t pprint;
-            uint32_t pprofiler;
-            uint16_t core_id;
-            uint16_t cluster_id;
-            uint16_t rsvd1;
-            uint16_t tec_id;
-            uint32_t rsvd2[5];
-            uint32_t dsize;
-            uint32_t tcbp;
-            uint32_t global_param;
-            uint32_t rsvd3;
-        } task;
-
-        union {
-            struct
-            {
-                uint32_t rsvd0;
-                uint32_t group_num;
-                uint32_t rsvd1;
-                uint32_t interrupt_en;
-                uint16_t group_id;
-                uint16_t grid_id;
-                uint32_t rsvd2[6];
-                uint32_t gm_ctrl;
-                uint32_t gm_sync;
-                uint32_t gm_addr_low;
-                uint32_t gm_addr_high;
-                uint32_t rsvd3[14];
-            } grid;
-
-            struct
-            {
-                uint32_t segmmu_ctrl;
-                uint32_t segmmu_remap_ctrl0;
-                uint32_t segmmu_remap_ctrl1;
-                uint32_t interrupt_en;
-                uint16_t group_id;
-                uint16_t grid_id;
-                uint32_t segmmu_seg_ctrl[16];
-                uint32_t asids[8];
-            } group;
-        };
-    };
-    uint16_t group_deps[4];
-};
-
 #endif /* __AIPU_TCB_H__ */
diff --git a/drivers/misc/armchina-npu/config.h b/drivers/misc/armchina-npu/config.h
--- a/drivers/misc/armchina-npu/config.h
+++ b/drivers/misc/armchina-npu/config.h
@@ -14,6 +14,5 @@
 #define AIPU_CONFIG_DEFAULT_RESET_DELAY_US  50
 #define AIPU_CONFIG_CORE_RESET_DELAY_US     140
 #define AIPU_CONFIG_ENABLE_INTR_PROFILING   0
-#define AIPU_USE_STANDARD_DMA_API_FOR_V3_2  0
 
 #endif /* __CONFIG_H__ */
diff --git a/drivers/misc/armchina-npu/zhouyi/v1.h b/drivers/misc/armchina-npu/zhouyi/v1.h
deleted file mode 100644
--- a/drivers/misc/armchina-npu/zhouyi/v1.h
+++ /dev/null
@@ -1,34 +0,0 @@
-/* SPDX-License-Identifier: GPL-2.0 */
-/* Copyright (c) 2023-2025 Arm Technology (China) Co. Ltd. */
-
-#ifndef __V1_H__
-#define __V1_H__
-
-#include "zhouyi.h"
-
-/*
- * Zhouyi V1 AIPU Interrupts
- */
-#define ZHOUYI_V1_IRQ              (ZHOUYI_IRQ)
-#define ZHOUYI_V1_IRQ_ENABLE_FLAG  (ZHOUYI_V1_IRQ)
-#define ZHOUYI_V1_IRQ_DISABLE_FLAG (ZHOUYI_IRQ_NONE)
-
-#define ZHOUYI_V1_MAX_SCHED_JOB_NUM  1
-
-/*
- * Zhouyi V1 AIPU Specific Host Control Register Map
- */
-#define ZHOUYI_INTR_CAUSE_REG_OFFSET          0x20
-#define ZHOUYI_INTR_STAT_REG_OFFSET           0x24
-#define ZHOUYI_INTR_BACKUP_STAT_REG_OFFSET    0x28
-#define ZHOUYI_INTR_BACKUP_PC_REG_OFFSET      0x2C
-#define ZHOUYI_DBG_ERR_CAUSE_REG_OFFSET       0x30
-#define ZHOUYI_DBG_DATA_REG_0_OFFSET          0x34
-#define ZHOUYI_DBG_DATA_REG_1_OFFSET          0x38
-#define ZHOUYI_L2_CACHE_FEATURE_REG_OFFSET    0x6C
-#define ZHOUYI_V1_MAX_REG_OFFSET              0x6C
-
-struct aipu_priv_operations *get_v1v2_priv_ops(void);
-struct aipu_operations *get_zhouyi_v1_ops(void);
-
-#endif /* __V1_H__ */
diff --git a/drivers/misc/armchina-npu/zhouyi/v2.h b/drivers/misc/armchina-npu/zhouyi/v2.h
deleted file mode 100644
--- a/drivers/misc/armchina-npu/zhouyi/v2.h
+++ /dev/null
@@ -1,54 +0,0 @@
-/* SPDX-License-Identifier: GPL-2.0 */
-/* Copyright (c) 2023-2025 Arm Technology (China) Co. Ltd. */
-
-#ifndef __V2_H__
-#define __V2_H__
-
-#include "zhouyi.h"
-
-/*
- * Zhouyi V2 AIPU Specific Interrupts/Configs
- */
-#define ZHOUYI_IRQ_FAULT                    0x8
-
-#define ZHOUYI_V2_IRQ                       (ZHOUYI_IRQ | ZHOUYI_IRQ_FAULT)
-#define ZHOUYI_V2_IRQ_ENABLE_FLAG           (ZHOUYI_V2_IRQ)
-#define ZHOUYI_V2_IRQ_DISABLE_FLAG          (ZHOUYI_IRQ_NONE)
-
-#define ZHOUYI_V2_MAX_SCHED_JOB_NUM         1
-
-#define ZHOUYI_V2_ASE_READ_ENABLE           BIT(31)
-#define ZHOUYI_V2_ASE_WRITE_ENABLE          BIT(30)
-#define ZHOUYI_V2_ASE_RW_ENABLE             (ZHOUYI_V2_ASE_READ_ENABLE | ZHOUYI_V2_ASE_WRITE_ENABLE)
-#define ZHOUYI_V2_DTCM_ENABLE               BIT(16)
-#define ZHOUYI_V2_DTCM_MAX_BYTES            (32 * SZ_1M) /* 32MB at maximum */
-
-/*
- * Zhouyi V2 AIPU Specific Host Control Register Map
- */
-#define AIPU_DATA_ADDR_2_REG_OFFSET         0x1C
-#define AIPU_DATA_ADDR_3_REG_OFFSET         0x20
-#define AIPU_SECURE_CONFIG_REG_OFFSET       0x30
-#define AIPU_POWER_CTRL_REG_OFFSET          0x38
-#define AIPU_ADDR_EXT0_CTRL_REG_OFFSET      0xC0
-#define AIPU_ADDR_EXT0_HIGH_BASE_REG_OFFSET 0xC4
-#define AIPU_ADDR_EXT0_LOW_BASE_REG_OFFSET  0xC8
-#define AIPU_ADDR_EXT1_CTRL_REG_OFFSET      0xCC
-#define AIPU_ADDR_EXT1_HIGH_BASE_REG_OFFSET 0xD0
-#define AIPU_ADDR_EXT1_LOW_BASE_REG_OFFSET  0xD4
-#define AIPU_ADDR_EXT2_CTRL_REG_OFFSET      0xD8
-#define AIPU_ADDR_EXT2_HIGH_BASE_REG_OFFSET 0xDC
-#define AIPU_ADDR_EXT2_LOW_BASE_REG_OFFSET  0xE0
-#define AIPU_ADDR_EXT3_CTRL_REG_OFFSET      0xE4
-#define AIPU_ADDR_EXT3_HIGH_BASE_REG_OFFSET 0xE8
-#define AIPU_ADDR_EXT3_LOW_BASE_REG_OFFSET  0xEC
-#define ZHOUYI_V2_DTCM_CTRL                 0x180
-#define ZHOUYI_V2_DTCM_HIGH_BASE_ADDR       0x184
-#define ZHOUYI_V2_DTCM_LOW_BASE_ADDR        0x188
-#define ZHOUYI_V2_SOFT_RESET_OFFSET         0x200
-#define ZHOUYI_V2_MAX_REG_OFFSET            (ZHOUYI_V2_SOFT_RESET_OFFSET)
-
-struct aipu_priv_operations *get_v1v2_priv_ops(void);
-struct aipu_operations *get_zhouyi_v2_ops(void);
-
-#endif /* __V2_H__ */
diff --git a/drivers/misc/armchina-npu/zhouyi/v3_2.h b/drivers/misc/armchina-npu/zhouyi/v3_2.h
deleted file mode 100644
--- a/drivers/misc/armchina-npu/zhouyi/v3_2.h
+++ /dev/null
@@ -1,473 +0,0 @@
-/* SPDX-License-Identifier: GPL-2.0 */
-/* Copyright (c) 2023-2025 Arm Technology (China) Co. Ltd. */
-
-#ifndef __V3_2_H__
-#define __V3_2_H__
-
-/**************************************************************************************
- *                             Cluster [N] Registers
- *************************************************************************************/
-/**
- * Cluster Range Base Address
- *
- * 0xC00 ~ 0xC1F    Cluster 0
- * 0xC20 ~ 0xC3F    Cluster 1
- * 0xC40 ~ 0xC5F    Cluster 2
- * ...
- * 0xCE0 ~ 0xCFF    Cluster 7
- */
-#define _GET_CLUSTER_OFFSET_V3_2(id)                     (0xC00 + 0x20 * (id))
-#define MAX_CLUSTER_NUM_V3_2                             8
-
-#define _GET_CLUSTER_FETR_V3_2(id)                     (0x2800 + 0x70 * (id))
-
-/**
- * A1.1.1 Cluster Configuration Register (Read-Only)
- * [31-13]  Reserved
- * [12]     set if cluster is present
- * [11:8]   number of available AIPU cores
- * [7:4]    number of available AIFF in every core
- * [3:0]    number of available TEC in every core
- */
-#define IS_CLUSTER_PRESENT_V3_2(config_32)               (((config_32) >> 12) & 0x1)
-#define GET_AIPU_CORE_NUM_V3_2(config_32)                (((config_32) >> 8) & 0xF)
-#define GET_AIFF_NUM_V3_2(config_32)                     (((config_32) >> 4) & 0xF)
-#define GET_TEC_NUM_V3_2(config_32)                      ((config_32) & 0xF)
-#define GET_NUMS_V3_2(config_32)                         ((config_32) & 0xFFF)
-#define CLUSTER_CONFIG_REG_V3_2(id)                      (_GET_CLUSTER_OFFSET_V3_2(id) + 0x0)
-
-/*
- * A1.1.2 Cluster Control Register
- * [31:24] core enable bits (27:24: core 3 - core 0; )
- * [23:13] reserved (RO)
- * [12]    cluster enable (RO)
- * [11:8]  Core enable number (RO)
- * [7:4]   AIFF enable number (RO)
- * [3:0]   TEC enable number (RO)
- * [3:0] number of TEC enabled in every core (RO)
- */
-#define _ENABLE_CORE_V3_2(core_en)                      ((core_en) << 24)
-#define _ENABLE_CLUSTER_V3_2                             (0x1 << 12)
-#define _DISABLE_CLUSTER_V3_2                            (0x0 << 12)
-#define _EN_CORE_NUM_V3_2(num)                           (((num) & 0xF) << 8)
-#define _EN_AIFF_NUM_V3_2(num)                           (((num) & 0xF) << 4)
-#define _EN_TEC_NUM_V3_2(num)                            ((num) & 0xF)
-#define EN_NUMS_V3_2(nums)                               ((nums) & 0xFFF)
-#define ENABLE_CLUSTER_V3_2(partition, nums) \
-	(EN_NUMS_V3_2(nums) | _ENABLE_CLUSTER_V3_2)
-#define DISABLE_CLUSTER_V3_2                             _DISABLE_CLUSTER_V3_2
-#define CONFIG_CLUSTER_V3_2(core_en, en_core_num, en_aiff, en_tec, par_mode) \
-	(_ENABLE_CORE_V3_2(core_en) | \
-	 _EN_CORE_NUM_V3_2(en_core_num) | \
-	 _EN_AIFF_NUM_V3_2(en_aiff) | \
-	 _EN_TEC_NUM_V3_2(en_tec) | \
-	 _ENABLE_CLUSTER_V3_2 | (par_mode))
-
-#define CLUSTER_CONTROL_REG_V3_2(id)                     (_GET_CLUSTER_OFFSET_V3_2(id) + 0x4)
-
-/*
- * A.3.41 Cluster Global Memory Feature Register
- * [31:12] reserved (RO)
- * [11:8]  Banks (RO)
- * [7:4]   Width (RO)
- * [3:0]   Size (RO) 512k * 2^size
- */
-#define CLUSTER_GM_FEATURE_V3_2(id)                     (_GET_CLUSTER_FETR_V3_2(id) + 0x58)
-
-/*****************************************************************************
- *                             TSM Registers
- *****************************************************************************/
-/**
- * TSM Command Schedule Control Handle Register
- *
- * [31:28] Write to select a cluster for direct dispatching
- * [27:24] Write to select a core for direct dispatching
- * [23:20] Reserved (RO)
- * [19]    Command pool type
- *         0: PCP
- * [18:16] Write to select a command pool
- *         0 command pool 0 sellected
- * [15:13] Reserved (RO)
- * [12]    0 to map one cluster to this command pool (RO)
- *         1) Only works for command pool creation
- *         2) R/O for single cluster TSM
- * [11:10] Reserved (RO)
- * [9:8]   QOS 0 slow, 1 Media, 2 fast (reserved in LIN), 3 reserved
- * [7:4]   Reserved (RO)
- * [3:0]   command tpye:0 none, 1 create, 2 destroy, 3 Abortion,
- *                      4 Dispatch,5 direct dispatch, 6 Others.
- */
-#define _TSM_DIRECT_CLUSTER_V3_2(cluser)                  ((cluster) << 28)
-#define _TSM_DIRECT_CORE_V3_2(core)                       ((core) << 24)
-#define _TSM_POOL_V3_2(pool)                              ((pool) << 16)
-#define _TSM_MAP_V3_2(map)                                ((map) << 12)
-#define _TSM_QOS_V3_2(qos)                                ((qos) << 8)
-#define _TSM_CREATE_V3_2                                  0x1
-#define _TSM_DESTROY_V3_2                                 0x2
-#define _TSM_ABORT_V3_2                                   0x3
-#define _TSM_DISPATCH_V3_2                                0x4
-#define _TSM_BIND_DISPATCH_V3_2                           0x5
-
-#define TSM_MAP_SINGLE_V3_2                              _TSM_MAP_V3_2(0x0)
-#define TSM_MAP_ALL_V3_2                                 _TSM_MAP_V3_2(0x1)
-#define TSM_CREATE_CMD_POOL_V3_2(pool, map)              (_TSM_CREATE_V3_2 | _TSM_POOL_V3_2(pool) | (map))
-#define TSM_DESTROY_CMD_POOL_V3_2(pool)                  (_TSM_DESTROY_V3_2 | _TSM_POOL_V3_2(pool))
-#define TSM_ABORT_CMD_POOL_V3_2(pool)                    (_TSM_ABORT_V3_2 | _TSM_POOL_V3_2(pool))
-#define TSM_BIND_DISPATCH_CMD_POOL_V3_2(pool, qos, core)  (_TSM_BIND_DISPATCH_V3_2 | (qos) | \
-						     _TSM_POOL_V3_2(pool) | _TSM_DIRECT_CORE_V3_2(core))
-
-#define TSM_CMD_SCHD_CTRL_HANDLE_REG_V3_2             0x0
-#define TSM_DISPATCH_CMD_POOL_V3_2(pool, qos)         (_TSM_DISPATCH_V3_2 | (qos) | (pool))
-
-/**
- * A1.1.6 TSM Command Schedule Address High Register
- *
- * [31:0] Write to set the address[63:32] of head of a TCB chain to be dispatched
- */
-#define TSM_CMD_SCHD_ADDR_HIGH_REG_V3_2               0x8
-
-/**
- * A1.1.7 TSM Command Schedule Address Low Register
- *
- * [31:12] Write to set the address[31:12] of head of a TCB chain to
- *         be dispatched
- *         1) this address shall be 4k page aligned
- */
-#define TSM_CMD_SCHD_ADDR_LOW_REG_V3_2                0xC
-
-/**
- * A1.1.9 TSM Configuration Register
- * [31:4] Reserved (RO)
- * [3:0]  TSM scheduling policy
- */
-#define TSM_CONFIG_REG_V3_2                           0x10
-
-/**
- * TSM Build Info Register (Read-Only)
- *
- * [25:24] Partition mode: 0
- * [19:16] Supported command pool number: 1/2
- *
- * other fields are the same as v3
- */
-#define TSM_BUILD_INFO_REG_V3_2                       0x14
-#define GET_CMD_POOL_NUM_V3_2(build_info_32)          ((((build_info_32) >> 16) & 0xF) + 1)
-
-/**
- * TSM Status Register
- * [31]    scheduling cmd fail
- * [30:24] Reserved (RO)
- * [23:16] TSM abort command pool done
- * [15:12] Reserved
- * [11:8]  TSM PCP (QoS high) is full
- * [7:4]   Reserved
- * [3:0]   TSM PCP (QoS low) is full
- */
-#define PCP_ABORT_DONE_BIT                          16
-#define IS_ABORT_DONE(status_32, done_bit)          (((status_32) >> (done_bit)) & 0x1)
-#define CLEAR_ABORT(done_bit)                       ((~(1 << (done_bit))) & (0x7FFFFFFF))
-#define IS_PCP_FULL(status, high)                   (((status) >> ((high) ? 8 : 0)) & 0x1)
-#define IS_CMD_FAIL_V3_2(status_32)                   ((status_32) >> 31)
-#define IS_CMD_POOL_FULL_V3_2(status_32)              ((status_32) & 0xFF)
-#define CLEAR_CMD_FAIL_V3_2(status_32)                ((status_32) & 0x7FFFFFFF)
-#define TSM_STATUS_REG_V3_2                           0x18
-
-/**
- * A1.1.8 TSM Command Schedule TCB Number Register
- *
- * [31:0] TCB number of chain
- */
-#define TSM_COMMAND_SCHEDULE_TCB_NUM_REG_V3_2         0x1C
-
-/**************************************************************************************
- *                             Debug Link Access Registers
- *************************************************************************************/
-/**
- * A1.1.28 AHB Internal CSR Selection Control Register
- *
- * [12]   debug enable
- * [11:4] cluster selection
- * [3:0]  core selection
- */
-#define _ENABLE_DEBUG_V3_2                            BIT(12)
-#define _SET_DBG_CLUSTER_V3_2(id)                     (((id) & 0xFF) << 4)
-#define _SET_DBG_CORE_V3_2(id)                        ((id) & 0xF)
-#define SELECT_DEBUG_CORE_V3_2(cluster, core)         (_ENABLE_DEBUG_V3_2 | \
-			_SET_DBG_CLUSTER_V3_2(cluster) | _SET_DBG_CORE_V3_2(core))
-#define DISABLE_DEBUG_V3_2                            0
-#define AHB_INTERNAL_CSR_SELECTION_CTRL_REG         0x1F00
-#define CLUSTER_MMR_WIN_BASE                        (0x2000)
-#define CORE_MMR_WIN_BASE                           (0x3000)
-
-/**
- * A1.1.8 TSM Revision Register (Read-Only)
- *
- * [27:24] implementation ID
- * [23:16] primary part number
- * [15:8]  secondary part number
- * [7:4]   major revision ID
- * [3:0]   minor number
- */
-#define _IS_TSM_ARCH_V3_2(rev_32)                     (((rev_32) >> 16) & 0x1)
-#define _IS_V3_2_ARCH(rev_32)                         ((((rev_32) >> 8) & 0x1))
-
-#define TSM_REVISION_REG_V3_2                         0x50
-
-/**
- * A1.1.22 Tick Counter Control Status Register
- *
- * [8] counter overflow flag
- * [1] clear
- * [0] enable
- */
-#define IS_COUNTER_OVERFLOW_V3_2(status_32)           (((status_32) >> 8) & 0x1)
-#define CLEAR_COUNTER_V3_2                            BIT(1)
-#define ENABLE_COUNTER_V3_2                           0x1
-#define DISABLE_COUNTER_V3_2                          ((CLEAR_COUNTER_V3_2))
-
-#define TICK_COUNTER_CONTROL_STATUS_REG_V3_2          0x68
-
-/**
- * TSM Interrupt Status Register
- *
- * Bit 0 - 15: TSM interrupt 0 - 15
- */
-#define TSM_CLEAR_INTERRUPT_V3_2(i)                   BIT(i)
-#define GET_V3_2_IRQS_BITS(status)                    ((status) & 0xFFFF)
-#define IS_ALL_CLEAR_IDLE(status)		    (status&0xFFFF0000)
-
-#define TSM_INTERRUPT_STATUS_REG                    0x80
-#define TSM_IRQ_MAX_NUM                             16
-
-/**************************************************************************************
- *                             TSM Command Pool (PCP) Registers
- *************************************************************************************/
-#define _TSM_PCP_REGISTERS_BASE                     0x800
-
-/**************************************************************************************
- *                             HOST TSM PMU Register
- *************************************************************************************/
-/**
- * A1.1.32 PMU TOP Soft Reset
- *
- * [1] soft reset status bit
- *     1) host write 0 to launch AIPU soft reset
- *     2) AIPU write 1 when a soft reset is done
- * [0] soft reset config bit
- *     1) host write 1 to launch AIPU soft reset
- *     2) AIPU write 0 when a soft reset is done
- */
-#define PMU_TOP_SOFT_RESET_REG                      (0x20)
-
-/**
- * A1.1.33 PMU Cluster Soft Reset
- *
- * [7:4] soft reset status bit
- *     1) host write 0 to clear it after reading
- *     2) AIPU write 1 when a soft reset is done
- * [3:0] cluster soft reset config bit
- *     1) host write 1 to launch AIPU soft reset
- *     2) AIPU write 0 when a soft reset is done
- */
-#define CONFIG_PMU_CLUSTER_RESET(cluster)           (1 << ((cluster) & 0xF))
-#define PMU_CLUSTER_RESET_DONE(cluster)			(1 << (((cluster) & 0xF) + 4))
-#define PMU_CLUSTER_RESET_REG                       (0x24)
-
-/**
- * A1.1.34 PMU Core Soft Reset
- *
- * [31:16] soft reset status bit
- *     1) host write 0 to clear it after reading
- *     2) AIPU write 1 when a soft reset is done
- * [15:0] cluster soft reset config bit
- *     1) host write 1 to launch AIPU soft reset
- *     2) AIPU write 0 when a soft reset is done
- */
-#define CONFIG_PMU_CORE_RESET(cluster, core)	(1 << (((core) & 0xFF) + \
-								((cluster) & 0xF) * 4))
-#define PMU_CORE_RESET_DONE(cluster, core)		(1 << (((core) & 0xFF) + \
-								(((cluster) & 0xF) * 4) + 16))
-#define PMU_CORE_RESET_REG                          (0x28)
-
-/**
- * A1.1.35 PMU Reset Timeout
- *
- * [31:16] core reset timeout flag bit
- *     1) host write 0 to clear it after reading
- *     2) AIPU write 1 when there is reset request.
- * [3:0] Cluster reset timeout flag
- *     1) host write 0 to clear it after reading
- *     2) AIPU write 1 when there is reset request.
- */
-#define PMU_RESET_TIMEOUT_REG                       (0x2c)
-
-/**
- * A1.1.36 PMU Top Clock and Power Control Register
- *
- * [31:3] reserved
- * [2]    secure/non-secure mode (0/1)
- * [1]    clock gating disabled/enabled (0/1)
- * [0]    PMU disabled/enabled (0/1)
- */
-#define PMU_SECURE_MODE                             BIT(2)
-#define PMU_NONSECURE_MODE                          0
-#define PMU_ENABLE_CLOCK_GATING                     BIT(1)
-#define PMU_DISABLE_CLOCK_GATING                    0
-#define PMU_ENABLE                                  BIT(0)
-#define PMU_DISABLE                                 0
-#define PMU_TOP_CLOCK_POWER_CTRL_REG                0x30
-
-/**
- * TSM Command Pool [N] Status PCP Register
- *
- * [31:24]: cluster physical ID
- * [6]: idle (0: busy; 1: idle)
- * [5]: timeout (0: none; 1: MIF timeout)
- * [4]: error (0: none; 1: pool error)
- * [0]: done (0: none; 1: pool done)
- */
-#define IS_POOL_DONE(val)                           ((val) & 0x1)
-#define IS_POOL_ERROR(val)                          (((val) >> 4) & 0x1)
-#define IS_POOL_TIMEOUT(val)                        (((val) >> 5) & 0x1)
-#define IS_POOL_BUSY(val)                           (!(((val) >> 6) & 0x1))
-#define IS_POOL_CID(val)                            ((val) >> 24)
-#define CLEAR_POOL_DONE                             BIT(0)
-
-#define COMMAND_POOL_PCP_STATUS_REG                 (_TSM_PCP_REGISTERS_BASE + 0x4)
-
-/**
- * A1.1.20 TSM Command Pool [N] Interrupt Control PCP Register
- * [31:12] Reserved (RO)
- * [11] command pool interrupt enable
- * [10] cluster interrupt enable
- * [9]  core interrupt enable
- * [8]  tec interrupt enable
- * [6]  timeout interrupt enable
- * [5]  signal interrupt enable
- * [4]  error interrupt enable
- * [3]  fault interrupt enable
- * [2]  exception interrupt enable
- * [0]  done interrupt enable
- */
-#define EN_CMD_POOL_INTR_V3_2                         BIT(11)
-#define EN_CLUSTER_INTR_V3_2                          BIT(10)
-#define EN_CORE_INTR_V3_2                             BIT(9)
-#define EN_TEC_INTR_V3_2                              BIT(8)
-#define EN_TIMEOUT_INTR_V3_2                          BIT(5)
-#define EN_ERROR_INTR_V3_2                            BIT(4)
-#define EN_FAULT_INTR_V3_2                            BIT(3)
-#define EN_EXCEPTION_INTR_V3_2                        BIT(2)
-#define EN_SIGNAL_INTR_V3_2                           BIT(1)
-#define EN_DONE_INTR_V3_2                             BIT(0)
-
-#define EN_ALL_TYPE_INTRS_V3_2 \
-						(EN_SIGNAL_INTR_V3_2 | EN_ERROR_INTR_V3_2 | \
-						EN_FAULT_INTR_V3_2 | EN_EXCEPTION_INTR_V3_2 | \
-						EN_DONE_INTR_V3_2 | EN_TIMEOUT_INTR_V3_2)
-
-#define EN_ALL_LEVEL_INTRS_V3_2 \
-						(EN_CMD_POOL_INTR_V3_2 | EN_CLUSTER_INTR_V3_2 | \
-						 EN_CORE_INTR_V3_2 | EN_TEC_INTR_V3_2)
-#define EN_CMD_POOL_ALL_INTRS_V3_2  (EN_CMD_POOL_INTR_V3_2 | EN_ALL_TYPE_INTRS_V3_2)
-#define EN_ALL_INTRS_V3_2           (EN_ALL_LEVEL_INTRS_V3_2 | EN_ALL_TYPE_INTRS_V3_2)
-#define DISABLE_ALL_INTRS_V3_2                           0
-
-#define COMMAND_POOL_PCP_INTERRUPT_CONTROL_REG      (_TSM_PCP_REGISTERS_BASE + 0x8)
-
-/**************************************************************************************
- *                             Interrupt Registers (Per Interrupt)
- *************************************************************************************/
-/**
- * Per interrupt registers
- *
- * 0xA00: interrupt 0
- * 0xA10: interrupt 1
- * 0xA20: interrupt 2
- * ...
- * 0xAf0: interrupt 15
- */
-
-#define _GET_PER_INTERRUPT_REGISTER_OFFSET(id)      (0xA00 + 0x10 * (id))
-
-/**
- * A1.1.14 TSM Interrupt Type Info Register [N]
- * [31:28] cmd pool ID
- * [27:24] Cluster physical ID
- * [23:20] core physical ID
- * [19:16] TEC physical ID
- * [11]    pool interrupt status
- * [10]    cluster interrupt status
- * [9]     core interrupt status
- * [8]     TEC interrupt status
- * [5]     timeout bit
- * [4]     pool error bit
- * [3]     fault bit
- * [2]     exception bit
- * [1]     irq_signal bit
- * [0]     done bit
- */
-#define IS_RESET(status_32)                           (((status_32) >> 4) & 0x3)
-#define GET_INTR_CLUSTER_ID_V3_2(status_32)           (((status_32) >> 24) & 0xFF)
-#define GET_INTR_CORE_ID_V3_2(status_32)              (((status_32) >> 20) & 0xF)
-#define GET_INTR_TEC_ID_V3_2(status_32)               (((status_32) >> 16) & 0xF)
-#define GET_INTR_GROUP_ID_V3_2(status_32)             ((status_32) & 0xFF)
-#define IS_POOL_IRQ_V3_2(status_32)                   (((status_32) >> 11) & 0x1)
-#define IS_CLUSTER_IRQ_V3_2(status_32)                (((status_32) >> 10) & 0x1)
-#define IS_CORE_IRQ_V3_2(status_32)                   (((status_32) >> 9) & 0x1)
-#define IS_TEC_IRQ_V3_2(status_32)                    (((status_32) >> 8) & 0x1)
-#define IS_TIMEOUT_IRQ_V3_2(status_32)                (((status_32) >> 5) & 0x1)
-#define IS_ERROR_IRQ_V3_2(status_32)                  (((status_32) >> 4) & 0x1)
-#define IS_FAULT_IRQ_V3_2(status_32)                  (((status_32) >> 3) & 0x1)
-#define IS_EXCEPTION_IRQ_V3_2(status_32)              (((status_32) >> 2) & 0x1)
-#define IS_SIGNAL_IRQ_V3_2(status_32)                 (((status_32) >> 1) & 0x1)
-#define IS_DONE_IRQ_V3_2(status_32)                   ((status_32) & 0x1)
-#define IS_ABNORMAL_V3_2(status_32)                   (((status_32) >> 2) & 0xF)
-#define IS_SERIOUS_ERR_V3_2(status_32)                (((status_32) >> 3) & 0x7)
-#define GET_INTR_TYPE_V3_2(status_32)                 ((status_32) & 0x3F)
-#define IS_IRQ_TO_HANDLE_V3_2(status_32)              ((status_32) & 0xF1F)
-#define IS_COREDUMP_IRQ_V3_2(status_32)               (IS_EXCEPTION_IRQ_V3_2(status_32))
-
-#define INTERRUPT_TYPE_INFO_REG(id)                 (_GET_PER_INTERRUPT_REGISTER_OFFSET(id) + 0x0)
-
-/**
- * TSM Interrupt TCB Pointer Register [N]
- *
- * [31:0] TCBP (RO)
- */
-#define INTERRUPT_TCB_PTR_REG(id)                   (_GET_PER_INTERRUPT_REGISTER_OFFSET(id) + 0x4)
-
-/**
- * TSM Interrupt Signal Register [N]
- *
- * [31:15]   reserved
- * [14:0]    irq signal id
- */
-#define INTERRUPT_SIGNAL_REG(id)                    (_GET_PER_INTERRUPT_REGISTER_OFFSET(id) + 0x8)
-
-/**
- * TSM Interrupt Signal flag Register [N]
- *
- * [31:0]    irq signal flag
- */
-#define IS_HW_EXCEPTION_SIGNAL_V3_2(flag)             ((flag) >> 31)
-#define IS_SW_EXCEPTION_SIGNAL_V3_2(flag)             (((flag) >> 30) & 0x1)
-#define IS_EXCEPTION_SIGNAL_V3_2(flag)                ((flag) >> 30)
-#define IS_PRINTF_SIGNAL_V3_2(flag)                   (((flag) >> 29) & 0x1)
-#define IS_PROFILER_SIGNAL_V3_2(flag)                 (((flag) >> 28) & 0x1)
-#define IS_COREDUMP_SIGNAL_V3_2(flag)                 (((flag) >> 27) & 0x1)
-#define GET_ERR_CODE_V3_2(flag)                       ((flag) & 0xFFFF)
-#define GET_PRINF_SIZE_V3_2(flag)                     ((flag) & 0xFFFF)
-#define GET_PROFILER_BUF_PA_V3_2(flag)                (((flag) & 0xFFFFF) << 12)
-
-#define INTERRUPT_GROUP_ID_REG(id)               (_GET_PER_INTERRUPT_REGISTER_OFFSET(id) + 0x8)
-#define INTERRUPT_SIGNAL_FLAG_REG(id)               (_GET_PER_INTERRUPT_REGISTER_OFFSET(id) + 0xc)
-
-#define ZHOUYI_V3_2_MAX_REG_OFFSET                    0x322C
-#define DEBUG_CLUSTER_STATUS_V3_2                    0x2004
-#define DEBUG_CORE_STATUS_V3_2                    0x3004
-
-u64 get_gm_size(u32 val);
-struct aipu_operations *get_zhouyi_v3_2_ops(void);
-struct aipu_priv_operations *get_v3_2_priv_ops(void);
-
-#endif /* __V3_2_H__ */
diff --git a/drivers/misc/armchina-npu/zhouyi/v3_priv.c b/drivers/misc/armchina-npu/zhouyi/v3_priv.c
--- a/drivers/misc/armchina-npu/zhouyi/v3_priv.c
+++ b/drivers/misc/armchina-npu/zhouyi/v3_priv.c
@@ -72,7 +72,6 @@ static struct aipu_partition *v3_create_partitions(struct aipu_priv *aipu,
 {
 	int ret = 0;
 	int version = 0;
-	int config = 0;
 	struct aipu_partition *partitions = NULL;
 	u32 *cluster_arr = NULL;
 	u32 cluster_cnt = 0;
@@ -86,7 +85,7 @@ static struct aipu_partition *v3_create_partitions(struct aipu_priv *aipu,
 	if (!aipu || !p_dev)
 		return ERR_PTR(-EINVAL);
 
-	zhouyi_detect_aipu_version(p_dev, &version, &config, NULL);
+	zhouyi_detect_aipu_version(p_dev, &version, NULL);
 	dev_info(&p_dev->dev, "AIPU detected: zhouyi-v3\n");
 
 	WARN_ON(!aipu->is_init);
diff --git a/drivers/misc/armchina-npu/zhouyi/zhouyi.c b/drivers/misc/armchina-npu/zhouyi/zhouyi.c
--- a/drivers/misc/armchina-npu/zhouyi/zhouyi.c
+++ b/drivers/misc/armchina-npu/zhouyi/zhouyi.c
@@ -41,13 +41,13 @@ int zhouyi_io_rw(struct io_region *io, struct aipu_io_req *io_req)
 	}
 }
 
-int zhouyi_detect_aipu_version(struct platform_device *p_dev, int *version, int *config, int *rev)
+int zhouyi_detect_aipu_version(struct platform_device *p_dev, int *version, int *rev)
 {
 	struct resource *res = NULL;
 	struct io_region io;
 
 	/* rev == NULL is allowed */
-	if (!p_dev || !version || !config)
+	if (!p_dev || !version)
 		return -EINVAL;
 
 	res = platform_get_resource(p_dev, IORESOURCE_MEM, 0);
@@ -62,11 +62,6 @@ int zhouyi_detect_aipu_version(struct platform_device *p_dev, int *version, int
 	}
 
 	*version = zhouyi_get_hw_version_number(&io, rev);
-	if (*version > 0 && *version < AIPU_ISA_VERSION_ZHOUYI_V3)
-		*config = zhouyi_get_hw_config_number(&io);
-	else
-		*config = 0;
-
 	deinit_aipu_ioregion(&io);
 	return 0;
 }
@@ -122,52 +117,14 @@ int zhouyi_get_hw_version_number(struct io_region *io, int *rev)
 		*rev = revision_id;
 
 	switch (revision_id) {
-	case ZHOUYI_V1_REVISION_ID:
-		return AIPU_ISA_VERSION_ZHOUYI_V1;
-	case ZHOUYI_V2_0_REVISION_ID:
-		return AIPU_ISA_VERSION_ZHOUYI_V2_0;
-	case ZHOUYI_V2_1_REVISION_ID:
-		return AIPU_ISA_VERSION_ZHOUYI_V2_1;
-	case ZHOUYI_V2_2_REVISION_ID:
-		return AIPU_ISA_VERSION_ZHOUYI_V2_2;
 	case ZHOUYI_V3_REVISION_ID_R0P2:
-		return AIPU_ISA_VERSION_ZHOUYI_V3;
 	case ZHOUYI_V3_REVISION_ID_R0P3:
 		return AIPU_ISA_VERSION_ZHOUYI_V3;
-	case ZHOUYI_V3_2_REVISION_ID_R1P0:
-		return AIPU_ISA_VERSION_ZHOUYI_V3_2_0;
-	case ZHOUYI_V3_2_REVISION_ID_R2P0:
-		return AIPU_ISA_VERSION_ZHOUYI_V3_2_1;
 	}
 
 	return 0;
 }
 
-int zhouyi_get_hw_config_number(struct io_region *io)
-{
-	int high = 0;
-	int low = 0;
-	int isa_version = 0;
-	int aiff_feature = 0;
-	int tpc_feature = 0;
-
-	if (!io)
-		return 0;
-
-	isa_version = aipu_read32(io, ZHOUYI_ISA_VERSION_REG_OFFSET);
-	aiff_feature = aipu_read32(io, ZHOUYI_HWA_FEATURE_REG_OFFSET);
-	tpc_feature = aipu_read32(io, ZHOUYI_TPC_FEATURE_REG_OFFSET);
-
-	if (isa_version == 0)
-		high = (aiff_feature & 0xF) + 6;
-	else if (isa_version == 1)
-		high = (aiff_feature & 0xF) + 8;
-
-	low = (tpc_feature) & 0x1F;
-
-	return high * 100 + low;
-}
-
 int zhouyi_soft_reset(struct io_region *io, int offset, int delay_us)
 {
 	int ret = 0;
diff --git a/drivers/misc/armchina-npu/zhouyi/zhouyi.h b/drivers/misc/armchina-npu/zhouyi/zhouyi.h
--- a/drivers/misc/armchina-npu/zhouyi/zhouyi.h
+++ b/drivers/misc/armchina-npu/zhouyi/zhouyi.h
@@ -24,17 +24,9 @@
 
 #define ZHOUYI_ASID_COUNT                     4
 
-/*
- * Revision ID for V1 ~ V3_2
- */
-#define ZHOUYI_V1_REVISION_ID                 0x0
-#define ZHOUYI_V2_0_REVISION_ID               0x100
-#define ZHOUYI_V2_1_REVISION_ID               0x200
-#define ZHOUYI_V2_2_REVISION_ID               0x300
+/* Sky1 V3 revisions. */
 #define ZHOUYI_V3_REVISION_ID_R0P2            0x10000
 #define ZHOUYI_V3_REVISION_ID_R0P3            0x10003
-#define ZHOUYI_V3_2_REVISION_ID_R1P0          0x10210
-#define ZHOUYI_V3_2_REVISION_ID_R2P0          0x10220
 
 /*
  * Soft Reset
@@ -53,7 +45,6 @@
  */
 #define ZHOUYI_COMMAND_POOL_DEFAULT           0
 #define ZHOUYI_COMMAND_POOL_PCP               0
-#define ZHOUYI_V3_2_COMMAND_POOL_FULL         0xFFFF
 
 #define PARTITION_MODE_NONE                   0
 
@@ -90,13 +81,12 @@ void zhouyi_clear_qempty_interrupt(struct io_region *io);
 void zhouyi_clear_done_interrupt(struct io_region *io);
 void zhouyi_clear_excep_interrupt(struct io_region *io);
 int zhouyi_io_rw(struct io_region *io, struct aipu_io_req *io_req);
-int zhouyi_detect_aipu_version(struct platform_device *p_dev, int *version, int *config, int *rev);
+int zhouyi_detect_aipu_version(struct platform_device *p_dev, int *version, int *rev);
 #ifdef CONFIG_SYSFS
 int zhouyi_print_reg_info(struct io_region *io, char *buf, const char *name, int offset);
 int zhouyi_sysfs_show(struct io_region *io, char *buf);
 #endif
 int zhouyi_get_hw_version_number(struct io_region *io, int *rev);
-int zhouyi_get_hw_config_number(struct io_region *io);
 int zhouyi_soft_reset(struct io_region *io, int offset, int delay_us);
 int get_qos(u32 exec_flag);
 
